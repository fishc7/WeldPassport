# WeldPassport — индекс рабочих чатов

## Назначение

Этот файл фиксирует рабочие чаты и обсуждения по проекту WeldPassport.

Он нужен, чтобы не терять важные мысли, решения и договорённости при росте количества чатов.

Чаты используются как рабочие обсуждения.

Источником истины являются проектные файлы:

- [[docs/ARCHITECTURE|docs/ARCHITECTURE]]
- [[docs/project/DECISIONS|docs/project/DECISIONS]]
- [[docs/project/PROJECT_SUMMARY|docs/project/PROJECT_SUMMARY]]
- [[docs/project/ROADMAP|docs/project/ROADMAP]]
- [[AGENTS|AGENTS]]

## Главное правило

Если решение есть только в чате — оно ещё не считается закреплённым.

Решение считается закреплённым только после переноса в проектную документацию.

Правило работы:

```text
Обсудили в чате → приняли решение → перенесли в документацию → проверили git diff → сделали commit
```

---

## Статусы разбора чатов

Используются статусы:

```text
new          — чат ещё не разобран
in_progress  — из чата извлекаются решения
documented   — решения перенесены в документацию
archived     — чат сохранён как история, активных действий нет
```

---

## Рабочие чаты

### 1. Архитектура WeldPassport

Статус: `in_progress`

Назначение:

Обсуждение общей архитектуры WeldPassport, базовых сущностей, ролей, проектных решений и правил работы AI-агентов.

Ключевые темы:

- модель организаций и проектов;
- проектные решения и ADR;
- структура документации;
- правила фиксации решений;
- контроль Cursor Agent и Claude Code;
- разделение ответственности между ChatGPT, Cursor Agent и Claude Code.

Ключевые решения:

- не использовать модель «1 фирма = 1 проект»;
- использовать связь `companies ↔ projects` через `project_companies`;
- решения фиксировать в `docs/project/DECISIONS.md`;
- текущую архитектуру фиксировать в `docs/ARCHITECTURE.md`;
- правила для AI-агентов фиксировать в `AGENTS.md`;
- чат не считать единственным источником истины.

Связанные файлы:

- `docs/ARCHITECTURE.md`
- `docs/project/DECISIONS.md`
- `docs/project/PROJECT_SUMMARY.md`
- `docs/project/ROADMAP.md`
- `AGENTS.md`

Что ещё проверить:

- перенесена ли ролевая цепочка `ОК → ОГС → СМР → ПТО → ОТК → Закрытие` в `PROJECT_SUMMARY.md`;
- создан ли ADR по модели организаций и проектов;
- нет ли противоречий в `ARCHITECTURE.md`.

---

### 2. База данных WeldPassport

Статус: `new`

Назначение:

Обсуждение структуры базы данных, схем, таблиц, связей, ER-модели и миграций.

Ключевые темы:

- `companies`;
- `projects`;
- `project_companies`;
- `persons`;
- `employees`;
- `welders`;
- `welder_stamps`;
- `welder_certifications`;
- `welder_admissions`;
- `project_members`;
- `isometrics`;
- `weld_joints`;
- `ndt_reports`;
- `documents`.

Связанные файлы:

- `docs/ARCHITECTURE.md`
- `docs/project/DECISIONS.md`
- `docs/project/ROADMAP.md`
- будущие файлы в `docs/modules/`

Что ещё сделать:

- подготовить ER-диаграмму;
- определить минимальную модель MVP;
- отделить справочники от производственных документов;
- определить правила миграций.

---

### 3. Персонал, сварщики и клейма

Статус: `new`

Назначение:

Обсуждение модели персонала, кадровых данных, сварщиков, клейм, аттестаций и внутренних допусков ОГС.

Ключевые темы:

- человек создаётся отделом кадров;
- сварщиком человек становится после проверки ОГС;
- клеймо связано со сварщиком;
- аттестация имеет сроки действия;
- допуск может зависеть от проекта, материала, способа сварки, диаметра, толщины и срока.

Ключевые решения для проверки:

- роль ОК в создании человека;
- роль ОГС в допуске сварщика;
- возможность нескольких клейм или истории клейм;
- отличие аттестации от внутреннего допуска.

Связанные файлы:

- `docs/ARCHITECTURE.md`
- `docs/project/DECISIONS.md`
- `docs/project/ROADMAP.md`
- будущий файл `docs/modules/personnel.md`

---

### 4. Стыки, изометрии и сварочное производство

Статус: `new`

Назначение:

Обсуждение модели сварного соединения, связи стыка с проектом, изометрией, сварщиками, журналом сварки и исполнительной документацией.

Ключевые темы:

- проект;
- участок;
- линия;
- система;
- изометрия;
- чертёж;
- стык;
- диаметр;
- толщина;
- материал;
- тип соединения;
- способ сварки;
- дата сварки;
- сварщик корня;
- сварщик заполнения;
- статус стыка.

Связанные файлы:

- `docs/ARCHITECTURE.md`
- `docs/project/ROADMAP.md`
- будущий файл `docs/modules/welding.md`

---

### 5. НК, ОТК и качество

Статус: `new`

Назначение:

Обсуждение контроля качества, видов НК, результатов контроля, лабораторий, дефектов, ремонта и повторного контроля.

Ключевые темы:

- ВИК;
- РК;
- УЗК;
- ПВК;
- МК;
- процент контроля;
- результат контроля;
- дефект;
- ремонт;
- повторный контроль;
- связь с лабораторией НК;
- связь с ОТК.

Связанные файлы:

- `docs/ARCHITECTURE.md`
- `docs/project/ROADMAP.md`
- будущий файл `docs/modules/ndt.md`

---

### 6. Периодика КСС

Статус: `new`

Назначение:

Обсуждение правил периодики КСС, партий однотипных стыков, сроков, лимитов, брака и удвоения контроля.

Ключевые темы:

- ГОСТ 32569;
- партия однотипных стыков;
- срок партии не более 3 месяцев;
- лимиты по DN;
- один сварщик;
- одна марка стали;
- единый процесс сварки;
- толщина в пределах ±50%;
- удвоение контроля при браке;
- ближайшие КСС к контролю;
- просроченные КСС;
- КСС без МКК.

Связанные файлы:

- `docs/ARCHITECTURE.md`
- `docs/project/DECISIONS.md`
- `docs/project/ROADMAP.md`
- будущий файл `docs/modules/periodic-kss.md`

---

### 7. Нормирование сварочных работ

Статус: `deferred` (отложено — не входит в активный MVP, см. [[docs/project/DECISIONS#ADR-003. Исключение модуля нормирования из активного MVP|ADR-003]])

Назначение:

Обсуждение нормативов времени, операций, методов расчёта, интерполяции, материалов и коэффициентов.

Ключевые темы:

- ЕНиР-22;
- ЕНиР-26;
- ЕНиР-40;
- ВНиР-17-5;
- В10-1;
- MARK_PIPE;
- CUT_PIPE;
- EDGE_PREP;
- JOINT_ASSEMBLY;
- WELD_PIPE;
- QC_WELD;
- материалы M01, M11 и другие группы;
- коэффициенты материала;
- коэффициенты условий;
- интерполяция;
- нормы-аналоги.

Связанные файлы:

- `docs/project/DECISIONS.md` (ADR-003)
- `docs/ARCHITECTURE.md`
- `docs/project/ROADMAP.md`
- `10_Проектирование_WeldPassport/10_Нормы_времени/00_Нормы_времени.md`
- будущий файл `docs/modules/norms.md` (при возврате модуля в работу)

---

### 8. МТО, PDF и Excel

Статус: `new`

Назначение:

Обсуждение извлечения MTO из PDF-документов, обработки таблиц, сборки Excel и сверки номенклатуры.

Ключевые темы:

- извлечение строк MTO из PDF;
- `extract_mto.py`;
- `build_mto_xlsx.py`;
- `inspect_pdfs.py`;
- `outputs/mto_extract/mto_rows.json`;
- итоговая сводная МТО;
- справочник номенклатуры;
- сопоставление позиций;
- Excel-выгрузки.

Связанные файлы:

- отдельная документация по модулю МТО;
- будущий файл `docs/modules/mto.md`

---

### 9. Cursor Agent, Claude Code и контроль AI

Статус: `in_progress`

Назначение:

Обсуждение того, как использовать AI-инструменты в проекте без потери контроля над архитектурой.

Ключевые темы:

- ChatGPT отвечает за архитектуру, план и ревью;
- Cursor Agent выполняет точечные изменения в проекте;
- Claude Code может использоваться для реализации кода;
- все изменения проверяются через `git diff`;
- архитектуру нельзя менять без ADR;
- AI-агенты должны читать `AGENTS.md`.

Связанные файлы:

- `AGENTS.md`
- `docs/project/DECISIONS.md`
- `docs/project/ROADMAP.md`

---

### 10. B-04R — dual-state evidence PostgreSQL 18

Статус: `in_progress`

Назначение:

Разбор расхождения структурного fingerprint между рабочей базой после Alembic и
той же базой после `pg_dump` / `pg_restore`, выбор архитектурного контракта и
подготовка отдельного шлюза B-04R перед B-04B.

Ключевые темы:

- live migration-built fingerprint;
- restore-roundtrip fingerprint;
- PostgreSQL 18 deparser;
- точная типизированная карта допустимой эквивалентности;
- двухкратная проверка fixed point после восстановления;
- запрет ослаблять существующий B-04A-R18 evidence.

Ключевые решения:

- принят вариант 1: dual-state evidence contract;
- существующий `postgresql-18-fingerprint-v2` остаётся byte-identical и проверяет
  только рабочую migration-built базу;
- восстановленная база проверяется отдельным
  `postgresql-18-restore-roundtrip-v1`;
- B-04B запрещён до отдельной приёмки B-04R и повторной readiness-проверки.

Связанные файлы:

- `docs/project/ADR-031-b04-dual-state-live-restore-evidence.md`
- `docs/project/TASK_B-04R_RESTORE_ROUNDTRIP_EVIDENCE_SPEC.md`
- `docs/project/TASK_B-04R_RESTORE_ROUNDTRIP_EVIDENCE_IMPLEMENTATION_PLAN.md`
- `docs/project/TASK_B-04R_RESTORE_ROUNDTRIP_EVIDENCE_CLAUDE_CODE_PROMPT.md`
- `docs/project/TASK_B-04B_CUT_MAINTENANCE_ADOPTION_IMPLEMENTATION_PLAN.md`
- `docs/project/ROADMAP.md`
- `docs/project/PROJECT_STATUS.yaml`

Что ещё сделать:

- accepted documentation зафиксирована commit
  `08bc6a09974e0272ff27938511af5d4d6ba33403`;
- pure implementation B-04R завершена; focused `58 passed`, полный набор
  `378 passed, 1 skipped`;
- Diff и отдельная code acceptance B-04R завершены 2026-07-29;
- implementation commit разрешён и создан 2026-07-29;
- первый operator run остановлен до restore с `B04R-DISPOSABLE-SAFETY`;
- root cause: одиночный `%` в direct psycopg SQL;
- remediation реализована TDD и принята владельцем 2026-07-29;
- remediation commit разрешён и создан;
- получить отдельное принятие нового remediation SHA;
- после принятия SHA отдельно повторить operator run;
- только после этого отдельно разрешать DB run, генерацию и приёмку evidence;
- повторить B-04B readiness только после приёмки B-04R.

---

## Шаблон записи нового чата

Для новых рабочих чатов использовать шаблон:

```markdown
### N. Название чата

Статус: `new`

Назначение:

Кратко описать, для чего нужен чат.

Ключевые темы:

- тема 1;
- тема 2;
- тема 3.

Ключевые решения:

- решение 1;
- решение 2.

Связанные файлы:

- `docs/...`

Что ещё сделать:

- задача 1;
- задача 2.
```

---

## Правило закрытия чата

Перед тем как считать чат обработанным, нужно проверить:

- все важные решения перенесены в `docs/project/DECISIONS.md`;
- актуальная архитектура перенесена в `docs/ARCHITECTURE.md`;
- ближайшие задачи перенесены в `docs/project/ROADMAP.md`;
- правила для AI-агентов перенесены в `AGENTS.md`;
- при необходимости создан файл в `docs/modules/`;
- изменения проверены через `git diff`;
- сделан commit.

После этого статус чата можно изменить на:

```text
documented
```

или:

```text
archived
```
