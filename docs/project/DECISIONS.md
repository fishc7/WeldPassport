# WeldPassport — журнал проектных решений

## Назначение

Этот файл фиксирует важные проектные и архитектурные решения.

Формат записи — ADR: Architecture Decision Record.

Канон управления архитектурой — [[docs/project/ARCHITECTURE_GOVERNANCE|AGF]] ·
[[docs/project/CONSTITUTION#5. Architecture Governance|Конституция §5]] ·
[[docs/project/CONSTITUTION#4. Архитектурный принцип №2. Канонический язык проекта|§4 (принцип №2)]] ·
[[docs/project/ARCHITECTURE_SESSIONS|журнал сессий]] ·
[[docs/project/CONSTITUTION#2. Архитектурный принцип №0. Сначала архитектурная сессия — потом код|§2 (принцип №0)]]:
фундаментальные решения принимаются только в архитектурной сессии и фиксируются
в документации **до** реализации в коде.

ADR нужен, чтобы через время было понятно:

- какое решение принято;
- почему оно принято;
- какие варианты были отклонены;
- какие последствия у решения;
- где находится каноническое описание.

Канон архитектуры — [[docs/ARCHITECTURE|docs/ARCHITECTURE]]; **главный документ** —
[[docs/project/CONSTITUTION|CONSTITUTION]]. Контекст домена —
[[docs/00_PROJECT_CONTEXT|docs/00_PROJECT_CONTEXT]]. Краткая сводка — [[docs/project/PROJECT_SUMMARY|PROJECT_SUMMARY]];
план работ — [[docs/project/ROADMAP|ROADMAP]]. Карта чатов — [[docs/project/CHAT_INDEX|CHAT_INDEX]].
Тематические узлы производства: [[02_Процессы/Сварочные_операции|Сварочные операции]] ·
[[02_Процессы/Неразрушающий_контроль|НК]] · [[10_Проектирование_WeldPassport/09_Периодика_КСС/00_Периодика_КСС|Периодика КСС]] ·
[[10_Проектирование_WeldPassport/05_Исполнительная_документация/00_Исполнительная_документация|Исполнительная документация]] ·
[[10_Проектирование_WeldPassport/10_Нормы_времени/00_Нормы_времени|Нормы времени (backlog)]] ·
[[10_Проектирование_WeldPassport/11_МТО_и_материалы/00_МТО_и_материалы|МТО и материалы]].

---

## 2026-07-06 — Архитектурные принципы №0 и №1 (управление архитектурой)

**Дата:** 2026-07-06

**Контекст:** необходимо закрепить правила принятия фундаментальных архитектурных
решений и совместного проектирования до начала масштабной реализации производственных
модулей.

**Принятые решения:**

- **Принцип №0:** ни одно фундаментальное архитектурное решение не принимается в
  процессе написания кода; обязательный цикл — архитектурная сессия → ADR →
  Конституция → синхронизация документации → код.
- **Принцип №1:** архитектура формируется совместно экспертом предметной области,
  AI Chief Architect и инструментами реализации (Cursor / Claude Code).
- **Девиз проекта:** «Сначала производство. Потом архитектура. Потом код.»

**Где зафиксировано:** [[docs/project/CONSTITUTION#2. Архитектурный принцип №0. Сначала архитектурная сессия — потом код|Конституция §2–§3]] ·
[[docs/project/ARCHITECTURE_GOVERNANCE|AGF]] (отдельный ADR не создавался — правило уровня Конституции).

---

## 2026-07-01 — Завершение архитектурной ревизии

**Дата:** 2026-07-01

**Контекст:** завершена независимая архитектурная ревизия проекта; выводы
подтверждены тремя согласованными анализами (ChatGPT, Cursor Agent, Claude).

**Принятые решения:**

- Backend принят как основная архитектура.
- Документация больше не должна опережать код.
- Начинается реализация вертикальных бизнес-сценариев.
- Первый вертикальный сценарий — полный жизненный цикл сварного стыка.
- ETL откладывается.
- Import Pipeline остаётся внутри Backend.
- Cursor используется для реализации.
- Архитектурные решения принимаются только после ревизии.

**Связанные материалы:** `docs/ARCHITECTURE.md` (раздел «Архитектурная веха —
2026-07-01»).

---

## 2026-07-01 — Изменение стратегии разработки

**Дата:** 2026-07-01

**Контекст:** после независимой архитектурной экспертизы (ChatGPT, Cursor Agent,
Claude) пересмотрен порядок вертикальной реализации. Ранее первым сценарием
предполагался полный жизненный цикл сварного стыка (см. запись «Завершение
архитектурной ревизии» выше).

**Принятые решения:**

- Мы изменяем порядок разработки.
- Первым вертикальным сценарием становится полный жизненный цикл работника.
- Вторым вертикальным сценарием станет полный жизненный цикл сварного стыка.
- Контур допуска является фундаментом производственной системы WeldPassport.
- Это решение не отменяет центральную роль сварного стыка в производственном
  контуре.

**Причина:** контур допуска является фундаментом всей производственной системы
WeldPassport.

**Связанные материалы:** `docs/ARCHITECTURE.md` (подраздел «Приоритет первого
вертикального сценария» в разделе «Архитектурная веха — 2026-07-01»).

---

## ADR-001. Модель организаций и проектов

Дата: 2026-07-02

Статус: принято

Сводный узел: [[03_База_данных/Модель_организаций_и_проектов|Модель организаций и проектов]] ·
канон: [[docs/ARCHITECTURE#Модель организаций и проектов|Архитектура]].

### Контекст

В WeldPassport необходимо учитывать реальные строительные и сварочные проекты, где
в одном проекте могут участвовать разные организации: заказчик, генподрядчик,
сварочный подрядчик, лаборатория НК, технадзор, проектировщик и другие участники.

Модель «1 фирма = 1 проект» не подходит, потому что она ограничивает систему и не
отражает реальную структуру проекта.

### Решение

В системе используется модель связи «многие ко многим»:

```text
companies ↔ projects
```

Связь реализуется через таблицу:

```text
project_companies
```

Минимальные поля:

```text
project_id
company_id
role
```

Одна организация может участвовать во многих проектах.

Один проект может включать несколько организаций.

Роль организации в проекте фиксируется в поле `role`.

Примеры ролей:

```text
customer              — заказчик
general_contractor    — генподрядчик
welding_contractor    — сварочный подрядчик
ndt_lab               — лаборатория НК
inspection            — технадзор / инспекция
designer              — проектировщик
```

### Последствия

Модель позволяет корректно вести проекты с несколькими участниками.

В дальнейшем все модули WeldPassport должны использовать эту модель при работе с
проектами, организациями, документами, сварщиками, стыками, НК и исполнительной
документацией.

AI-агенты и разработчики не должны создавать реализацию по принципу «1 фирма = 1
проект».

Нельзя ограничивать проект одним полем:

```text
projects.company_id
```

Если нужно указать участников проекта, используется связующая таблица
`project_companies`.

### Где зафиксировано

Каноническое описание модели:

```text
docs/ARCHITECTURE.md
```

Правило для AI-агентов:

```text
AGENTS.md
```

---

## ADR-002. Двойной учёт фактического и документального сварщика

Дата: 2026-07-03

Статус: принято

Полный текст: [[docs/project/ADR-002-double-welder-accounting|ADR-002-double-welder-accounting.md]].

Сводный узел: [[02_Процессы/Сварочные_операции|Сварочные операции]] ·
[[10_Проектирование_WeldPassport/05_Исполнительная_документация/00_Исполнительная_документация|Исполнительная документация]] ·
канон: [[docs/ARCHITECTURE#6. Ключевые правила модели данных|Архитектура — разделение назначения и факта]].

### Контекст

В реальном производстве фактически сварку может выполнить один сварщик, а в
исполнительной документации указан другой сварщик или другое клеймо. Одно поле
«сварщик» в системе приводит к потере производственной или документальной правды.
WeldPassport обязан разделять факт (СМР), документацию (ПТО) и контроль
расхождений (ОГС, ОТК).

### Решение

Сварочная операция хранит **два независимых учёта**: фактический сварщик и
документальный сварщик (с клеймами). Система не подменяет одного другим; при
расхождении фиксируется `mismatch_status` и запись уходит на проверку.

Рекомендуемые поля: `actual_welder_id`, `actual_stamp_id`, `documented_welder_id`,
`documented_stamp_id`, `fact_confirmed_by_worker_id`,
`documentation_confirmed_by_worker_id`, `mismatch_status`, `mismatch_reason`.

Статусы расхождения: `OK`, `MISMATCH`, `NEEDS_REVIEW`, `APPROVED_EXCEPTION`,
`DOCUMENTATION_ERROR`, `FACT_ERROR`.

### Последствия

Раздельный учёт факта и ИД для стыка; контроль ответственности по ролевой цепочке;
основа для будущей модели `welding.weld_operations`, журналов сварки и закрытия ИД.

Код и миграции БД **не меняются** до отдельной задачи реализации.

### Где зафиксировано

```text
docs/project/ADR-002-double-welder-accounting.md
docs/ARCHITECTURE.md
```

---

## ADR: ОГС → Допуски сварщика v0.1

Дата: 2026-07-03

Статус: принято

Связанный коммит: `5f2da02` — feat: add OGS welder admissions MVP.

### Контекст

Backend v0.1 модуля допусков сварщика от ОГС реализован и запушен. Цель v0.1 —
минимальный рабочий внутренний допуск сварщика от ОГС, а не полная модель
удостоверений, клейм и аттестаций.

### Решение

1. Для v0.1 используется схема БД `welding`.

2. Основная таблица — `welding.welder_admissions`.

3. Структура v0.1 — плоская. Отдельные таблицы `welding.stamps` и
   `welding.certifications` пока не вводятся.

4. Причина: полная модель удостоверений, клейм и аттестаций избыточна для
   первой рабочей версии внутреннего допуска.

5. Связь с кадровым контуром:
   - `hr.workers` не трогаем;
   - `worker_id` ссылается на `hr.workers.id`;
   - `worker_id` имеет тип `Integer`, потому что `hr.workers.id` в текущей БД
     `Integer`.

6. UUID:
   - UUID используется для новой сущности `welding.welder_admissions`;
   - генерация UUID на v0.1 выполняется на уровне приложения.

7. API: `/api/v1/ogs/welder-admissions`.

### Последствия

Перенос на v0.2:

- проверка, что работник имеет роль `WELDER`, перед созданием допуска;
- возможное выделение отдельных сущностей `welding.stamps` и
  `welding.certifications`;
- возможная привязка допуска к `project_id` после появления проектного модуля.

### Где зафиксировано

```text
docs/project/DECISIONS.md
09_Разработка/backend/app/welding/
```

> **Дополнено ADR-004 (2026-07-06):** добавлена таблица `welding.welders`; правила
> `stamp_code` и предусловия допуска — см. [[docs/project/ADR-004-ogs-model-stabilization|ADR-004]].

---

## ADR-004. Стабилизация модели ОГС (welders, admissions, stamp_code)

Дата: 2026-07-06

Статус: принято

Полный текст: [[docs/project/ADR-004-ogs-model-stabilization|ADR-004-ogs-model-stabilization.md]].

### Суть

- `welding.welders` — профиль сварщика ОГС; **каноническое клеймо** — `welders.stamp_code`.
- `welding.welder_admissions` — допуск; `stamp_code` — копия для поиска/аудита, при
  создании совпадает с профилем.
- Создание допуска требует: работник HR + роль `WELDER` + активный профиль сварщика.

---

## ADR-005. Вывод legacy-модуля workforce из эксплуатации

Дата: 2026-07-06

Статус: принято

Полный текст: [[docs/project/ADR-005-legacy-workforce-deprecation|ADR-005-legacy-workforce-deprecation.md]].

### Суть

- `app.workforce` — **deprecated**; новые функции не добавляются.
- Целевые API: `/api/v1/hr` (ОК), `/api/v1/ogs` (ОГС).
- Постепенный вывод: desktop_ok → импортёры → снятие роутера.

---

## ADR-006. Матрица владения доменами

Дата: 2026-07-06

Статус: принято

Полный текст: [[docs/project/ADR-006-domain-ownership-matrix|ADR-006-domain-ownership-matrix.md]].

### Суть

- Стык — центральная производственная сущность; точка схода доменов.
- WPS, PQR, технология сварки — **только ОГС**; ПТО не принимает технологических решений.
- СМР владеет фактом выполнения; ОТК — статусом качества и приёмкой; НК — протоколами и результатами методов НК.
- МТО владеет поставками, партиями, сертификатами; ОГС задаёт технологические требования к материалам.
- ИД — структурированный результат процесса (комплект + связи с фактами), не файловый архив.

Канон границ доменов для всех последующих модулей (`production`, `quality`, `documents`, `mto`).

---

## ADR-007. Жизненный цикл стыка и инженерная модель

Дата: 2026-07-06

Статус: принято

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 001|Session 001]]

Полный текст: [[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007-joint-lifecycle-and-engineering-model.md]].

### Суть

- **Joint** — инженерная сущность из утверждённой РД, **центральный объект
  производственного процесса**; все домены работают относительно конкретного стыка.
- События жизненного цикла Joint: WeldOperation, Inspection, NDTInspection,
  RepairOperation, ExecutiveDocumentation (`production`, `quality`, `documents`).
- Стык рождается из **утверждённой РД**, никогда — из факта выполнения.
- Неизменяемый `joint_id`; `project_joint_no` — бизнес-атрибут, уникален только внутри иерархии.
- Каноническая иерархия: Проект → Титул/Установка/Блок → Объект → Линия → Изометрия → Стык.
- Стыки не удаляются; при смене РД — `CANCELLED` или `SUPERSEDED`; существенное изменение → новый Joint.
- Сварщик — участник **WeldOperation**, не свойство стыка; несколько операций и сварщиков на стыке.
- Три типа сущностей вокруг Joint (инженерная / события жизненного цикла / ИД) —
  без смешения ролей; `production` работает **со стыком**, а не вместо него.

Уточняет ADR-006: создание Joint — `engineering`; события жизненного цикла —
`production`, `quality`, `documents`.

---

## ADR-008. Каноническая модель предметной области WeldPassport (Session 003)

Дата: 2026-07-08

Статус: принято — закрывает Architecture Session 003 (решения 003-A — 003-AM)

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 003|Session 003]]

Уточняет: [[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007]] ·
[[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] ·
[[docs/project/ADR-002-double-welder-accounting|ADR-002]]

Канон: [[docs/ARCHITECTURE#5.1. Каноническое ядро engineering-модели (Session 003)|ARCHITECTURE §5.1]]

### Контекст

Session 002 выявила расхождение между упрощённой иерархией
`Project → Line → Isometric → Joint` и реальными производственными документами.
Session 003 фиксирует каноническую предметную модель engineering-контура для MVP:
как Joint появляется, к чему привязан, как идентифицируется, как классифицируется
и как хранит геометрию и материал.

Код, миграции и API **не меняются** до отдельной задачи реализации.

---

### 003-A. СМР и FOREMAN

**СМР** не является отдельной системной ролью. СМР — производственный контур /
зона ответственности, где выполняются строительно-монтажные и сварочные работы.

Факт выполнения сварки от имени СМР подтверждает работник с ролью **FOREMAN**
(мастер / прораб). Для MVP роль FOREMAN достаточна. Роль `SITE_MANAGER` может
быть добавлена позже для разделения мастера, прораба и начальника участка.

Цепочка ответственности:

```text
ОК → ОГС → СМР → ПТО → ОТК/НК → Закрытие
```

- ОК создаёт работника.
- ОГС допускает сварщика.
- СМР выполняет производство.
- FOREMAN подтверждает факт сварки от имени СМР.
- ПТО оформляет исполнительную документацию.
- ОТК/НК подтверждают качество.
- Закрытие собирает полную историю стыка.

---

### 003-B. Минимальная инженерная привязка Joint

Joint **не может быть создан** без привязки к `Project` и минимальному инженерному
источнику.

Для MVP допускается рабочая инженерная привязка в статусе `draft` на основании
данных СМР: объект, участок, Line, Drawing, Isometric или другой рабочий
инженерный источник. ПТО **не обязано** заранее предоставить подтверждённую
исполнительную структуру, чтобы СМР мог зафиксировать производственный стык.

До закрытия исполнительной документации инженерная привязка Joint должна быть
проверена, уточнена и подтверждена уполномоченной ролью:

- **ПТО** — для целей исполнительной документации;
- **ОГС** — для целей сварочного производства, технологии сварки, допуска сварщика
  и корректного учёта стыка.

Минимальные статусы инженерной привязки:

| Статус | Значение |
|--------|----------|
| `draft` | рабочая инженерная привязка |
| `confirmed` | подтверждённая инженерная привязка |

---

### 003-C. EngineeringDocument

В MVP минимальным инженерным источником для создания Joint является универсальная
сущность **EngineeringDocument**.

`EngineeringDocument` хранит рабочий или подтверждённый инженерный источник.
Типы документа (`document_type`):

```text
isometric
drawing
line_list
isometric_register
joint_register
weld_map
field_sketch
repair_scheme
as_built_markup
other_engineering_doc
```

Joint должен быть связан минимум с `Project` и `EngineeringDocument`.
Joint **не создаётся** напрямую «в проекте» — только через инженерное основание.

---

### 003-D. Line как отдельная сущность

**Line** — самостоятельная инженерная сущность WeldPassport. Line **не хранится**
только как текстовое поле в `EngineeringDocument`.

Line создаётся на основании инженерных документов: List of pipelines, Перечень
трубопроводов, Экспликация трубопроводов, Line List, Piping Line List и аналогов.

Line содержит инженерные и нормативные параметры трубопровода:

```text
line_number, материал, среда, DN, давление, температура, категория,
класс трубопровода, процент НК, PMI, термообработка, МКК, изоляция, статус
```

Joint должен быть связан с `Line`, если стык относится к трубопроводу.

---

### 003-E. Isometric как EngineeringDocument

**Isometric** не вводится как отдельная корневая сущность в MVP. Isometric хранится
как `EngineeringDocument` с `document_type = isometric`.

Связь между `EngineeringDocument` и `Line` — через связующую сущность
**EngineeringDocumentLine** (не one-to-one): одна изометрия обычно относится к одной
Line, но иногда — к нескольким.

Правила:

- `EngineeringDocument` может быть связан с одной или несколькими `Line`;
- Joint **всегда** имеет конкретную `Line`;
- если документ связан с несколькими Line, пользователь обязан выбрать Line для Joint.

---

### 003-F. DocumentationPackage

Вводится сущность **DocumentationPackage** — объект учёта передачи документации.
Пакет **не означает** обязательный полный комплект.

В пакет может входить: один документ, несколько документов, полный или частичный
комплект, документ без последующего полного комплекта.

`EngineeringDocument` может существовать:

1. самостоятельно;
2. внутри частичного пакета;
3. внутри полного пакета;
4. сначала самостоятельно, затем быть привязанным к пакету.

Пакет не заменяет `EngineeringDocument` — пакет объединяет документы.

Статусы пакета: `received`, `in_work`, `superseded`, `cancelled`.

Статус полноты: `partial`, `complete`, `unknown`.

---

### 003-G. Источник создания Joint

Joint может создаваться **не только** из Isometric. Joint создаётся из любого
`EngineeringDocument`, если документ связан с `Line` и содержит достаточную
информацию для идентификации стыка или рабочего участка.

Isometric — основной, но **не единственный** источник Joint.

**Может создавать Joint:** `isometric`, `joint_register`, `weld_map`, `field_sketch`,
`repair_scheme`, `as_built_markup`, монтажный чертёж со стыками / участками.

**Обычно не создаёт Joint напрямую:** `line_list`, перечень трубопроводов,
экспликация трубопроводов, `isometric_register` — они создают или уточняют
`Line`, `EngineeringDocument` и документационный контекст.

---

### 003-H. Один основной EngineeringDocument для Joint

Joint имеет один основной `source_engineering_document_id` — главное инженерное
основание, по которому Joint был создан или идентифицирован.

Дополнительные документы участвуют в проверке, уточнении, исполнительном оформлении
или истории изменений, но **не являются** равноправными основными источниками.

---

### 003-I. Уникальность JointNumber

`joint_number` **может повторяться** — номер стыка не является глобально уникальным.

Уникальность Joint в MVP определяется составным контекстом:

```text
UNIQUE(project_id, line_id, source_engineering_document_id, joint_number)
```

**Запрещено:**

- уникальность только по `joint_number`;
- уникальность только по `line_id + joint_number`.

Уточняет правило `project_joint_no` из ADR-007: бизнес-номер уникален в составе
проекта, линии и инженерного основания.

---

### 003-J. Многоуровневая классификация типа соединения Joint

Тип соединения Joint — **многоуровневый**; нельзя хранить всё в одном плоском enum.

1. **WeldShapeType** — геометрический тип: С (стыковой), У (угловой), Т (тавровый),
   Н (нахлёсточный), П (прорезной).
2. **WeldJointDesignType** — нормативное / конструктивное обозначение: С1, С2, С17,
   У1, Т3, Н4 и др. по ГОСТ или проекту.
3. **ProjectJointType** — проектное, международное или заказное обозначение:
   BW, SW, FW и др.

BW / SW / FW не являются ошибкой — хранятся как `ProjectJointType` или alias,
связываются с нормативным типом через правила нормализации.

Нормативные источники: ГОСТ 16037-80 (трубопроводы), ГОСТ 5264-80
(нетрубопроводные ручные дуговые соединения).

Сущность **WeldJointTypeAlias:**

```text
source_text, normalized_shape_type, normalized_design_type,
project_joint_type, mapping_status
```

Статусы сопоставления: `mapped`, `requires_review`, `project_specific`, `unknown`.

---

### 003-K. Геометрия и материал Joint

Joint хранит собственные значения:

```text
diameter_dn, diameter_outer, thickness, material_id,
material_text_source, size_text_source
```

Диаметр, толщина и материал могут подтягиваться из спецификации изометрии / чертежа,
номенклатуры, Line List, ведомости стыков, ручного уточнения ОГС / ПТО — но после
создания являются **атрибутами самого Joint**.

`Line` даёт общий инженерный контекст трубопровода; Joint — конкретные параметры
соединения.

---

### 003-L. Материалы Joint

Joint хранит материал как нормализованную ссылку на **Material**. `Material` связан
с **MaterialGroup** (сварочная / аттестационная группа: М01, М02, М03, М04, М05,
М11, М21, М31, М32, М33, М41, М51 и др.).

`MaterialGroup` используется для ОГС, WPS, допуска сварщика, проверки технологии,
отчётности. Исходное текстовое значение из документа сохраняется отдельно.

Если материал не распознан при импорте — сохраняется как `source_text`, статус
`requires_mapping_review`; ОГС нормализует вручную.

Минимальные сущности:

**MaterialGroup:** `code`, `name`, `description`, `material_class`, `source`

**Material:** `code`, `name`, `group_id`, `standard`, `is_active`, `source`

**MaterialAlias:** `source_text`, `material_id`, `confidence`, `status`

Связь с допусками: `WelderAdmission.allowed_material_groups` проверяется против
`Joint.material_id → Material → MaterialGroup`.

---

### 003-M. Один Joint может иметь несколько WeldOperation

Один `Joint` может иметь несколько `WeldOperation`.

`Joint` — инженерно-производственная сущность стыка, объект учёта.  
`WeldOperation` — производственное событие выполнения сварки над Joint.

`Joint ≠ WeldOperation`.

На один Joint могут приходиться: первичная сварка, корень, заполнение, облицовка,
комбинированный процесс RAD + RD, ремонтная заварка, повторная заварка после
дефекта, сварка после вырезки / переварки.

---

### 003-N. Комбинированная сварка RAD + RD

Для комбинированной сварки RAD + RD используется модель нескольких `WeldOperation`
на один Joint.

Корень, заполнение и облицовка фиксируются как отдельные события, если различаются
по способу сварки, сварщику, клейму, дате, WPS или технологическому этапу.

RAD + RD может отображаться как комбинированный способ в UI, но внутри модели
раскладывается на отдельные операции.

Пример:

```text
Joint W-001
- WeldOperation #1: layer_type = root, welding_process = RAD
- WeldOperation #2: layer_type = fill_cap, welding_process = RD
```

Минимальные `layer_type` для MVP: `root`, `fill_cap`, `repair`.

---

### 003-O. WPS при фиксации WeldOperation

`WeldOperation` может быть создана СМР без подтверждённой WPS/технологической карты
на этапе первичной фиксации факта.

На старте WPS может быть:

- не указана;
- указана предварительно;
- указана как предполагаемая;
- добавлена позже ОГС.

СМР фиксирует факт. ОГС подтверждает технологическую корректность.

WPS не обязательна для фиксации факта `WeldOperation`, но технологическое
подтверждение обязательно для закрытия Joint, исполнительной документации и
финального паспорта соединения.

Минимальные статусы WPS: `not_assigned`, `proposed`, `assigned`, `approved`, `rejected`.

Минимальные статусы технологической проверки ОГС:
`pending`, `approved`, `rejected`, `requires_fix`.

---

### 003-P. Факт сварки, выработка, брак и проверка допуска

СМР может фиксировать факт сварки без мгновенной проверки допуска сварщика.
Такая `WeldOperation` используется для производственного учёта, выработки,
статистики брака и отчётности.

Факт сварки нельзя терять или блокировать, но факт сварки не равен технологически
подтверждённой сварке.

Если допуск/WPS/проектные требования ещё не проверены, операция получает статус
проверки `requires_ogs_review`.

Без проверки ОГС `WeldOperation` не используется для финального закрытия Joint,
исполнительной документации и паспорта сварного соединения.

Разделяются четыре уровня проверки:

1. внутренний допуск ОГС;
2. аттестация/удостоверение сварщика;
3. допуск заказчика на проект;
4. соответствие конкретному Joint (материал, способ, диаметр, толщина, тип).

---

### 003-Q. Defect и журнал ремонта

При выявлении брака создаётся отдельная сущность `Defect`.

Журнал ремонта фиксирует не сам факт брака, а действия по устранению дефекта через
`RepairOperation`, которая должна ссылаться на `Defect`.

Источник истины:

- `Defect` — что обнаружено;
- `RepairOperation` — что сделано для устранения;
- `Inspection` / `NDTInspection` — контроль до и после ремонта.

---

### 003-R. RepairOperation и ремонтная WeldOperation

Ремонт дефекта фиксируется отдельной сущностью `RepairOperation`.

Если ремонт включает сварку, внутри `RepairOperation` создаётся отдельная
`WeldOperation` с `operation_type = repair`.

`RepairOperation ≠ WeldOperation`.

`RepairOperation` отвечает за контекст ремонта (какой дефект, каким способом, с
каким результатом), `WeldOperation` — за сварочную часть (кто, чем, когда, каким
клеймом).

---

### 003-S. Inspection и NDTInspection

Контроль качества разделяется на общий `Inspection` и специализированный
`NDTInspection`.

- `Inspection` — событие контроля;
- `NDTInspection` — детализация НК;
- `Defect` — обнаруженный брак.

Повторный контроль после ремонта фиксируется как новый `Inspection`/`NDTInspection`.

Примеры `inspection_type`: `visual`, `ndt`, `pmi`, `hardness`, `operational`, `acceptance`.

Примеры `ndt_method`: `RT`, `UT`, `PT`, `MT`, `VT` (если проект относит ВИК к НК).

---

### 003-T. HeatTreatmentOperation

Термообработка фиксируется как отдельная сущность `HeatTreatmentOperation`.

Это производственно-технологическое событие жизненного цикла Joint; оно не является
видом `Inspection`.

Журнал термообработки строится на основании `HeatTreatmentOperation`.

---

### 003-U. HeatTreatmentOperation может включать несколько Joint

Одна `HeatTreatmentOperation` может быть связана с одним или несколькими Joint.

Одна диаграмма/цикл термообработки может относиться к группе стыков.
Связь реализуется через `HeatTreatmentOperationJoint`.

---

### 003-V. Один Joint может иметь несколько HeatTreatmentOperation

Один Joint может участвовать в нескольких `HeatTreatmentOperation`
(после ремонта, после неудовлетворительной твёрдости, при отклонении режима и др.).

Причина повторной термообработки фиксируется явно в `reason`:
`initial_required`, `after_repair`, `hardness_failed`, `mode_deviation`,
`customer_requirement`, `ogs_decision`, `other`.

Допускаются ссылки:

- `previous_heat_treatment_operation_id` (nullable),
- `triggered_by_inspection_id` (nullable),
- `triggered_by_repair_operation_id` (nullable).

---

### 003-W. HardnessInspection

Замер твёрдости — вид `Inspection` с отдельной детализацией в сущности
`HardnessInspection`.

Минимальные поля:

```text
inspection_id
heat_treatment_operation_id (nullable)
method
unit (HB / HRC / HV)
measured_value
acceptance_limit_min
acceptance_limit_max
result
conclusion
```

Позже может быть добавлена сущность `HardnessMeasurementPoint` для точек измерения
(`weld` / `haz` / `base_metal`).

Неудовлетворительный `HardnessInspection` может быть основанием для повторной
`HeatTreatmentOperation`.

---

### 003-X. Общий механизм файлов: DocumentFile + Attachment

В системе используется общий механизм файлов:

- `DocumentFile` — описание файла;
- `Attachment` — привязка файла к конкретной бизнес-сущности.

Отдельные поля `*_file_id` в каждой сущности не используются.

Примеры связей: изометрия → `EngineeringDocument`, заключение РК → `NDTInspection`,
диаграмма ТО → `HeatTreatmentOperation`, протокол твёрдости → `HardnessInspection`,
фото дефекта → `Defect`, акт ремонта → `RepairOperation`, WPS → `WPS`,
скан допуска → `WelderAdmission/Certification`, исполнительный PDF → `DocumentationPackage`.

Роли вложений: `source_document`, `report`, `diagram`, `photo`, `scan`, `act`,
`certificate`, `wps_document`, `as_built`, `other`.

---

### 003-Y. Статусы Joint разделяются по контурам

Один универсальный статус для Joint не используется.

Для MVP вводятся отдельные контуры статусов:

- `production_status`;
- `ogs_review_status`;
- `inspection_status`;
- `repair_status`;
- `heat_treatment_status`;
- `documentation_status`;
- `closure_status`.

Примеры:

- `production_status`: `not_started`, `in_progress`, `welded`, `foreman_confirmed`, `cancelled`;
- `ogs_review_status`: `not_required`, `requires_review`, `approved`, `rejected`, `requires_fix`;
- `inspection_status`: `not_required`, `pending`, `in_progress`, `accepted`, `defect_found`, `reinspection_required`;
- `repair_status`: `not_required`, `open_defect`, `repair_in_progress`, `repaired`, `closed`;
- `heat_treatment_status`: `not_required`, `required`, `planned`, `completed`, `accepted`, `rejected`, `repeat_required`;
- `documentation_status`: `not_started`, `in_progress`, `ready_for_review`, `accepted`, `returned_for_fix`;
- `closure_status`: `open`, `blocked`, `ready_to_close`, `closed`.

---

### 003-Z. Финальное закрытие Joint

Финальное закрытие подтверждает отдельная роль `CLOSURE_RESPONSIBLE`.

Роль не заменяет ПТО, ОТК, НК, ОГС или СМР; она подтверждает, что все обязательные
контуры завершены и Joint готов к финальному закрытию.

Перед `closure_status = closed` система проверяет:

- есть инженерная привязка Joint;
- зафиксирован факт сварки;
- факт подтверждён FOREMAN;
- ОГС проверил WPS/допуск/технологическую корректность;
- выполнен контроль качества;
- дефекты, если были, закрыты;
- ремонт и повторный контроль после ремонта завершены;
- термообработка и контроль твёрдости (если требуются) приняты;
- исполнительная документация ПТО готова/принята;
- обязательные файлы и заключения приложены.

---

### 003-AM. RACI модель доменных сущностей

Для Production/Joints MVP вводится RACI-модель ответственности по ключевым
сущностям жизненного цикла Joint. Матрица по сущностям зафиксирована в решениях
**003-AB — 003-AL**; здесь — сводные архитектурные правила.

**Совместная ответственность ПТО + ОГС**

- **ПТО** и **ОГС** совместно подотчётны (**A**) за подтверждение инженерной
  привязки Joint (`engineering_status = confirmed`).
- **ПТО** подотчётен за Project, EngineeringDocument, Line и документационный контур.
- **ОГС** отвечает за технологическую корректность: WPS, допуск сварщика, материал,
  способ, параметры стыка.
- **DocumentationPackage** — связанный объект учёта передачи комплектов; не входит
  в базовый список из 10 RACI-сущностей (см. 003-F, 003-AL).

**Ответственность СМР за факт**

- **СМР** — ответственный (**R**) за фиксацию `WeldOperation`, ремонтных операций
  и термообработки на объекте.
- **FOREMAN** подтверждает факт сварки; без подтверждения FOREMAN производственный
  факт не считается завершённым для закрытия.
- Факт сварки не блокируется из-за непроверенного допуска, но до закрытия требует
  review ОГС (см. 003-P, 003-O).

**Ответственность ОТК / НК за контроль**

- **ОТК** подотчётен (**A**) за `Inspection`, `Defect`, приёмку и `RepairOperation`
  как производственное решение о годности.
- **НК** отвечает за выполнение и протоколы методов НК в рамках `Inspection`;
  **NDTInspection** и **HardnessInspection** — специализированные расширения
  `Inspection`, а не отдельные пункты базового RACI-списка (см. 003-S, 003-W).

**Файлы и закрытие**

- **Attachment / DocumentFile** — единый файловый механизм; ответственность определяется
  владельцем связанной бизнес-сущности (см. 003-X, 003-AK).
- **Joint closure** — процесс финального закрытия стыка; **CLOSURE_RESPONSIBLE**
  подотчётен (**A**) за `closure_status = closed`, не подменяя ПТО, ОТК, НК, ОГС
  или СМР и не заменяя файловый контур (см. 003-Z, 003-AL).
- До закрытия должны быть завершены: инженерная привязка, факт сварки, review ОГС,
  контроль, ремонт (если был), термообработка и твёрдость (если требуются), ИД ПТО,
  обязательные вложения.

Полная RACI-таблица по 10 MVP-сущностям — см.
[[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 003 — RACI модель Production/Joints MVP|Session 003 — RACI]].

**Итог ADR-008:** ADR-008 завершает Architecture Session 003 и является основанием
для проектирования Production/Joints MVP.

---

### Последствия

- Целевая схема `engineering.*` должна включать `engineering_documents`,
  `lines`, `engineering_document_lines`, `documentation_packages`, `joints`,
  справочники типов соединения и материалов.
- Целевая lifecycle-модель вокруг Joint должна включать: `weld_operations`,
  `inspections`, `ndt_inspections`, `hardness_inspections`, `defects`,
  `repair_operations`, `heat_treatment_operations`, `heat_treatment_operation_joints`,
  `document_files`, `attachments`.
- Для закрытия стыка вводится отдельный контур `closure_status` и роль
  `CLOSURE_RESPONSIBLE` как финальный подтверждающий контур.
- RACI-модель Production/Joints MVP (003-AB — 003-AM) задаёт ответственность
  по доменным сущностям для проектирования RBAC и workflow.
- ADR-007 сохраняет силу по `joint_id`, жизненному циклу и неизменяемости истории;
  правила появления Joint и иерархия уточняются через `EngineeringDocument` и
  статусы `draft` / `confirmed`.
- Реализация в коде — после завершения Session 003 и синхронизации документации.

### Где зафиксировано

```text
docs/project/ARCHITECTURE_SESSIONS.md (Session 003)
docs/project/DECISIONS.md (ADR-008)
docs/ARCHITECTURE.md (§5.1, §5.2)
docs/project/PROJECT_SUMMARY.md
```

---

## ADR-009. Production/Joints MVP — физическая модель БД, события и API

Дата: 2026-07-10

Статус: принято — закрывает Architecture Session 004 (решения 004-01 — 004-27)

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 004|Session 004]]

Уточняет: [[docs/project/DECISIONS#ADR-008. Каноническая модель предметной области WeldPassport (Session 003)|ADR-008]] ·
[[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007]] ·
[[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] ·
[[docs/project/ADR-002-double-welder-accounting|ADR-002]] ·
[[docs/project/DECISIONS#ADR-004. Стабилизация модели ОГС (welders, admissions, stamp_code)|ADR-004]]

Канон: [[docs/ARCHITECTURE#5.3. Физическая модель БД и API Production/Joints MVP (Session 004)|ARCHITECTURE §5.3]]

### Контекст

Session 003 (ADR-008) зафиксировала каноническую предметную модель Production/Joints
MVP. Session 004 переводит её в физическую схему PostgreSQL, границы модулей backend,
контракт REST API и правила переходов состояний.

Источники требований: реальные журналы сварки, ремонта, контроля, термообработки,
справочники типов соединений, данные сварщиков и материалов.

Код, миграции и тесты **не менялись** в рамках Session 004.

### Решение

#### Схемы и владение данными

| Схема | Сущности |
|-------|----------|
| `project` | Project, Line |
| `engineering` | EngineeringDocument, DocumentRevision, Joint |
| `production` | WeldOperation, RepairOperation, HeatTreatmentOperation |
| `quality` | Inspection, Defect |
| `documents` | DocumentFile, технические связи файлов |
| `hr`, `welding` | существующие контуры (без изменений) |

- **Joint** — инженерная сущность в `engineering`; центральный объект процесса.
- Производственные факты — `production`; контроль и дефекты — `quality`;
  файлы — `documents`.
- `engineering_status` хранится на Joint; `production_state` и `ready_for_welding`
  **вычисляются** API, в БД не хранятся.

#### Joint

- Двухэтапное создание: **FOREMAN** / **MASTER** создаёт `DRAFT`; **ПТО** подтверждает.
- Бизнес-ключ: `project_id` + `engineering_document_id` + `joint_no` (уточняет ADR-008, 003-I).
- `engineering_status`: `DRAFT`, `CONFIRMED`, `CANCELLED`, `SUPERSEDED`.
- При новой ревизии РД: неизменившийся Joint сохраняет UUID; удалённый — `CANCELLED`;
  заменённый — `SUPERSEDED` + `superseded_by_joint_id`; существенно изменённый — новый UUID.
- Геометрия (`geometry_type`) и форма шва (`weld_type`) разделены; материалы — каталог
  + исторический снимок на каждую сторону.

#### WeldOperation

- Несколько операций на Joint; этапы MVP: `ROOT`, `FILL`, `COVER`, `FILL_COVER`.
- `actual_welder_id` и `documented_welder_id` разделены (ADR-002).
- Допуск сварщика проверяется **строго** перед созданием операции; обход в MVP не предусмотрен.
- `Joint.planned_wps_id` (ОГС) и `WeldOperation.actual_wps_id` (факт) разделены.
- Статусы: `DRAFT`, `CONFIRMED`, `VOIDED`; подтверждённая операция неизменяема;
  исправление — новая операция с `replaces_operation_id`.
- Сварочные материалы — снимок в `weld_consumable_usages` (техническая таблица).

#### Контроль, ремонт, термообработка

- **Inspection** едина для всех методов (`VT`, `RT`, `UT`, `PT`, `MT`, `HARDNESS`, `PMI`, `FERRITE`).
- Подтверждённый `FAIL` требует минимум один **Defect**.
- **RepairOperation** связывается с Defect; ремонтная сварка — `WeldOperation` с
  `repair_operation_id`; Defect закрывается повторным `PASS`, не самим ремонтом.
- **HeatTreatmentOperation** — повторяемое событие; контроль твёрдости — `Inspection.HARDNESS`.
- Роль `HEAT_TREATMENT_OPERATOR` для регистрации термообработки.

#### API

Группы: `/api/v1/projects`, `/engineering`, `/production`, `/quality`, `/documents`.

Сущности — самостоятельные коллекции; команды `confirm` / `void` / `cancel` / `supersede`;
`PATCH` только для черновиков. Ответ Joint включает вычисляемые поля состояния.

#### Импорт Excel

Не входит в начальный MVP; отложен до стабилизации модели. Окончательный вариант
(прямой импорт или через промежуточную проверку) — отдельное будущее решение.

### Отклонённые варианты

1. **Хранение `production_state` как редактируемого поля** — отклонено: риск расхождения с фактами.
2. **Отдельные сущности NDTInspection / HardnessInspection в MVP** — отклонено: единая Inspection.
3. **Создание WeldOperation без проверки допуска** — отклонено: строгая проверка обязательна.
4. **Импорт Excel в начальном MVP** — отложено.
5. **Физическое удаление подтверждённых производственных записей** — отклонено.

### Последствия

- Потребуется добавить схемы `project`, `engineering`, `production`, `quality`, `documents`.
- Потребуется новая роль `HEAT_TREATMENT_OPERATOR`.
- Реализацию нужно разделить на несколько последовательных этапов (implementation plan).
- `hr` и `welding` остаются каноническими владельцами работников, сварщиков, допусков и WPS.
- Будущий импорт Excel **не должен** обходить доменные проверки.
- ADR-008 сохраняет силу по предметной модели; ADR-009 уточняет физическую реализацию.

### Где зафиксировано

```text
docs/project/ARCHITECTURE_SESSIONS.md (Session 004)
docs/project/DECISIONS.md (ADR-009)
docs/ARCHITECTURE.md (§5.3, активное ядро MVP, API)
docs/project/PROJECT_SUMMARY.md
docs/project/UBIQUITOUS_LANGUAGE.md
```

---

## ADR-010. Joint MVP — расширенная модель, двойное согласование, история ревизий и bulk-импорт

Дата: 2026-07-11

Статус: **принято** (замещает Joint-часть ADR-009; решения ADR-009
по EngineeringDocument и DocumentRevision **сохраняют силу**)

Замещает частично: [[docs/project/DECISIONS#ADR-009. Production/Joints MVP — физическая модель БД, события и API|ADR-009]] (только раздел Joint)

Уточняет: [[docs/project/DECISIONS#ADR-008. Каноническая модель предметной области WeldPassport (Session 003)|ADR-008]] ·
[[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007]] · IP-05, IP-07, IP-08

Канон реализации: [[docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP|Engineering Joints MVP — Implementation Plan]] (Tasks 5A, 5B, 6, 7)

### Контекст

ADR-009 (Session 004) зафиксировал минимальную модель Joint: одностадийное
подтверждение ПТО, статусы `DRAFT / CONFIRMED / CANCELLED / SUPERSEDED`, бизнес-ключ
`project_id + engineering_document_id + joint_no`, тонкая техническая связь
`joint_revision_links`. При проработке реализации выявлена потребность в более
полной модели производственного стыка:

- раздельное согласование двух служб — **ПТО** (документарная зона) и **ОГС**
  (технологическая зона), с автоматическим переходом в рабочее состояние только
  при подтверждении обеими;
- устойчивая идентичность Joint при выпуске новых ревизий инженерного документа с
  **неизменяемыми снимками** параметров на момент связи;
- **массовый ввод** стыков из одной ревизии документа с гарантией атомарности и
  идемпотентности.

Эти требования расходятся с Joint-частью ADR-009 и по правилу AGENTS.md фиксируются
отдельным ADR **до** написания кода. ADR-010 замещает **только** раздел Joint
ADR-009; решения ADR-009 по `EngineeringDocument` и `DocumentRevision` (реализованы
в Task 4, commit `6d2ea09`) остаются в силе без изменений.

### Решение

#### Базовые соглашения (Р-1 — Р-4)

- **Р-1. `hr.worker_roles.scope_type`** — значение `ENGINEERING_DOCUMENT` добавляется
  **аддитивно** к фактическому набору `GLOBAL, COMPANY, PROJECT, SITE, LINE`.
  Значения `COMPANY` и `SITE` сохраняются. Переименования и миграции данных нет
  (значения `ISOMETRIC`/`UNIT` в текущей схеме не существует). При
  `scope_type = ENGINEERING_DOCUMENT` поле `scope_id` содержит id `EngineeringDocument`
  (строковый UUID); отдельное поле не вводится.
- **Р-2. Роли** — используются существующие технические `role_code` `PTO_ENGINEER`
  (предметно «ПТО») и `OGS_ENGINEER` (предметно «ОГС»). Новые `role_code` `PTO`/`OGS`
  **не** вводятся (IP-07). Создавать Joint могут `MASTER`, `FOREMAN`, `PTO_ENGINEER`,
  `OGS_ENGINEER`; подтверждать ПТО — только `PTO_ENGINEER`; подтверждать ОГС —
  только `OGS_ENGINEER`.
- **Р-3. Сущности** — используются фактические `Line` (`project.lines`, не
  «ProjectLine»), `Project` (`project.projects`, PK UUID, есть `code`), `Worker`
  (`hr.workers`, PK Integer). Поля-акторы Joint (`created_by`, `pto_decided_by`,
  `submitted_by`, …) — Integer без FK, в стиле существующих `created_by`.
- **Р-4. Справочники отсутствуют** — ссылки на материалы (`material_id_*`),
  номенклатуру (`component_item_id_*`) и WPS (`planned_wps_id` / method) хранятся как
  **nullable UUID без FK**; текстовые дубли (`material_text_*`, `component_text_*`) —
  строки. FK добавляются отдельным решением после появления доменов МТО/WPS.

#### Согласованность иерархии Joint (Б-3)

- `Joint.line_id` — **обязателен** (NOT NULL).
- Если `EngineeringDocument.line_id` **заполнен**, он **обязан совпадать** с
  `Joint.line_id`.
- Если `EngineeringDocument.line_id IS NULL`, документ **разрешено** использовать как
  основание Joint; линия берётся из `Joint.line_id`. Такой документ обязан
  принадлежать тому же `Project`, что и Joint.
- Полная цепочка проверок при создании Joint и каждой новой связи: Project
  существует; Line существует и относится к Project; DocumentRevision существует и
  относится к EngineeringDocument; EngineeringDocument относится к тому же Project;
  соответствие Line — по правилу выше.
- Тип документа-основания **не** ограничивается `ISOMETRIC`.

#### Вычисляемые поля (Б-4)

Вычисляемые поля ADR-009 **сохраняются** (не колонки БД, вычисляются API):

- `ready_for_welding` — готовность стыка к сварке по заполненности инженерных полей;
- `missing_welding_requirements` — перечень недостающих полей;
- `production_state` — в MVP константа `NOT_STARTED`.

`requires_review` их **не** заменяет — это отдельный вычисляемый признак
согласования: `requires_review = true`, если ПТО **или** ОГС не подтвердили Joint
(`pto_status != CONFIRMED` или `ogs_status != CONFIRMED`, а также в состояниях
`DRAFT` / `PENDING_REVIEW`).

#### Модель Joint (заменяет раздел Joint ADR-009)

- Схема `engineering`; `id` UUID PK; обязательные `project_id`, `line_id`,
  `origin_document_revision_id`, `current_document_revision_id`.
- `system_code` формируется системой (`<project_code>-JNT-<sequence>`),
  не принимается от пользователя; последовательность отдельная на проект, номера не
  переиспользуются; `UNIQUE(project_id, system_code)`.
- `joint_no` (исходное обозначение) + `joint_no_normalized` (служебное: trim,
  схлопывание пробелов, типографские дефисы → `-`, сравнение без учёта регистра);
  исходное значение не теряется.
- `origin_document_revision_id` неизменяем; `current_document_revision_id` меняется
  только командой `set-current-revision` (не обычным PATCH).
- Статусы `status`: `DRAFT, PENDING_REVIEW, ACTIVE, CANCELLED, SUPERSEDED` (без
  `ON_HOLD`). Двойное согласование `pto_status` / `ogs_status`:
  `PENDING / CONFIRMED / REJECTED`. При создании `status=DRAFT`, оба статуса
  `PENDING`. `ACTIVE` — автоматически при обоих `CONFIRMED`; иначе не ACTIVE.
- `superseded_by_joint_id` — nullable self-FK; обязателен при `status=SUPERSEDED`;
  запрет self-supersede; заменяющий Joint того же проекта.
- Инженерные поля по сторонам соединения, классификация
  (`geometry_type` `BUTT/FILLET/TEE/LAP/SLOT/OTHER`, `weld_joint_type`
  `BW/SW/FW/OTHER`, `connection_code` `C/U/T/N/P/OTHER`), проектные способы сварки
  (`required_root/fill/cap_method`), требование термообработки, положение на чертеже
  (координаты необязательны; при заполненных `position_x/y` обязателен
  `coordinate_system`).
- Concurrency — `version` (optimistic locking); изменяющий запрос передаёт ожидаемую
  версию, несовпадение → конфликт без перезаписи. Физического удаления нет,
  DELETE-endpoint не создаётся.
  > **Уточнено ADR-011 (Р-11-2):** в Task 5B единая `version` разделяется на три —
  > `record_version` (concurrency, = прежняя `version`), `approval_version`,
  > `workflow_version`. См. [[docs/project/ADR-011-joint-lifecycle-approvals-blocking-scope|ADR-011 §15]].

#### Разделение ответственности ПТО / ОГС

- Поля ПТО: `joint_no`, `line_id`, документ/ревизия, `sheet_no`, `drawing_zone`,
  координаты, `location_note`, документарное примечание.
- Поля ОГС: `geometry_type`, `weld_joint_type`, `connection_code`, DN/толщины,
  материалы, компоненты, проектные способы сварки, WPS (при наличии ссылки),
  требования термообработки.
- Изменение полей ПТО сбрасывает `pto_status` → `PENDING`; полей ОГС → `ogs_status`
  → `PENDING`; общих полей — оба; из `ACTIVE` Joint возвращается в `PENDING_REVIEW`.

#### История ревизий: `joint_document_revisions`

- Неизменяемый снимок основных параметров Joint на момент связи; `revision_role`
  (`ORIGIN/CONFIRMED/MODIFIED/REMOVED`), `document_role`
  (`PRIMARY/ADDITIONAL/EXECUTIVE/REFERENCE`), `link_status` (`ACTIVE/INVALIDATED`).
- Снимок не редактируется (PATCH snapshot не создаётся); исправление — новая связь
  или аннулирование (`invalidated_reason/by/at`); аннулированная связь остаётся в
  истории и не может стать текущей PRIMARY.
- В пределах одной `DocumentRevision` `joint_no_normalized` уникален среди
  **активных** связей (partial unique index `WHERE link_status='ACTIVE'`); у одного
  Joint в момент времени только одна активная связь `document_role=PRIMARY`,
  соответствующая `current_document_revision_id`.

#### Доступ по областям (scope) и иерархия

- Области: `GLOBAL, PROJECT, COMPANY, SITE, LINE, ENGINEERING_DOCUMENT`. Проверяется
  не только совпадение `scope_id`, но и иерархия; для обычных действий — по текущему
  документу Joint; для смены текущей ревизии — доступ и к старому, и к новому
  документу. Несколько активных ролей дают объединение областей.

#### Bulk-импорт и идемпотентность

- `POST /api/v1/engineering/joints/bulk` — атомарное создание из одной
  `DocumentRevision`; ≤ 500 строк; при любой невалидной строке не создаётся ни один
  Joint; ошибки с `row_index`; `system_code` присваивается только после успешной
  валидации всего пакета; commit/rollback целиком.
- Таблица `engineering.joint_bulk_requests` (`UNIQUE(project_id, idempotency_key)`,
  `request_hash`, `response_payload` JSON): тот же ключ и тот же hash → сохранённый
  ответ; тот же ключ, другой hash → конфликт; ответ хранится целиком и не
  пересобирается из текущего состояния Joint.

#### Разбиение реализации на этапы

| Этап | Содержание | Миграция |
|------|-----------|----------|
| **Task 5A — Joint Core** | `joints`; обязательная связь с Line; `origin_/current_document_revision_id`; `system_code`; основные инженерные поля; нормализация `joint_no`; `version`; вычисляемые `ready_for_welding`, `missing_welding_requirements`, `production_state`; create / get / list / PATCH только допустимых полей | `20260711_05_engineering_joints` |
| **Task 5B — Joint Review Lifecycle** | `PENDING_REVIEW` / `ACTIVE`; `pto_status` / `ogs_status`; submit / confirm / reject; cancel / supersede; роли `PTO_ENGINEER` / `OGS_ENGINEER`; аддитивное добавление `ENGINEERING_DOCUMENT` в `scope_type` | `20260711_06_joint_review` |
| **Task 6 — Joint ↔ DocumentRevision History** | `joint_document_revisions`; неизменяемые снимки; роли связей; invalidate; set-current-revision | `20260711_07_joint_doc_revisions` |
| **Task 7 — Bulk Joint Import** | bulk endpoint; `joint_bulk_requests`; идемпотентность; атомарность пакета | `20260711_08_joint_bulk_requests` |

### Отклонённые варианты

1. **Реализовать всё как единый Task 5** — отклонено: объём охватывает несколько
   доменных подсистем; поэтапная сдача с ревью безопаснее.
2. **Одностадийное подтверждение (только ПТО) из ADR-009** — отклонено для Joint:
   производству требуется раздельная ответственность ПТО и ОГС.
3. **Буквальное `scope_type` из первичного ТЗ (рейнейм `ISOMETRIC`, набор с `UNIT`)**
   — отклонено: таких значений в схеме нет; исполнение удалило бы `COMPANY`/`SITE`.
4. **Новые `role_code` `PTO`/`OGS`** — отклонено (IP-07); используются существующие
   `PTO_ENGINEER` / `OGS_ENGINEER`.
5. **Редактируемый снимок связи (PATCH snapshot)** — отклонено: снимок неизменяем,
   исправление — новая связь или аннулирование.

### Последствия

- Joint-часть ADR-009 считается замещённой ADR-010; остальные разделы ADR-009 и вся
  реализация Task 4 (`EngineeringDocument`, `DocumentRevision`) — без изменений.
- Появляются таблицы `engineering.joints`, `engineering.joint_document_revisions`,
  `engineering.joint_bulk_requests`; `hr.worker_roles.scope_type` расширяется
  значением `ENGINEERING_DOCUMENT`.
- `IMPLEMENTATION_PLAN` перестраивается на этапы 5A / 5B / 6 / 7.
- Материалы, номенклатура и WPS остаются без FK до появления соответствующих
  доменов.
- ADR-010 является каноном реализации Joint для Tasks 5A, 5B, 6 и 7.

### Где зафиксировано

```text
docs/project/DECISIONS.md (ADR-010)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Tasks 5A, 5B, 6, 7)
```

---

## ADR-011. Жизненный цикл Joint, согласования, блокировки и контроль областей

Дата: 2026-07-11

Статус: **принято** (детализирует Task 5B поверх [[docs/project/DECISIONS#ADR-010. Joint MVP — расширенная модель, двойное согласование, история ревизий и bulk-импорт|ADR-010]]; базовую идентичность Joint не меняет)

Область: Engineering / Joint / ПТО / ОГС

Полный текст: [[docs/project/ADR-011-joint-lifecycle-approvals-blocking-scope|ADR-011-joint-lifecycle-approvals-blocking-scope.md]].

Уточняет: [[docs/project/DECISIONS#ADR-010. Joint MVP — расширенная модель, двойное согласование, история ревизий и bulk-импорт|ADR-010]] ·
[[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007]] ·
[[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] ·
[[docs/project/ADR-002-double-welder-accounting|ADR-002]]

Канон реализации: [[docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP#Task 5B — Joint Review Lifecycle|Implementation Plan, Task 5B]]

### Суть

- **Жизненный цикл Joint:** `DRAFT → PENDING_REVIEW → ACTIVE`, терминальные `CANCELLED`
  и `SUPERSEDED` (необратимы). Переходы — только доменными командами, не правкой поля.
- **Независимые согласования ПТО и ОГС** (`NOT_SUBMITTED / PENDING / APPROVED /
  REJECTED / REVOKED`); `ACTIVE` — автоматически и атомарно при действующих `APPROVED`
  обеих сторон для текущей `approval_version`. Согласования не объединяются в одно поле.
- **Выборочный сброс:** изменение инженерных данных сбрасывает ПТО, технологических —
  ОГС, общих — оба; незатронутая сторона сохраняет решение.
- **Автосогласование ОГС** (`AUTOMATIC` / `REJECTED` / `MANUAL_REVIEW_REQUIRED`);
  преодоление формального запрета — только `CHIEF_WELDER` через `OVERRIDE` с обоснованием.
- **Три версии Joint:** `record_version` (concurrency), `approval_version` (значимые
  данные), `workflow_version` (workflow); конфликт → `409 Conflict` без автоповтора.
- **Блокировки** — отдельные записи с областью (`PRODUCTION / EDITING / APPROVAL /
  ALL`); не статус ЖЦ. Готовность действий (`available_actions`) **вычисляется**
  сервером, не хранится и не дублируется во frontend.
- **Отмена и замена:** штатная `CANCELLED` (после первой `WeldOperation` — совместное
  решение ПТО_MANAGER + CHIEF_WELDER); атомарная `SUPERSEDED` через
  `REPLACEMENT_PENDING`; производственные события на преемника **не переносятся**;
  ошибочная отмена — новый Joint через `recreated_from_joint_id`.
- **Переоценка одного Joint** по зонам `ENGINEERING` / `WELDING_TECHNOLOGY` (`NDT`,
  `PWHT`, `EXECUTIVE_DOCUMENTATION` — архитектурно предусмотрены, логика позже).
- **Аудит и авторство:** поля-акторы не принимаются из тела запроса — только из
  auth-контекста; история статусов, согласований, блокировок и переоценок неизменяема.

Обязательный объём Task 5B (§44) и отложенные расширения (§45, приложение E)
разграничены. Расхождения словарей с ADR-010 **закрыты** архитектурными решениями
Р-11-1 — Р-11-4 (раздел «Решения по согласованию с ADR-010» полного текста):
состояния согласования — набор ADR-011, положительное решение `APPROVED`; версии —
три (`record`/`approval`/`workflow`, `version → record_version`); роли — техимена
ADR-010 (`PTO_ENGINEER`/`OGS_ENGINEER`) + новые `PTO_MANAGER`/`CHIEF_WELDER`/`AUDITOR`;
`scope_type` — набор ADR-010 (`ISOMETRIC → ENGINEERING_DOCUMENT`, `UNIT → SITE`).
Общая линия: словари и физическая схема — по ADR-010, полнота жизненного цикла — по
ADR-011.

### Где зафиксировано

```text
docs/project/ADR-011-joint-lifecycle-approvals-blocking-scope.md
docs/project/DECISIONS.md (ADR-011)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Task 5B)
docs/project/PROJECT_SUMMARY.md
```

---

## ADR-012. WeldOperation как неизменяемый производственный факт сварки

Дата: 2026-07-12

Статус: **принято** — закрывает Architecture Session 005 (решения 005-A — 005-CE)

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 005|Session 005]]

Замещает частично: [[docs/project/DECISIONS#ADR-009. Production/Joints MVP — физическая модель БД, события и API|ADR-009]] (только раздел WeldOperation и связанные правила допуска при создании операции)

Уточняет: [[docs/project/DECISIONS#ADR-008. Каноническая модель предметной области WeldPassport (Session 003)|ADR-008]] (003-P, 003-O) ·
[[docs/project/ADR-002-double-welder-accounting|ADR-002]] ·
[[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] ·
[[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007]] ·
[[docs/project/DECISIONS#ADR-010. Joint MVP — расширенная модель, двойное согласование, история ревизий и bulk-импорт|ADR-010]] ·
[[docs/project/DECISIONS#ADR-011. Жизненный цикл Joint, согласования, блокировки и контроль областей|ADR-011]]

Канон: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 005|Architecture Session 005]]

### Контекст

После завершения инженерного контура `Project → Line → EngineeringDocument →
DocumentRevision → Joint` (Task 7) требуется определить модель фактического
выполнения сварки.

Нельзя смешивать:

- инженерную сущность `Joint`;
- работу конкретного сварщика;
- технологическое принятие ОГС;
- последующую корректировку данных.

Ранее ADR-009 (Session 004) зафиксировал упрощённую физическую модель
`WeldOperation` (единый статус, этапы `ROOT`/`FILL`/`COVER`, строгая блокировка
создания при отсутствии допуска, исправление через `replaces_operation_id`).
Session 005 и настоящий ADR уточняют канон производственного факта без отмены
центральной роли Joint и разделения плановой / фактической WPS из ADR-009.

### Решение

Принять следующие правила:

- одна `WeldOperation` = один `Joint` + один фактический сварщик + один
  классифицированный этап (`root`, `fill`, `cap`, `back_weld`, `tack`) + один
  фактически применённый способ сварки;
- производственный факт **сохраняется** даже при нарушении допуска, WPS, клейма
  или иных технологических требований; нарушение классифицируется и направляется
  ОГС, технологическое принятие блокируется;
- `lifecycle_status`, `welder_confirmation_status` и `ogs_review_status` —
  **раздельные** оси состояния; единый статус не используется;
- завершённая операция (`lifecycle_status = completed`) **неизменяема**; прямое
  редактирование запрещено;
- исправления завершённого факта выполняются только через отдельную сущность
  `WeldOperationCorrection` с атомарным применением и переводом исходной операции
  в `superseded`;
- полная переварка оформляется как новая `WeldOperation` с признаком `reweld`;
  **локальный ремонт** проектируется отдельно и **не входит** в Task 8;
- WPS хранится на `Joint` (актуальный план) и на `WeldOperation` (фактически
  применённая версия); после первой завершённой операции смена WPS — только через
  технологическую ревизию;
- проверка допуска и WPS выполняется при создании, значимом изменении, завершении
  и после корректировки; при несоответствии факт не стирается, а создаёт маршрут
  review ОГС;
- подтверждение участия сварщика фиксирует мастер / прораб; сварщику не обязателен
  доступ в систему; история подтверждений неизменяема;
- интеграция с МТО, ОТК и НК реализуется через явные границы: временная ручная
  фиксация материалов до МТО; `InspectionApplicabilityDecision` для применимости
  результатов контроля при замене операции;
- реализация разбита на последовательные задачи **8A — 8F**; к Task 8A переходят
  только после принятия Session 005 и ADR-012.

### Отклонённые варианты

1. **Блокировка создания WeldOperation при отсутствии допуска** (ADR-009, 004-12) —
   отклонено: противоречит принципу сохранения производственного факта (ADR-008,
   003-P; Session 005).
2. **Единый статус операции** — отклонено: смешивает lifecycle, подтверждение
   сварщика и review ОГС.
3. **Прямое редактирование завершённой операции** — отклонено: нарушает
   трассируемость и двойной учёт сварщика.
4. **Исправление только новой WeldOperation с `replaces_operation_id`** без
   `WeldOperationCorrection` (ADR-009, 004-13) — отклонено: недостаточно для
   согласований, снимков и атомарного применения.
5. **Включение локального ремонта в Task 8** — отклонено: отдельный контур
   `RepairOperation` / `Defect`.

### Последствия

**Положительные:**

- полная трассируемость производственного факта и решений ОГС;
- невозможно незаметно переписать исполнителя или клеймо завершённой операции;
- сохраняются нарушения, несоответствия и маршруты review;
- поддерживается идемпотентный импорт и разрешение конфликтов;
- поддерживаются корректировки, supersede и полная переварка (`reweld`);
- сохраняется связь с будущими модулями контроля, НК и МТО через явные границы.

**Ограничения:**

- реализация требует нескольких последовательных задач (8A — 8F);
- полный lifecycle `RepairOperation`, `Defect`, Inspection и NDT **не входят** в
  Task 8;
- модель корректировок сложнее обычного CRUD;
- завершённые операции нельзя редактировать напрямую;
- физическая схема `production.weld_operations` из ADR-009 потребует пересмотра
  при реализации Task 8A.

### Где зафиксировано

```text
docs/project/ARCHITECTURE_SESSIONS.md (Session 005)
docs/project/DECISIONS.md (ADR-012)
docs/project/UBIQUITOUS_LANGUAGE.md
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Tasks 8A — 8F)
```

Связанные ADR:

- [[docs/project/ADR-002-double-welder-accounting|ADR-002]] — двойной учёт сварщика
  (фактический сварщик на операции; документальный учёт остаётся в контуре ПТО /
  ИД);
- [[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] — владельцы доменов
  (СМР — факт, ОГС — технологическое принятие);
- [[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007]] — Joint
  как центральная инженерная сущность;
- [[docs/project/DECISIONS#ADR-010. Joint MVP — расширенная модель, двойное согласование, история ревизий и bulk-импорт|ADR-010]] — инженерный контур и
  готовность Joint к сварке;
- [[docs/project/DECISIONS#ADR-011. Жизненный цикл Joint, согласования, блокировки и контроль областей|ADR-011]] — блокировки и отмена Joint после
  первой WeldOperation.

---

## ADR-013. Импорт XLSX и разрешение конфликтов (Task 8E)

Дата: 2026-07-13

Статус: **принято**

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 005|Session 005]] (импорт: типы источников, идемпотентный ключ, конфликт → корректировка)

Уточняет: [[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]] ·
[[docs/project/DECISIONS#ADR-010. Joint MVP — расширенная модель, двойное согласование, история ревизий и bulk-импорт|ADR-010]]

### Контекст

Task 8A–8D сформировали канонический контур `WeldOperation`, включая lifecycle,
подтверждение сварщика, результат ОГС и корректировки (Task 8D). Для загрузки данных
из внешнего XLSX потребовался отдельный импортный контур, который:

- не изменяет существующие `Joint` и `WeldOperation`;
- не использует correction/supersede-механизмы Task 8D;
- предварительно проверяет и сопоставляет данные;
- разрешает конфликты до записи в производственные таблицы;
- обеспечивает полный аудит и происхождение данных.

### Решение

Принять отдельный импортный контур:

```text
XLSX → ImportSession → ImportRow → ImportGroup → matching/resolution → apply → Joint/WeldOperation
```

Основные решения:

1. Источник MVP — только канонический XLSX-шаблон WeldPassport с обязательным
   `template_version`.
2. Одна строка источника может содержать данные `Joint` и одной `WeldOperation`.
3. Прямая запись из XLSX в производственные таблицы запрещена — только через staging.
4. Подготовка импорта выполняется через staging-контур.
5. Техническая роль `OGS_ENGINEER` реализует архитектурную роль `WELDING_ENGINEER`
   (новых role_code в `hr.worker_roles` не вводится).
6. Только `CHIEF_WELDER` может выполнить окончательный apply.
7. Сопоставление `Joint` выполняется сначала по `project_id` и нормализованному
   номеру стыка, затем уточняется по линии, изометрии и ревизии.
8. Расхождения инженерных данных создают конфликт и не изменяют существующий `Joint`.
9. Изменение существующей `WeldOperation` через импорт запрещено; для этого
   используется механизм Task 8D.
10. Полный дубликат операции пропускается идемпотентно (`SKIPPED_DUPLICATE`).
11. Атомарная единица применения — `ImportGroup`: один `Joint` и все связанные
    импортируемые операции.
12. Ошибка одной бизнес-независимой группы не откатывает ранее успешно применённые
    независимые группы.
13. Частичное применение разрешено.
14. `ImportResolution` хранит неизменяемую историю ручных решений (supersede-цепочка,
    не путать с supersede-механизмом Task 8D).
15. `ImportProvenance` хранится отдельно и не добавляет import-поля в `Joint` или
    `WeldOperation`.
16. Исходный файл — неизменяемый артефакт с SHA-256 (сверка при каждом чтении).
17. Хранение файлов абстрагировано через `FileStorage`; локальный адаптер — для
    разработки/тестов, S3-совместимый адаптер включается конфигурацией.
18. Команды импорта поддерживают идемпотентность (ключ — заголовок `Idempotency-Key`).
19. Применение в MVP выполняется синхронно (без фоновых очередей).
20. Физическое удаление import-сессий, provenance и истории запрещено.

### Физическая модель

Миграция `20260712_13_import_pipeline` (следует единственному Alembic head, второго
head не вводит) создаёт 11 таблиц схемы `engineering` (по ORM-моделям
`import_models.py`):

```text
engineering.import_sessions
engineering.import_parse_attempts
engineering.import_groups
engineering.import_rows
engineering.import_row_changes
engineering.import_resolutions
engineering.import_apply_attempts
engineering.import_apply_group_results
engineering.import_provenance
engineering.import_status_events
engineering.import_idempotency_keys
```

FK на `joints`/`weld_operations` — `RESTRICT`; дочерние таблицы импорта — `CASCADE`
на `import_sessions` (каскад только техническая целостность; доменное удаление
сессии запрещено).

### Реализация

Новые модули `app/engineering/`:

- `import_workflow.py` — словари статусов/кодов, роли, чистые функции пересчёта;
- `import_models.py` — 11 ORM-таблиц;
- `import_storage.py` — `FileStorage` + локальный и S3 адаптеры;
- `import_parse.py` — разбор XLSX и нормализация;
- `import_matching.py` — сопоставление Joint и классификация дублей;
- `import_repository.py` — доступ к данным;
- `import_schemas.py` — Pydantic-схемы;
- `import_services.py` — оркестрация;
- `import_api.py` — HTTP-слой.

Итоги:

- 24 API endpoint;
- 31 интеграционный тест (`tests/test_import_pipeline.py`);
- регрессия: `639 passed`;
- один Alembic head: `20260712_13_import_pipeline`.

### Последствия

Положительные:

- импорт отделён от производственного lifecycle;
- существующие производственные данные не изменяются молча;
- обеспечены аудит, идемпотентность и provenance;
- допускается безопасное частичное применение;
- Task 8D остаётся единственным механизмом корректировки операций.

Ограничения MVP:

- только канонический XLSX;
- нет произвольного сопоставления legacy-колонок;
- apply синхронный;
- UI не входит;
- локальный storage допустим только как адаптер разработки;
- расширенная аналитика и фоновые очереди отложены.

### Где зафиксировано

```text
docs/project/DECISIONS.md (ADR-013)
docs/ARCHITECTURE.md (раздел Task 8E)
```

---

## ADR-014. Heat Treatment Integration (Task 8F)

Дата: 2026-07-13

Статус: **принято**

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 006|Session 006]] (термическая обработка: цикл, участие Joint, снимок карты, актуальность)

Уточняет: [[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]] ·
[[docs/project/DECISIONS#ADR-011. Жизненный цикл Joint, согласования, блокировки и контроль областей|ADR-011]]

> Номер: изначально задание называло ADR-013, но ADR-013 занят импортом (Task 8E),
> поэтому Task 8F зафиксирован как **ADR-014**.
>
> Переопределение Task 8F: ранний план описывал «Task 8F — Integration Boundaries»
> (границы МТО/ОТК/НК, `InspectionApplicabilityDecision`). Фактически Task 8F
> реализован как **Heat Treatment Integration**; прежнее наполнение «Integration
> Boundaries» вынесено в отдельный будущий этап.

### Контекст

WeldPassport должен учитывать **фактическую термическую обработку** сварных
соединений, не смешивая её:

- с импортом XLSX (Task 8E);
- с корректировками `WeldOperation` (Task 8D);
- с контролем качества и НК;
- с полным контуром метрологии и управления оборудованием.

Один фактический цикл термообработки может охватывать **несколько** сварных
соединений. Технологические требования к режиму должны браться из утверждённой
технологической карты термообработки, а не повторно вводиться по каждому стыку.

### Решение

1. `HeatTreatmentProcedureRevision` — минимальная утверждаемая редакция
   технологической карты термообработки (нейтральное имя, не `WPS`: `WPS`
   означает технологию сварки).
2. `HeatTreatmentBatch` — общий фактический цикл термообработки.
3. `HeatTreatmentOperation` — участие одного `Joint` в конкретном цикле.
4. Один `HeatTreatmentBatch` может включать несколько `Joint`.
5. Одна строка журнала соответствует одной `HeatTreatmentOperation`.
6. Общий результат цикла и индивидуальный результат по каждому `Joint`
   учитываются раздельно.
7. Цикл обязательно связывается с утверждённой редакцией технологической карты
   до запуска.
8. При запуске сохраняется неизменяемый снимок ключевых требований карты
   (`procedure_snapshot`).
9. После начала цикла его состав и ссылка на карту блокируются.
10. Повторная термообработка всегда оформляется новым циклом и новой операцией со
    ссылкой на предыдущую (`previous_heat_treatment_operation_id`,
    причина `REPEAT_AFTER_REJECTION`).
11. Отклонённая или историческая запись не переписывается.
12. Актуальность принятой термообработки определяется **вычисляемо** относительно
    актуальной завершённой `WeldOperation` (последняя `COMPLETED`, не заменённая).
13. Новая сварка или ремонт не удаляет прежний результат, но делает его
    неактуальным для текущего состояния соединения.
14. Журнал термической обработки — **отчётное представление**, а не отдельная
    первичная бизнес-таблица.
15. Контроль качества после термообработки остаётся отдельным контуром (Task 8F
    добавляет только интеграционное правило готовности `Joint`).

### Жизненный цикл общего цикла (`HeatTreatmentBatch.status`)

- `DRAFT` — Черновик;
- `PLANNED` — Запланировано;
- `IN_PROGRESS` — Выполняется;
- `COMPLETED` — Физически выполнено, но ещё не принято;
- `REVIEWED` — Проверено ОГС;
- `CLOSED` — Полностью закрыто;
- `CANCELLED` — Отменено;
- `REJECTED` — Отклонено ОГС.

Основной путь: `DRAFT → PLANNED → IN_PROGRESS → COMPLETED → REVIEWED → CLOSED`.
Дополнительно: `DRAFT/PLANNED → CANCELLED`, `COMPLETED → REJECTED`.

### Результаты ОГС (`review_result`, отдельно от workflow-статуса)

- `PENDING` — Ожидает решения;
- `ACCEPTED` — Принято;
- `ACCEPTED_WITH_JUSTIFICATION` — Принято с инженерным обоснованием;
- `REJECTED` — Отклонено.

Индивидуальный результат по `Joint` (`HeatTreatmentOperation.result`) использует
те же значения плюс `NOT_APPLICABLE`. Достаточность подтверждающих данных
(`evidence_sufficiency`) ограничивает допустимый результат: `INSUFFICIENT`
запрещает любое принятие, `PARTIALLY_SUFFICIENT` допускает только
`ACCEPTED_WITH_JUSTIFICATION`/`REJECTED`/`NOT_APPLICABLE`.

### Роли (существующий канон, новых не вводится)

- инженерный результат термообработки принимает `OGS_ENGINEER` (техническая роль
  ОГС/инженера-сварщика) или `CHIEF_WELDER`;
- оператор/мастер/прораб — `MASTER`, `FOREMAN`, `CHIEF_WELDER` — создают цикл,
  формируют состав, планируют, запускают, вносят фактические параметры,
  завершают, загружают документы, регистрируют отклонения;
- ОТК (`OTK_INSPECTOR`) может просматривать цикл/документы и регистрировать
  отклонения, но не принимает инженерный результат.

### Граница MVP

В Task 8F **не входят**:

- `JointProductionState`;
- полноценный конструктор технологических карт;
- управление установкой термообработки;
- справочник оборудования с обслуживанием;
- полный метрологический контур;
- детальный учёт каждой термопары и канала;
- временные ряды температуры;
- workflow контроля твёрдости и НК;
- универсальный механизм документальных корректировок;
- сложные циклы по нескольким технологическим картам;
- экспорт журнала в PDF/XLSX (общего механизма экспорта пока нет — только
  API-представление, пригодное для будущего экспорта).

### MVP-ограничение по оператору-термисту

Роль `HEAT_TREATMENT_OPERATOR` (запланирована в 004-22) **фактически не
реализована**. Поэтому:

- текущая версия использует существующих производственных акторов
  (`MASTER`/`FOREMAN`/`CHIEF_WELDER`) как исполнителей цикла;
- отдельная кадровая роль, аттестация и проверка допуска оператора-термиста
  **отложены**;
- это **не означает**, что любой работник считается квалифицированным оператором
  в полном промышленном контуре; ограничение сознательное и подлежит снятию
  отдельным решением.

### Физическая модель

Миграция `20260713_14_heat_treatment` (следует единственному Alembic head, второго
head не вводит) создаёт 5 таблиц схемы `engineering`:

```text
engineering.heat_treatment_procedure_revisions
engineering.heat_treatment_batches
engineering.heat_treatment_operations
engineering.heat_treatment_records
engineering.heat_treatment_deviations
```

Перечисления реализованы CHECK-ограничениями (стиль модуля, как `weld_operations`),
без native enum-типов. FK на `joints`/`weld_operations`/`procedure_revisions`/
`batches` — `RESTRICT`. `UNIQUE(batch_id, joint_id)` — один `Joint` в цикле не
более одного раза. Данные Tasks 8A–8E не изменяются; `DROP SCHEMA` не используется.

> Расхождение схем с ранним планом: ADR-008/009 размещали термообработку в схеме
> `production` единой сущностью `HeatTreatmentOperation` (+ junction
> `HeatTreatmentOperationJoint`). Фактически реализация выполнена в схеме
> `engineering` (как и `weld_operations`) и разделена на `HeatTreatmentBatch` +
> `HeatTreatmentOperation`.

### Реализация

Новые модули `app/engineering/`:

- `heat_treatment_workflow.py` — словари статусов/результатов/ролей/кодов ошибок,
  чистые функции переходов, автоматической проверки и вычисления состояния Joint;
- `heat_treatment_schemas.py` — Pydantic-схемы;
- `heat_treatment_repository.py` — доступ к данным, запрос журнала, актуальность;
- `heat_treatment_services.py` — оркестрация (`HeatTreatmentService`);
- `heat_treatment_api.py` — HTTP-слой.

Модели добавлены в `app/engineering/models.py`; роутер подключён в `app/main.py`.

Итоги:

- workflow цикла (`plan`/`start`/`complete`/`review`/`close`/`cancel`),
  операции (`exclude`/`evaluate`), документы (`verify`/`reject`), отклонения
  (`decision`), read-only журнал, вычисляемое состояние `Joint`;
- 40 тестов (`tests/test_heat_treatment.py`);
- регрессия: `679 passed`;
- один Alembic head: `20260713_14_heat_treatment`.

### Последствия

Положительные:

- фактическая термообработка отделена от импорта, корректировок и контроля;
- требования карты не дублируются по каждому стыку;
- обеспечены снимок, история и вычисляемая актуальность;
- принятая термообработка влияет на готовность `Joint` к зависимым этапам.

Ограничения MVP — см. «Граница MVP» и «MVP-ограничение по оператору-термисту».

### Где зафиксировано

```text
docs/project/DECISIONS.md (ADR-014)
docs/ARCHITECTURE.md (§5.5)
docs/project/ARCHITECTURE_SESSIONS.md (Session 006)
docs/project/UBIQUITOUS_LANGUAGE.md (термины HeatTreatment*)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Task 8F)
```

---

## ADR-015. Inspection and NDT Workflow Canon (Session 007)

Дата: 2026-07-13

Статус: **принято** — закрывает Architecture Session 007 (решения 007-01 — 007-27).
Канон утверждён; **Tasks 9A — 9C реализованы**. Task 9C завершает ядро выполнения
назначенных методов контроля и регистрации лабораторных заключений. Tasks
**9D — 9G** — planned / not implemented. Отложена только **детальная реализация
импорта** результатов НК — до Architecture Session 008.

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 007|Session 007]]

Замещает частично: [[docs/project/DECISIONS#ADR-009. Production/Joints MVP — физическая модель БД, события и API|ADR-009]] (только раздел Inspection: единая сущность с результатами `PASS`/`FAIL`/`CONDITIONAL`)

Уточняет: [[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]] ·
[[docs/project/DECISIONS#ADR-014. Heat Treatment Integration (Task 8F)|ADR-014]] ·
[[docs/project/ADR-006-domain-ownership-matrix|ADR-006]]

Канон: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 007|Architecture Session 007]]

> Номер: ADR-013 занят импортом (Task 8E), ADR-014 — термообработкой (Task 8F),
> поэтому канон контроля/НК зафиксирован как **ADR-015**.

### Контекст

Контур контроля качества и НК был **осознанно отложен** в Session 005 (решения
005-U, 005-W, 005-BR) и упрощённо описан в ADR-009 как единая сущность `Inspection`
с результатами `PASS`/`FAIL`/`CONDITIONAL`. Реального промышленного контроля этого
недостаточно: нужно разделить заявку на контроль, назначение и выполнение методов,
технический результат лаборатории и технологическое решение ОГС, отчёты,
первичные материалы, групповые выборки, зоны частичного контроля и влияние контроля
на состояние `Joint`.

Основной инициатор и создатель заявки на контроль — **ОГС**. СМР фиксирует готовность
соединения; ОГС формирует заявку; ОТК проверяет результаты; лаборатория НК выполняет
назначенные методы.

### Решение

Принять канон контроля качества и НК (полный текст — Session 007, решения
007-01 — 007-27):

- основной контур: `Joint → Inspection → назначение методов → выполнение контроля →
  технические результаты → проверка ОТК → решение ОГС → состояние Joint → журнал`;
- **Inspection** — заявка/контрольное мероприятие для одного `Joint` либо
  утверждённой групповой выборки; lifecycle
  `DRAFT → REQUESTED → ASSIGNED → IN_PROGRESS → COMPLETED → REVIEWED → CLOSED`
  (+ `SUSPENDED`, `CANCELLED`, `TERMINATED`);
- методы контроля моделируются цепочкой **InspectionMethodAssignment** (назначение) →
  **InspectionMethodExecution** (фактическое выполнение; несколько на назначение) →
  **InspectionMethodResult** (технический результат:
  `CONFORMING`/`NONCONFORMING`/`INCONCLUSIVE`/`NOT_EVALUATED`,
  lifecycle `DRAFT → SUBMITTED → VERIFIED` + `SUPERSEDED`/`CANCELLED`);
  реализовано как `MethodExecutionResultItem` внутри `MethodExecution`, терминал
  выполнения — `LAB_CONFIRMED` (см. «Реализация Task 9C» ниже);
- **технический результат лаборатории (`InspectionMethodResult`) и технологическое
  решение ОГС (`InspectionDecision`) хранятся раздельно**; лаборатория фиксирует
  результат, ОТК переводит его в `VERIFIED`, ОГС принимает решение уровня
  `RESULT`/`INSPECTION` (`ACCEPT`/`REJECT`/`REPEAT_INSPECTION`/`SEND_TO_REPAIR`);
- **InspectionSample** — групповая выборка `Joint`, после начала контроля неизменяема;
- **InspectionCoverage** — структурированная зона частичного контроля;
- **InspectionReport** — официальный протокол/заключение
  (`DRAFT → ISSUED → VERIFIED` + `PRELIMINARY`/`SUPERSEDED`/`CANCELLED`);
  предварительный отчёт не закрывает контроль; один отчёт может включать несколько
  `Joint`/методов/результатов;
- **InspectionEvidence** — первичные материалы (снимки, УЗК-файлы, фото ВИК, схемы);
- **Defect** — лаборатория фиксирует индикацию, ОТК подтверждает и классифицирует,
  ОГС принимает технологическое решение; полный ремонтный lifecycle — отдельно;
- **NdtLaboratoryProfile** — участие лаборатории через `project_companies` с ролью
  `ndt_lab`; `Company` остаётся юрлицом; профиль хранит разрешённые методы и статус;
- методы `VT`/`RT`/`UT`/`PT`/`MT` (ВИК — в общем контуре `Inspection`); системные
  коды неизменяемы; проект ограничивает набор, но не меняет смысл;
- **требуемый** контроль (на `Joint`) и **назначенный** контроль (в
  `InspectionMethodAssignment`) разделены; отклонение — только с обоснованием ОГС;
  требования уже созданной заявки задним числом не пересчитываются;
- готовность `Joint` к контролю рассчитывается системой, фиксируется СМР,
  подтверждается ОГС; условия делятся на **блокирующие** и **предупреждающие**;
- повторный контроль без ремонта — внутри того же `Inspection`; после
  ремонта/переварки — новый `Inspection`; обязательный контроль после термообработки
  требует явной связи `Inspection ↔ HeatTreatmentOperation`;
- влияние на `Joint` — **вычисляемое** `inspection_state`
  (`NOT_REQUIRED`/`PENDING`/`IN_PROGRESS`/`PASSED`/`FAILED`/`REPAIR_REQUIRED`/`REINSPECTION_REQUIRED`);
  основной lifecycle `Joint` не переписывается каждым результатом; `PASSED` — только
  после окончательных принятых результатов по всем обязательным методам;
- нумерация `<PROJECT_CODE>-INS-<SEQUENCE>` + внешние номера ОГС и лаборатории;
- нормативные основания: проектные `EngineeringDocument`/`DocumentRevision` и внешние
  `NormativeDocument`/`NormativeDocumentRevision`; в заявке — снимок обозначения,
  редакции и пункта;
- файлы: метаданные и контрольные суммы в PostgreSQL, файлы — в объектном хранилище;
  доступ через короткоживущие подписанные ссылки; замена файла = новая версия; доступ
  по роли/scope/проекту/типу; rate limit (по пользователю и по файлу) → `429` +
  `Retry-After`;
- журнал контроля — представление из канонических сущностей (XLSX/PDF/CSV/JSON), а не
  ручной регистр; для закрытых периодов — неизменяемые снимки
  (`DRAFT → GENERATED → VALIDATED → APPROVED → PUBLISHED` + `SUPERSEDED`/`CANCELLED`);
- реализация разбита на Tasks **9A — 9G**; к Task 9A переходят только после принятия
  Session 007 и ADR-015.

### Роли (существующий канон, новых role_code не вводится)

- ОГС / `WELDING_ENGINEER` (техническая роль `OGS_ENGINEER`) — создаёт `Inspection`,
  выбирает методы, назначает лабораторию, подтверждает основания, принимает
  технологические решения;
- ОТК (`OTK_INSPECTOR`) — проверяет технические результаты, подтверждает дефекты,
  проверяет документы, подтверждает контрольную часть;
- лаборатория НК — выполняет методы, фиксирует результаты, выпускает отчёты,
  прикладывает первичные материалы (участие через `project_companies` role `ndt_lab`);
- `CHIEF_WELDER` — глобальные административные права, критические исключения;
- СМР — фиксирует готовность `Joint`;
- ПТО — предоставляет проектные требования, получает отчёты и журналы.

**Соответствие ролей.** Бизнес-наименование роли инженера-сварщика/ОГС —
`WELDING_ENGINEER`; существующий технический `role_code` backend — `OGS_ENGINEER`.
Новый `role_code` **не вводится**.

### Эволюция результатов контроля (историческая совместимость)

- значения результата контроля `PASS`/`FAIL`/`CONDITIONAL` из ADR-009 (004-14)
  остаются **историческим проектным вариантом**;
- ADR-015 **заменяет** их для будущей реализации;
- исторически предложенные в Session 007 технические результаты `InspectionMethodResult`:
  - `CONFORMING`;
  - `NONCONFORMING`;
  - `INCONCLUSIVE`;
  - `NOT_PERFORMED` — историческое имя, заменённое в реализации Task 9C на
    `NOT_EVALUATED`;
- реализованный `MethodExecutionResultItem` использует `NOT_EVALUATED`: оценка ещё
  не сформирована. `CONTROL_NOT_PERFORMED` означает, что контроль не выполнен; это
  отдельный тип отмены, а не оценка результата.

### Эволюция Defect (историческая совместимость)

- прежняя схема `quality.defects` из ADR-009 является **предварительным черновиком**;
- для будущей реализации действует канон ADR-015:
  `индикация лаборатории → подтверждение и классификация ОТК → Defect → решение ОГС`;
- полный ремонтный lifecycle остаётся **вне** Session 007 и **вне** Task 9E.

### Граница импорта (интеграционное требование верхнего уровня)

Импорт результатов контроля (XLSX, CSV, PDF, API лаборатории) зафиксирован **только
как интеграционное требование верхнего уровня**: импорт **не может автоматически
принимать результат**. Детальная архитектура импорта (форматы, разбор протоколов,
PDF-извлечение, интеграция с API лабораторий, staging/сопоставление/конфликты/провенанс,
правила ручного подтверждения) относится к будущей **Architecture Session 008** и в
ADR-015 не проектируется.

### Отклонённые варианты

1. **Единая `Inspection` с результатом `PASS`/`FAIL`/`CONDITIONAL`** (ADR-009,
   004-14) — отклонено на уровне метода: не разделяет назначение, выполнение,
   технический результат и решение ОГС. Значение сохранено как исторический контекст;
   технический результат метода теперь `InspectionMethodResult`.
2. **Совмещение технического результата лаборатории и решения ОГС в одной записи** —
   отклонено: нарушает разделение ответственности лаборатории/ОТК/ОГС и трассируемость.
3. **Хранимый ручной статус контроля `Joint`** — отклонено: состояние контроля
   `inspection_state` **вычисляется** (принцип 004-23), основной lifecycle `Joint` не
   переписывается каждым результатом.
4. **Проектирование детального импорта XLSX/CSV/PDF в Session 007** — отклонено:
   вынесено в Architecture Session 008; в Session 007 — только интеграционное
   требование верхнего уровня.
5. **Отдельный ручной регистр «Журнал контроля»** — отклонено: журнал — отчётное
   представление из канонических сущностей.

### Последствия

**Положительные:**

- полная трассируемость контроля: назначение → выполнение → результат → решение ОГС;
- раздельная ответственность лаборатории (результат), ОТК (проверка) и ОГС (решение);
- поддержка групповых выборок, зон частичного контроля, повторных выполнений и
  официальных отчётов, включающих несколько `Joint`;
- влияние контроля на `Joint` вычисляемо и согласовано с ADR-012/ADR-014;
- обязательный контроль после термообработки связан с `HeatTreatmentOperation`;
- задел под импорт результатов без добавления импортных сущностей в модель контроля.

**Ограничения:**

- Tasks 9A — 9C реализованы: Task 9C завершает ядро выполнения назначенных методов
  контроля и регистрации лабораторных заключений; решения ОГС/ОТК, дефекты,
  evidence/файлы и журналы остаются в Tasks **9D — 9G** (planned / not
  implemented);
- полный ремонтный lifecycle, `RepairOperation`, `Reweld`, полная аттестация
  дефектоскопистов, реестр оборудования НК и модель аккредитации лабораторий — **вне**
  Session 007;
- детальная архитектура импорта отложена в Architecture Session 008;
- физическая схема (таблицы, миграции) проектируется на этапе Task 9A и в ADR-015 не
  фиксируется.

### Где зафиксировано

```text
docs/project/ARCHITECTURE_SESSIONS.md (Session 007)
docs/project/DECISIONS.md (ADR-015)
docs/ARCHITECTURE.md (§5.7)
docs/project/PROJECT_SUMMARY.md
docs/project/UBIQUITOUS_LANGUAGE.md (термины Inspection*/NdtLaboratoryProfile/журнал контроля)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Tasks 9A — 9G)
```

Связанные ADR:

- [[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]] — готовность и актуальность относительно актуальной завершённой `WeldOperation`; `reweld` → новый `Inspection`;
- [[docs/project/DECISIONS#ADR-014. Heat Treatment Integration (Task 8F)|ADR-014]] — обязательный контроль после термообработки, связь `Inspection ↔ HeatTreatmentOperation`;
- [[docs/project/DECISIONS#ADR-009. Production/Joints MVP — физическая модель БД, события и API|ADR-009]] — прежняя единая `Inspection` (раздел частично замещён);
- [[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] — владельцы доменов (ОТК — контроль, НК — методы, ОГС — решение).

---

## ADR-016. Quality Execution Model (Task 9C)

Дата: 2026-07-15

Статус: принято, реализовано

### Контекст

ADR-015 зафиксировал проектный канон контроля, но реализация Task 9C уточнила
границы агрегатов, правила редакций, модель лабораторного заключения и внешних
участников. Эти уточнения должны быть каноном текущей физической архитектуры, не
переписывая историческую запись Architecture Session 007.

### Решение

- `MethodExecution` отделён от `InspectionMethodAssignment`: назначение отвечает на
  вопрос «что и кому назначено», выполнение — «что фактически выполнено в конкретном
  эпизоде контроля».
- Результаты контроля (`MethodExecutionResultItem`) версионируются. История не
  переписывается; исправления создаются через revision с lineage-полями
  `root_result_item_id`, `revision_no`, `supersedes_result_item_id`.
- Сам `MethodExecution` также исправляется новой редакцией, а подтверждённая редакция
  остаётся неизменяемой.
- `LaboratoryConclusion` — отдельный агрегат, а не поле выполнения. Заключение
  ссылается на конкретную редакцию одного или нескольких подтверждённых
  `MethodExecution`; исправление выпущенного заключения создаёт новую редакцию.
- Внешние лаборатории моделируются существующими `project.companies` и участием в
  проекте через `project_companies` с ролью `NDT_LAB`; отдельный профиль юрлица не
  вводится.
- Внешние контролёры и утверждающие лица моделируются как `QualityExternalPerson` и
  не являются `hr.Worker`.
- `LaboratoryAccreditation` связывается с лабораторией, имеет срок действия, а при
  выпуске заключения её реквизиты фиксируются неизменяемым snapshot.
- `LAB_CONFIRMED` означает подтверждение лабораторного результата или регистрацию
  внешнего лабораторного документа и не является решением ОТК. `VERIFIED`
  зарезервирован для будущего этапа проверки ОТК и в Task 9C не реализован.
- `NOT_EVALUATED` означает, что оценка результата ещё не сформирована;
  `CONTROL_NOT_PERFORMED` означает, что контроль не выполнен. Эти состояния не
  взаимозаменяемы.

### Последствия

- Task 9C завершает ядро выполнения назначенных методов контроля и регистрации
  лабораторных заключений. Цепочка исполнения трассируется как назначение →
  выполнение → локальные результаты → лабораторное заключение, с отдельной историей
  редакций каждого изменяемого факта.
- `quality_audit_events` фиксирует доменные действия Quality; API Task 9C работает с
  актуальными агрегатами `MethodExecution` и `LaboratoryConclusion`.
- Tasks **9D — 9G** (решения ОГС/ОТК, дефекты, evidence/файлы, журналы) остаются
  planned / not implemented.
- Исторические имена `InspectionMethodExecution`, `InspectionMethodResult` и
  `InspectionReport` сохраняются только в записях прежнего канона и ссылках на их
  замену.

### Где зафиксировано

```text
docs/ARCHITECTURE.md (§5.7)
docs/project/ARCHITECTURE_SESSIONS.md (консолидация Task 9C)
docs/project/DECISIONS.md (ADR-016)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md
docs/project/PROJECT_SUMMARY.md
docs/project/UBIQUITOUS_LANGUAGE.md
```

---

## ADR-017. Quality Decision, Defect, Repair and Quality Documents Canon (Session 008)

Дата: 2026-07-15

Статус: **ACCEPTED — принято**, **`PARTIALLY_SUPERSEDED_BY_ADR-019`** (решение
008-07-BQ, 2026-07-16) — закрывает блоки Architecture Session 008 (008-01 — 008-05).
Канон утверждён; **код, миграции и тесты на этой сессии не создавались**. Блок
**008-06 «Печатные формы»** не завершён и **не входит** в принятый канон.

> **Частичное замещение (2026-07-16, ADR-019, решение 008-07-BQ).** Модель участка
> `Quality Finding → Closure` углублена в
> [[docs/project/DECISIONS#ADR-019. Quality Finding and Engineering Evaluation Canon (Session 008-07)|ADR-019]].
> **Замещены** следующие положения ADR-017:
> - самостоятельная доменная сущность **Quality Decision** — декомпозирована на
>   `EngineeringEvaluation` → `DefectAcceptanceAssessment` → `FindingDisposition`
>   (термин «Quality Decision» сохраняется только как обобщённое бизнес-понятие);
> - **единая модель технической оценки и решения** — разделена на оценку («что
>   установлено»), приемлемость дефекта и disposition («что сделать»);
> - термин **`Defect Severity`** — заменён на **`Confirmed Severity`**;
> - **безусловная обязательность внутреннего ОТК** — снята: `OTK_INSPECTOR` —
>   опциональная проектная роль, критический fallback — `CHIEF_WELDER` (решение
>   008-07-BP).
>
> ADR-017 **не удаляется** и **продолжает действовать в непересекающейся части**:
> общий принцип внутреннего и внешнего решения; связь с печатными формами (ADR-018);
> роли и внешние решения, не противоречащие ADR-019; положения вне модели
> finding/evaluation/defect/disposition. Реализационная структура Task 9D — блоки
> **9D-1 … 9D-8** (mapping старых Tasks 9E — 9K —
> [[docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP#Соответствие старых Tasks 9E — 9K блокам 9D-1 … 9D-8 (решение 008-07-BO)|таблица соответствия]]).

> **Обновление (2026-07-16, ADR-018):** блок **008-06 «Печатные формы»**
> впоследствии завершён отдельной сессией 008-06 и зафиксирован как
> [[docs/project/DECISIONS#ADR-018. Electronic Documents and Printed Forms Canon (Session 008-06)|ADR-018 — Electronic Documents and Printed Forms Canon]].
> Формулировки ниже (в т.ч. «008-06 остаётся открытым» / «не входит в канон»)
> отражают объём ADR-017 на момент принятия и сохранены как история.

> **Реализация Tasks 9D — 9K:** структура пересмотрена решением 008-07-BO —
> см. [[docs/project/DECISIONS#ADR-019. Quality Finding and Engineering Evaluation Canon (Session 008-07)|ADR-019]]
> и [[docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP#Соответствие старых Tasks 9E — 9K блокам 9D-1 … 9D-8 (решение 008-07-BO)|таблицу соответствия]].
> Ниже сохранена историческая формулировка «Tasks 9D — 9K» на момент принятия ADR-017.

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 008|Session 008]]

Продолжает и уточняет: [[docs/project/DECISIONS#ADR-015. Inspection and NDT Workflow Canon (Session 007)|ADR-015]] ·
[[docs/project/DECISIONS#ADR-016. Quality Execution Model (Task 9C)|ADR-016]]

Опирается на: [[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] (владельцы доменов) ·
[[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]] (неизменяемый производственный факт) ·
[[docs/project/DECISIONS#ADR-014. Heat Treatment Integration (Task 8F)|ADR-014]]

> Номер: ADR-015 занят каноном контроля/НК, ADR-016 — моделью выполнения (Task 9C),
> поэтому канон решений по качеству, дефектов, ремонта и документов качества
> зафиксирован как **ADR-017**.

### Контекст

ADR-015 (Session 007) зафиксировал контур контроля и НК до получения технического
результата, ADR-016 (Task 9C) — модель фактического выполнения методов и
лабораторных заключений. Оставался неопределённым **процесс после получения
результата контроля**: как отрицательный результат превращается (или не
превращается) в подтверждённый дефект, кто и как принимает решение по качеству, как
устроен ремонт и повторный контроль, как ведутся документы качества.

Основной контур Session 008:

```text
Inspection Result — результат контроля
        ↓
Quality Finding — обнаруженный признак
        ↓
Engineering Evaluation — инженерная оценка
        ↓
Defect — подтверждённый дефект
        ↓
Quality Decision — решение по качеству
        ↓
Repair — ремонт
        ↓
Reinspection — повторный контроль
        ↓
Defect Closure — закрытие дефекта
```

### Проблема

- отрицательный результат контроля ошибочно приравнивался к дефекту;
- не были разделены технический результат, обнаруженный признак и подтверждённый
  дефект;
- отсутствовал канон совместного решения ОГС и ОТК, разрешения разногласий главным
  сварщиком, версионирования и обоснования решений;
- не были определены жизненные циклы `Defect`, `Repair`, `Reinspection` и единая
  модель документа качества.

### Решение

Принять канон Session 008 (полный текст — Session 008, блоки 008-01 — 008-05):

**008-01. Result ≠ Finding ≠ Defect.** Три разные сущности:

- `Inspection Result` — технический факт метода; **не** содержит решения о ремонте,
  окончательной оценки, признания дефекта или разрешения дальнейшего производства;
- `Quality Finding` — обнаруженный исполнителем НК/лабораторией признак; создание
  Finding **не** означает автоматического признания дефекта;
- `Defect` — несоответствие, признанное недопустимым **только после инженерной
  оценки**. Отрицательный результат контроля может существовать без `Defect`
  (например `RT: FAILED → Engineering Review → индикация допустима → ACCEPT →
  Defect отсутствует`).

**008-02. Quality Decision.** Инженерная оценка Finding выполняется совместно: ОГС —
техническая и технологическая оценка, ОТК — соответствие нормативам и требованиям
качества; решения фиксируются раздельно. Итоговые решения:
`ACCEPT` / `REPAIR_REQUIRED` / `REINSPECTION_REQUIRED` / `REJECT`. При разногласии
ОГС и ОТК — `Chief Welder Review → Final Decision` с обязательным обоснованием.
Решения после фиксации **не редактируются** — только новая версия с обязательной
причиной; старая версия остаётся в истории. Для каждого решения (включая `ACCEPT`)
обязательны нормативный документ, пункт/критерий приёмки, текстовое обоснование,
автор, дата и время. Решение принимается **отдельно по каждому** Quality Finding;
итог Inspection рассчитывается автоматически по самому строгому активному решению
(`REJECT > REPAIR_REQUIRED > REINSPECTION_REQUIRED > ACCEPT`). Главный сварщик может
выполнить override рассчитанного итога с обоснованием; автоматически рассчитанный
итог сохраняется отдельно от изменённого. Отменить итоговое решение может только
главный сварщик, затем выбирает: вернуть Finding на повторную оценку ОГС и ОТК либо
сразу установить новое итоговое решение.

**008-03. Defect.** Создаётся только после явного подтверждения ОГС и ОТК; ведётся
совместно, владелец инженерной сущности — ОГС, ОТК — обязательный участник
подтверждения, проверки и контроля закрытия. Связь `Quality Finding ↔ Defect` —
**многие-ко-многим**. Defect содержит тип (из отдельного расширяемого справочника,
не зашитого только в код), описание, локализацию, размеры, нормативные
классификаторы, критичность (`MINOR` / `MAJOR` / `CRITICAL`). Изменение критичности
и локализации — только новой версией инженерной оценки с причиной. Локализация
обязательно связана с `Joint` и конкретной зоной шва и содержит структурированные
поля и текстовое уточнение. Причина может быть неизвестна при создании, но должна
быть установлена до закрытия (категория, коренная причина, пояснение; ОГС определяет
техническую причину, ОТК подтверждает достаточность анализа). Корректирующее
действие обязательно для критических и повторяющихся дефектов; для критического
повторяющегося Defect закрытие блокируется до выполнения действия (исключение — по
обоснованному решению главного сварщика). Жизненный цикл — см. ниже.

**Повторяемость** определяется настраиваемым версионируемым правилом (тип дефекта,
сварщик, WPS, материал, диаметр, толщина, участок, проект, период). ОГС формирует
правило, главный сварщик утверждает; система выявляет возможную повторяемость, ОГС
подтверждает или отклоняет; уведомляются ОГС, ОТК и главный сварщик.

**Связь Defect с WeldOperation и сварщиком.** Связь с `Joint` обязательна; **прямая
связь Defect со сварщиком не создаётся** — только через
`Defect → WeldOperation → Welder`. Причинная связь с `WeldOperation` — отдельное
инженерное решение с уровнем уверенности `POSSIBLE` / `PROBABLE` / `CONFIRMED` (ОГС
устанавливает/меняет, ОТК подтверждает достаточность доказательств, история — по
версиям). `POSSIBLE`/`PROBABLE` — аналитические сигналы; для персональной
ответственности используется только `CONFIRMED`.

**008-04. Repair.** Отдельная сущность; связь `Defect ↔ Repair` —
**многие-ко-многим**. Выполненный Repair **не** означает автоматического устранения
Defect. Мастер создаёт заявку, ОГС оформляет и утверждает план, ОТК согласовывает;
главный сварщик подключается при критическом дефекте, превышении лимита, разногласии
или исключении. План структурирован (связанные Defect, зона, технология, WPS, способ
выборки, требования к сварщикам, материалы, методы и объём повторного контроля,
нормативное основание); для сварочного Repair WPS обязателен (исключение — по
обоснованию главного сварщика), для несварочного не требуется. Назначенный и
фактический исполнитель фиксируются отдельно; для сварочного Repair неподходящий
допуск блокирует назначение. После утверждения план неизменяем — изменения только
новой версией с повторным утверждением ОГС и согласованием ОТК. Отклонения от плана
фиксируются отдельно (`MINOR DEVIATION` / `MAJOR DEVIATION`). Повторный контроль
разрешается только после `COMPLETED`. Лимиты ремонтов считаются отдельно:
`Defect Repair Count`, `Joint Repair Count`, `Repair Zone Count`; при превышении
новый Repair блокируется (исключение — по обоснованию главного сварщика).

**Reinspection.** Каждый Reinspection связывается с конкретным Repair (один
Reinspection может покрывать несколько Repair в одной зоне и объёме; один Repair
может требовать несколько методов). Итог рассчитывается автоматически по самому
строгому результату; главный сварщик может изменить с обоснованием (исходный расчёт
сохраняется). После принятого Reinspection и отдельных подтверждений ОГС и ОТК
система автоматически переводит Defect в `CLOSED_AFTER_REPAIR`. При неудачном
Reinspection главный сварщик выбирает новую попытку Repair или окончательное
отклонение Joint; предыдущие Repair и Reinspection сохраняются.

**Quality State Joint.** При окончательном отклонении Joint не удаляется и не
переводится в обычный `CANCELLED`, а получает качество-состояние
`REJECTED — отклонено по качеству`: история сохраняется, производственные действия
блокируются, новые Repair и Inspection не запускаются без отдельного решения.
Снять это состояние может только главный сварщик новой версией решения при наличии
новых подтверждённых данных, технического и нормативного обоснования и причины;
после снятия блокировки автоматически не снимаются — маршрут (повторная оценка /
новый ремонт / повторный контроль / возврат в производство) выбирается отдельно.

**008-05. Quality Document.** Единая сущность документа качества, связываемая
одновременно с `Inspection`, `Quality Finding`, `Defect`, `Repair`, `Reinspection`
и другими объектами; один файл не дублируется для каждой связи. Категории:
`WORKING` / `EVIDENCE` / `OFFICIAL` (тип и статус хранятся раздельно).
Версионирование: файл выпущенной версии не перезаписывается, предыдущая получает
`SUPERSEDED`. Владение обязательно (подразделение-владелец, ответственное лицо,
автор, источник); схема утверждения настраивается по типу; `ISSUED` возможен только
после прохождения схемы; выпущенный документ неизменяем. Подписи: `WORKING` — без
обязательной ЭП; `EVIDENCE` — автор, время загрузки, источник; `OFFICIAL` — ЭП или
формализованное электронное утверждение. Для каждого файла хранится контрольная
сумма (при совпадении новая физическая копия не создаётся — создаётся новая связь).
Удаление: `WORKING` — до утверждения; `EVIDENCE`/`OFFICIAL` физически не удаляются
(связь можно отменить / перевести в `CANCELLED`). Обязательный комплект документов
настраивается (проект, тип процесса, критичность, норматив); ОГС определяет
технические документы, ОТК — документы качества, главный сварщик утверждает правило;
Defect и Repair сохраняют ссылку на применённую версию правила. Система формирует
единый **реестр документов качества**; экспорт — Excel (рабочий) и PDF (официальный
реестр с ЭП/контрольной суммой/QR-кодом); публично доступны только базовые сведения,
полные данные и сам PDF — по правам.

### Архитектурные границы

- Session 008 определяет **процесс после получения результата контроля** и не
  переопределяет модель выполнения контроля (ADR-016) и контур заявки/НК (ADR-015).
- `Inspection Result` (ADR-015/016) остаётся техническим фактом; Session 008
  добавляет над ним слой `Finding → Evaluation → Decision → Defect → Repair →
  Reinspection → Closure`.
- В канон **не входят** (см. «Исключения»): печатные формы и макеты PDF, генераторы
  документов, реализация ЭП, публичный API проверки подлинности, frontend, backend,
  миграции, импорт документов, реализация уведомлений и фактическая реализация
  Defect/Repair/Reinspection.
- **Импорт результатов НК** (XLSX/CSV/PDF/API лаборатории), ранее прогнозно
  привязанный к «Architecture Session 008», в эту сессию **не вошёл** и остаётся
  открытым для отдельной будущей архитектурной сессии (см. раздел «Связь с Session
  007»).

### Роли и ответственность

- **ОГС / `WELDING_ENGINEER`** — техническая и технологическая оценка, владелец
  инженерной сущности Defect, оформление и утверждение плана Repair, техническая
  причина, техническое заключение по ремонту.
- **ОТК / `OTK_INSPECTOR`** — соответствие нормативам и требованиям качества,
  обязательный участник подтверждения Defect, согласование плана Repair, подтверждение
  качества и результата.
- **Главный сварщик / `CHIEF_WELDER`** — разрешение разногласий, override и отмена
  итоговых решений, критические исключения, утверждение правил повторяемости и
  комплектности, отмена подтверждённого Defect и качество-состояния `REJECTED` —
  всегда с обязательным обоснованием и сохранением истории.
- **Мастер** — заявка на ремонт, перевод в `IN_PROGRESS`, фиксация физического
  завершения, инициирование остановки/возобновления/отмены.
- **Лаборатория НК** — повторный контроль (Reinspection) по назначенным методам.
- Новые `role_code` **не вводятся**.

### Жизненные циклы

**Defect:** `DRAFT → CONFIRMED → REPAIR_REQUIRED → REINSPECTION_REQUIRED →
CLOSURE_PENDING → CLOSED_AFTER_REPAIR / CLOSED_AS_ACCEPTABLE`; плюс
`REJECTED` (не подтвердился/ошибочно создан) и `CANCELLED` (запись/процесс отменены
по другой причине) — эти два различаются. `CONFIRMED` — только после подтверждений
ОГС и ОТК; отменить подтверждённый Defect может только главный сварщик.

**Repair:** `DRAFT → PLANNED → APPROVED → IN_PROGRESS → PAUSED → REWORK_REQUIRED →
PERFORMED → VERIFICATION_PENDING → COMPLETED`; плюс `REJECTED` (попытка отклонена) и
`CANCELLED`. `RESUMED — возобновлён` фиксируется как **событие**, а не обязательно
как отдельное длительное состояние. `COMPLETED` — после подтверждений ОГС и ОТК и
закрытия всех активных замечаний.

**Quality Document:** `DRAFT → UNDER_REVIEW → APPROVED → ISSUED → SUPERSEDED /
CANCELLED`.

Английские статусы сопровождаются русскими пояснениями в
[[docs/project/UBIQUITOUS_LANGUAGE|Ubiquitous Language]] (Раздел 10).

### Последствия

**Положительные:**

- строгое разделение технического результата, признака и подтверждённого дефекта;
- прослеживаемая ответственность ОГС/ОТК/главного сварщика с обоснованием и
  версиями;
- явные жизненные циклы Defect/Repair/Reinspection и единая модель документа
  качества без дублирования файлов;
- аналитика повторяемости и причинности без несправедливой персональной
  ответственности (только `CONFIRMED`).

**Ограничения:**

- Session 008 фиксирует только канон; реализация — Tasks **9D — 9K** (planned / not
  implemented);
- блок **008-06 «Печатные формы»** остаётся открытым;
- детальная архитектура импорта результатов НК остаётся открытой для отдельной
  будущей сессии;
- физическая схема (таблицы, миграции, API) в ADR-017 не фиксируется.

### Исключения из текущего объёма

Не входят в принятый канон: шаблоны печатных форм, конкретные макеты PDF, генераторы
документов, реализация ЭП, публичный API проверки подлинности, frontend, backend,
миграции, импорт документов, реализация уведомлений, фактическая реализация Defect,
Repair и Reinspection. Блок 008-06 «Печатные формы» — отдельное будущее обсуждение.

### Связь с предыдущими ADR и Session 007

- **ADR-015 / Session 007.** Session 007 прогнозно называл «Architecture Session 008»
  сессией **импорта результатов НК**. Фактически проведённая Session 008 посвящена
  канону решений по качеству, дефектов, ремонта и документов качества; **импорт
  результатов НК в неё не вошёл** и остаётся открытым для отдельной будущей сессии.
  Историческая формулировка ADR-015/Session 007 сохранена как запись прогноза и не
  переписывается.
- **ADR-016 (Task 9C).** `Inspection Result` и модель выполнения (`MethodExecution`,
  `LaboratoryConclusion`) остаются техническим слоем; Session 008 надстраивает над
  ними слой Finding/Evaluation/Decision/Defect/Repair/Reinspection/Closure.
- **ADR-015 (Defect placeholder).** Эволюция Defect из ADR-015
  (`индикация → подтверждение ОТК → решение ОГС`) уточнена и расширена каноном 008-03.

### Где зафиксировано

```text
docs/ARCHITECTURE.md (§5.8)
docs/project/ARCHITECTURE_SESSIONS.md (Session 008)
docs/project/DECISIONS.md (ADR-017)
docs/project/PROJECT_SUMMARY.md
docs/project/UBIQUITOUS_LANGUAGE.md (Раздел 10)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Tasks 9D — 9K)
```

---

## ADR-018. Electronic Documents and Printed Forms Canon (Session 008-06)

Дата: 2026-07-16

Статус: **ACCEPTED — принято** — закрывает блок Architecture Session **008-06
«Печатные формы»** (решения 008-06-01 — 008-06-09). Канон утверждён; **код,
миграции, API и тесты на этой сессии не создавались**.

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Блок 008-06 — Электронные документы и печатные формы (Electronic Documents and Printed Forms)|Session 008-06 — Printed Forms]]

Продолжает и уточняет: [[docs/project/DECISIONS#ADR-017. Quality Decision, Defect, Repair and Quality Documents Canon (Session 008)|ADR-017]] ·
[[docs/project/DECISIONS#ADR-016. Quality Execution Model (Task 9C)|ADR-016]] ·
[[docs/project/DECISIONS#ADR-015. Inspection and NDT Workflow Canon (Session 007)|ADR-015]]

Опирается на: [[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] (владельцы доменов) ·
[[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]] (неизменяемый производственный факт)

> **Номер:** ADR-015 занят каноном контроля/НК, ADR-016 — моделью выполнения
> (Task 9C), ADR-017 — каноном решений по качеству; блок 008-06 «Печатные формы» в
> ADR-017 остался открытым, поэтому его канон зафиксирован как **ADR-018**.

### Контекст

WeldPassport должен обеспечить управление официальными документами сварочного
производства как **доказательствами выполнения работ**, а не только генерацию PDF.
ADR-017 (Session 008) зафиксировал канон решений по качеству, дефектов, ремонта и
документов качества, но блок 008-06 «Печатные формы» остался открытым. Session 008-06
закрывает его, вводя канон Electronic Document Management.

### Решение

Принята модель **Electronic Document Management**:

- **Preview** и **Official Document** разделены (008-06-01).
- Операционные документы и документы закрытия разделены (008-06-02).
- Ответственность выпуска определяется типом документа (008-06-03).
- Официальный документ содержит **PDF, Snapshot, Source Links и Hash** (008-06-04).
- Выпущенные документы **неизменяемы**; изменения создают **новые версии** документа
  (008-06-05).
- Источники документа связываются через универсальную модель **Document Sources**
  (008-06-06).
- Выпуск документа выполняется **после утверждения ответственного лица** (008-06-07).
- История формирования документа хранится через **Document History Events**
  (008-06-08).
- Используется **гибридная модель шаблонов документов** (008-06-09).

### Последствия

- Формируется единый слой **Electronic Documentation**.
- Task **9D — Quality Finding and Engineering Evaluation** должен использовать ссылки
  на **Official Documents**; инженерная оценка качества обязана ссылаться на
  конкретные **редакции** (Document Revision) официального документа.
- Исполнительная документация получает **версионность и аудит**.
- Реализация (генераторы PDF, конкретные макеты форм, реализация ЭП, публичный API
  проверки подлинности, backend, миграции) — **отдельными задачами**; ADR-018
  фиксирует только канон.

### Где зафиксировано

```text
docs/project/DECISIONS.md (ADR-018)
docs/project/ARCHITECTURE_SESSIONS.md (Session 008-06)
docs/project/UBIQUITOUS_LANGUAGE.md (Official Document, Document Revision,
  Document Snapshot, Document Source, Document Template, Document History Event)
docs/project/PROJECT_SUMMARY.md (Electronic Documentation Layer)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Task 9D зависит от ADR-018)
```

---

## ADR-019. Quality Finding and Engineering Evaluation Canon (Session 008-07)

Дата: 2026-07-16

Статус: **ACCEPTED — принято** — закрывает Architecture Session **008-07 «Quality
Finding and Engineering Evaluation»** (углублённая проработка Task 9D). Канон
утверждён на сессии без создания кода/моделей/миграций/API/тестов.

> **Partial supersede для disposition-модели.** Разделы ADR-019 о
> `FindingDisposition` сохраняются как исторический канон, но принятый
> [[docs/project/ADR-024-defect-disposition-lifecycle-authority-model|ADR-024]] заменяет
> их моделью `DefectDisposition` для подтверждённых `Defect`. Это не простое
> переименование: меняются владелец, типы, lifecycle, authority и supersede timing.
> Прочие положения ADR-019 не пересматриваются. Текущий код требует отдельного приведения
> к принятому канону по Task Implementation Specification.

Статус реализации: **PARTIALLY IMPLEMENTED — реализовано частично.** Реализованный
блок — **Task 9D-1 QualityFinding Core** (модель `QualityFinding`, нумерация
`<PROJECT_CODE>-QF-<SEQUENCE>`, журнал, базовый lifecycle `DRAFT → REGISTERED →
UNDER_EVALUATION` + отмена/удаление DRAFT, репозиторий, сервис, схемы, API, RBAC,
миграция, тесты). Следующий блок — **Task 9D-2 EngineeringEvaluation** (углублённый
канон ядра —
[[docs/project/DECISIONS#ADR-021. EngineeringEvaluation Core Canon (Task 9D-2)|ADR-021]]);
блоки 9D-2 … 9D-6 не реализованы. Правка статуса реализации не меняет архитектурный канон,
lifecycle, роли, сущности и границы задач, зафиксированные ниже.

Architecture Session: [[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 008-07 — Quality Finding and Engineering Evaluation|Session 008-07]]

Продолжает и углубляет: [[docs/project/DECISIONS#ADR-017. Quality Decision, Defect, Repair and Quality Documents Canon (Session 008)|ADR-017]] (канон процессов после результата контроля) ·
[[docs/project/DECISIONS#ADR-018. Electronic Documents and Printed Forms Canon (Session 008-06)|ADR-018]] (официальные документы и редакции)

Опирается на: [[docs/project/ADR-006-domain-ownership-matrix|ADR-006]] (владельцы доменов) ·
[[docs/project/DECISIONS#ADR-015. Inspection and NDT Workflow Canon (Session 007)|ADR-015]] ·
[[docs/project/DECISIONS#ADR-016. Quality Execution Model (Task 9C)|ADR-016]] (`Inspection Result` как технический факт) ·
[[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]]

> **Номер:** ADR-017 занят каноном решений по качеству (Session 008), ADR-018 —
> электронными документами (008-06); углублённая архитектура Task 9D зафиксирована
> как **ADR-019**.

> **Отношение к ADR-017 (`PARTIALLY_SUPERSEDED_BY_ADR-019`, решение 008-07-BQ).**
> ADR-017 зафиксировал контур `Result → Finding → Evaluation → Defect →
> Quality Decision → Repair → Reinspection → Closure` на уровне Session 008. ADR-019
> **углубляет и частично замещает** участок от `Quality Finding` до `Closure`, разделяя
> ранее единое `Quality Decision` на **три** отдельных решения (`EngineeringEvaluation`
> — «что установлено», `DefectAcceptanceAssessment` — «приемлем ли дефект технически»,
> `FindingDisposition` — «что необходимо сделать») и вводя `ProductionHold`,
> `CustomerQualityDecision`, `CorrectiveActionLink`, `ReinspectionRequirement`.
> Замещённые положения ADR-017 (самостоятельная сущность `Quality Decision`; единая
> модель оценки и решения; термин `Defect Severity`; безусловная обязательность ОТК) и
> сохранённая в силе часть перечислены в статусном блоке ADR-017. Историческая
> формулировка ADR-017 не переписывается. Реализационные решения 008-07-BO (структура
> Task 9D), 008-07-BP (роль ОТК) и 008-07-BQ (статус ADR-017) зафиксированы ниже в
> разделе «Решения 008-07-BO / BP / BQ»; таблица соответствия старых Tasks 9E — 9K
> блокам 9D-1 … 9D-8 — в
> [[docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP#Соответствие старых Tasks 9E — 9K блокам 9D-1 … 9D-8 (решение 008-07-BO)|Implementation Plan]].

> **Терминология действующей оценки (обновление 2026-07-19, ADR-021 / 9D-2-C03·C05).** В тексте
> ADR-019 ниже встречается ранний термин «`APPROVED` EngineeringEvaluation». Он **сохранён как
> архитектурный след**; действующая формулировка —
> «`EFFECTIVE` `EngineeringEvaluationRevision`»
> (см. [[docs/project/DECISIONS#ADR-021. EngineeringEvaluation Core Canon (Task 9D-2)|ADR-021]],
> решения 9D-2-C03 и 9D-2-C05). Все упоминания «`APPROVED` (EngineeringEvaluation)» ниже читать
> как действующую `EFFECTIVE`-ревизию оценки. Это **не** относится к собственному статусу
> `APPROVED` сущности `FindingDisposition` (её lifecycle не меняется). Действующая lifecycle-модель
> оценки — `DRAFT → PREPARED → FIXED → EFFECTIVE → SUPERSEDED` (9D-2-C06); диаграмма
> `… → PENDING_APPROVAL → APPROVED → SUPERSEDED` ниже сохранена как исторический след ADR-019.

### Контекст

ADR-015/016 зафиксировали `Inspection Result` как технический факт, ADR-017 — контур
процессов после результата контроля. Оставалась недостаточно детализированной
**внутренняя механика участка `Quality Finding → Closure`**: как признак становится
(или не становится) подтверждённым дефектом, как отделяется инженерная классификация
от решения о действии и от технической приемлемости дефекта, как блокируется
производство, как фиксируется внешнее решение заказчика, как связываются
корректирующие действия и повторный контроль, как вычисляется готовность к закрытию.

### Основной канонический процесс

```text
QualityFinding
  → EngineeringEvaluation
  → Defect / DefectAcceptanceAssessment
  → FindingDisposition
  → ProductionHold
  → Corrective Action / Reinspection
  → CustomerQualityDecision
  → Closure
```

`QualityFinding` — зарегистрированный факт потенциального или подтверждённого
несоответствия, требующий формального рассмотрения. Сам по себе finding **не**
означает наличия `Defect`, негодности `Joint`, необходимости ремонта и **не** является
решением ОГС. `Defect` создаётся **только** после утверждённой `EngineeringEvaluation`
с классификацией `CONFIRMED_DEFECT`.

### Решение

**1. QualityFinding.** Принадлежит одному `Joint`; имеет UUID; после регистрации
получает человекочитаемый номер `<PROJECT_CODE>-QF-<SEQUENCE>`; поддерживает внешние
номера/ссылки, `origin_type`, `initial_risk`, неизменяемое исходное наблюдение,
location/evidence/correction/assignment history и точные ссылки на источник контроля.
Регистрировать finding может любой авторизованный участник проекта в пределах
полномочий. ОГС подтверждает получение (acknowledge), управляет инженерной оценкой,
контролирует disposition и выполняет формальное закрытие. Исходное описание после
регистрации **неизменяемо**; дополнения и исправления оформляются отдельными
аудируемыми сущностями `FindingEvidence`, `FindingCorrection`, `FindingAssignment`.
`DRAFT` можно удалить; `REGISTERED` физически удалить нельзя; ошибочная/дублирующая
зарегистрированная запись переводится в `CANCELLED` с обязательным основанием.

**2. Origin Type** (канонический справочник происхождения; не заменяет инженерную
классификацию): `INSPECTION_RESULT`, `VISUAL_OBSERVATION`, `WELDING_PROCESS`,
`PERSONNEL_ADMISSION`, `WELDING_MATERIAL`, `BASE_MATERIAL`, `HEAT_TREATMENT`,
`ENGINEERING_DOCUMENT`, `EXECUTIVE_DOCUMENTATION`, `TRACEABILITY`, `CUSTOMER_REMARK`,
`INTERNAL_REVIEW`, `OTHER`.

**3. Risk и Severity разделены.** `initial_risk` (`LOW`/`MEDIUM`/`HIGH`/`CRITICAL`/
`UNKNOWN`) назначается до завершения оценки и используется для приоритета, SLA и
эскалации. `confirmed_severity` (`NOT_APPLICABLE`/`MINOR`/`MAJOR`/`CRITICAL`)
устанавливается **только** утверждённой `EngineeringEvaluation`.

**4. FindingLocation.** Один finding может охватывать несколько зон только при
одновременном соблюдении: зоны однородны, имеют одно техническое значение, требуют
одной инженерной оценки и одного disposition. Для разных дефектов, причин или решений
создаются отдельные finding.

**5. Source Linkage.** Многоуровневая связь finding с источником: `Inspection` →
`MethodExecution` → `InspectionResult` → `InspectionResultItem` →
`LaboratoryConclusion`; также `WeldOperation`, `Repair`, `HeatTreatment`, редакция
`EngineeringDocument` и другие подтверждённые производственные записи. Ссылка на
точный `ResultItem` предпочтительнее общей ссылки на `Inspection`.

**6. Automatic Finding Creation** (управляемая). Автосоздание finding по финальным
отрицательным результатам контроля выполняется по явному **версионируемому правилу** с
**deduplication key**; повторная обработка одного результата не создаёт дубликат;
автоматически созданный finding проходит обычный lifecycle; положительный повторный
контроль **не** закрывает finding автоматически. Машинный движок `ComplianceRule` в
первую реализацию Task 9D **не входит**.

**7. EngineeringEvaluation** — отдельная версионная сущность. `WELDING_ENGINEER`
создаёт, редактирует `DRAFT`, отправляет на утверждение, дорабатывает после возврата.
`CHIEF_WELDER` возвращает и утверждает. Утверждённая версия неизменяема; новая версия
создаётся отдельно, ссылается на предыдущую, не перезаписывает историю и после
утверждения делает предыдущую `SUPERSEDED`. Одновременно активна только одна
`APPROVED`-версия. Классификации: `CONFIRMED_DEFECT`, `NOT_CONFIRMED`,
`TECHNOLOGICAL_DEVIATION`, `DOCUMENTATION_NONCONFORMITY`,
`INSPECTION_PROCESS_NONCONFORMITY`, `MATERIAL_TRACEABILITY_NONCONFORMITY`,
`PERSONNEL_QUALIFICATION_NONCONFORMITY`, `REQUIRES_ADDITIONAL_EVIDENCE`,
`OUT_OF_SCOPE`.

**8. RequirementReference** (структурированная). Сохраняет документ, редакцию
документа, пункт/раздел, снимок применённого текста, источник, применимость к проекту
и связь с `EngineeringEvaluation`/disposition/другим решением. Историческое решение
**не** изменяется при появлении новой редакции документа.

**9. Document Applicability** отделена от наличия документа в системе. Статусы:
`REFERENCE_ONLY`, `UNDER_REVIEW`, `APPLICABLE`, `APPLICABLE_WITH_LIMITATIONS`,
`SUPERSEDED`, `NOT_APPLICABLE`. Как **обязательное нормативное основание** используются
только `APPLICABLE` и `APPLICABLE_WITH_LIMITATIONS`. Документ, загруженный только для
ознакомления (`REFERENCE_ONLY`), **не** активирует правила проекта. `WELDING_ENGINEER`
готовит оценку применимости, `CHIEF_WELDER` утверждает техническую применимость;
внешнее согласование заказчика/проектировщика фиксируется отдельно при необходимости.
Применимость версионна и действует во времени; режимы перехода на новую редакцию:
`NEW_WORK_ONLY`, `ALL_OPEN_FINDINGS`, `ALL_UNCLOSED_JOINTS`, `PROJECT_WIDE_REASSESSMENT`,
`MANUAL_SCOPE`.

**10. Impact Scope.** `EngineeringEvaluation` определяет `impact_scope`:
`NO_OPERATIONAL_IMPACT`, `DOCUMENT_HANDOVER_BLOCK`, `INSPECTION_ACCEPTANCE_BLOCK`,
`FURTHER_PROCESSING_BLOCK`, `TECHNICAL_ACCEPTANCE_BLOCK`, `FULL_JOINT_BLOCK`. Impact
scope — основание для вычисления состояния `Joint` и `ProductionHold`.

**11. Defect** — отдельная подтверждённая техническая запись; создаётся только после
утверждённой `EngineeringEvaluation` с классификацией `CONFIRMED_DEFECT` (finding —
процесс рассмотрения, Defect — подтверждённый факт). `DefectType` — управляемый
справочник (канонический внутренний код, русское наименование, описание, внешние
отображения/коды, период действия); полный перечень дефектов **не** зашивается в enum
приложения. Вычисляемый lifecycle Defect: `CONFIRMED`, `ACTION_PLANNED`,
`UNDER_CORRECTION`, `REINSPECTION_PENDING`, `REINSPECTION_FAILED`, `REMOVED`,
`ACCEPTED_AS_IS`, `NOT_REMOVABLE`, `VOIDED` — пользователь не выбирает вычисляемый
статус произвольно.

**12. DefectAcceptanceAssessment** (техническая приемлемость дефекта, отдельно от
disposition): `ACCEPTABLE`, `UNACCEPTABLE`, `CONDITIONALLY_ACCEPTABLE`,
`INSUFFICIENT_DATA`, `NOT_APPLICABLE`. Приемлемость ≠ `FindingDisposition`
(например `UNACCEPTABLE + REPAIR`; `UNACCEPTABLE + CUT_OUT_AND_REPLACE`;
`CONDITIONALLY_ACCEPTABLE + ACCEPT_AS_IS`; `INSUFFICIENT_DATA + ADDITIONAL_INSPECTION`).
Система блокирует противоречивые комбинации.

**13. DefectMeasurement** создаётся на основании фактического источника контроля и
хранит тип измерения, значение, единицу, местоположение, метод контроля, источник
результата, автора, дату/время, версию и статус применимости. ОГС **не** переписывает
измерения лаборатории; отдельным действием принимает или отклоняет применимость
измерения для инженерной оценки.

**14. Геометрия дефекта и ремонта разделена** на три сущности: `DefectLocation`
(обнаруженная область), `RepairExcavationZone` (фактическая выборка), `RepairWeldZone`
(фактическая ремонтная сварка). Они не являются одной сущностью.

**15. FindingDisposition** отделён от `EngineeringEvaluation`: оценка отвечает «что
установлено технически?», disposition — «что необходимо сделать?». Типы:
`NO_ACTION_REQUIRED`, `ADDITIONAL_INSPECTION`, `DOCUMENT_CORRECTION`, `PROCESS_REVIEW`,
`ACCEPT_AS_IS`, `REPAIR`, `REWELD`, `CUT_OUT_AND_REPLACE`, `REJECT_JOINT`,
`RETURN_FOR_ADDITIONAL_EVALUATION`. Disposition создаётся только на основании
актуальной `APPROVED` `EngineeringEvaluation`. Уровень утверждения зависит от типа:
`WELDING_ENGINEER` утверждает решения низкого воздействия в пределах утверждённой
матрицы полномочий; `CHIEF_WELDER` — критические (`ACCEPT_AS_IS`, `REPAIR`, `REWELD`,
`CUT_OUT_AND_REPLACE`, `REJECT_JOINT` и иные с техническим блокированием).
`ACCEPT_AS_IS` включает внутреннее техническое обоснование ОГС и отдельное решение
заказчика/проектировщика, если оно требуется.

> **Историческая модель — `SUPERSEDED_BY DefectDisposition`.** Этот пункт не
> переносится в новую модель автоматически. Точный mapping типов/статусов и запрет
> автоматической миграции определены ADR-024 §C. Запись остаётся историческим источником,
> а не спецификацией будущего кода Task 9D-4A.

**16. Corrective Action Authorization.** Утверждение disposition **не** означает
автоматического разрешения начать работу. Вводится отдельное аудируемое действие
`authorize_corrective_action_start`; до него нельзя начинать repair, reweld, cut-out,
replacement и иные критические корректирующие действия. Разрешение выдаётся в пределах
матрицы полномочий.

**17. Repair Eligibility.** `RepairLimitPolicy` и `RepairEligibilityAssessment` —
часть канона; полный универсальный нормативный движок в первый объём 9D **не входит**.
`RepairEligibilityAssessment` определяет допустимость выбранного действия по типу
дефекта, числу предыдущих ремонтов, зоне, материалу, толщине, технологии, применимому
документу и другим проектным ограничениям.

**18. ReinspectionRequirement.** Для disposition `REPAIR`, `REWELD`,
`CUT_OUT_AND_REPLACE` до разрешения начала работы должен существовать утверждённый
`ReinspectionRequirement` (обязательные методы, объём, зоны, этап, критерии завершения,
необходимость внешнего решения, применимые требования). Нельзя сначала завершить
ремонт, а затем неаудируемо определить требуемый контроль.

**19. CorrectiveActionLink.** `FindingDisposition` связывается с фактическими
действиями через `CorrectiveActionLink` (цели: `Repair`, `Reweld`,
`CutOutAndReplacement`, дополнительный `Inspection`, `DocumentCorrection`,
`ProcessReview`, иное утверждённое действие). Disposition не считается исполненным по
текстовой отметке пользователя; состояние исполнения вычисляется по связанным
фактическим объектам.

**20. Reinspection Result.** Положительный повторный контроль **не** закрывает finding
автоматически — подтверждает выполнение одного из условий и может перевести finding в
`READY_FOR_CLOSURE` при выполнении остальных. Отрицательный повторный контроль:
продолжает текущий finding при том же дефекте в той же зоне; создаёт новую версию
`EngineeringEvaluation` и disposition при необходимости; создаёт новый связанный
finding при новом дефекте, новой зоне или самостоятельной проблеме.

**21. CustomerQualityDecision** — отдельная сущность, **не** внутренняя
`EngineeringEvaluation` ОГС. Разделяются внешний участник, фактически принявший
решение, и внутренний пользователь, зарегистрировавший его (`CHIEF_WELDER`,
`WELDING_ENGINEER`, `PTO_ENGINEER`, `INSPECTOR`). Доказательство внешнего решения
обязательно (документ, письмо, протокол, подписанная форма, ссылка на подтверждённый
источник). История внешних решений версионна и неизменяема.

**22. Quality State Joint.** Lifecycle `Joint` не смешивается с состоянием качества.
Для `Joint` вычисляются отдельно `technical_quality_state`, `documentation_state`,
`customer_acceptance_state`, `handover_readiness`, `production_hold`. Агрегация — по
всем активным finding; применяется наиболее строгое действующее состояние; API
возвращает ссылки на finding, блокирующие `Joint`.

**23. ProductionHold** — отдельная сущность; разделяются временный оперативный hold и
подтверждённый инженерный hold. Хранит уровень (`PARTIAL`/`FULL`), область действия,
перечень запрещённых операций, основание, автора, время установки, связь с finding,
подтверждение ОГС и release history. Снятие — только через `ProductionHoldRelease`;
прямое редактирование статуса запрещено; при нескольких hold применяется наиболее
строгое совокупное ограничение.

**24. Root Cause.** Локальное устранение состояния отделено от расследования системной
причины. Статусы root cause: `NOT_REQUIRED`, `NOT_INVESTIGATED`, `UNDER_INVESTIGATION`,
`IDENTIFIED`, `ACTION_PLANNED`, `ACTION_IMPLEMENTED`, `EFFECTIVENESS_VERIFIED`. Закрытие
отдельного finding не всегда требует завершённого CAPA.

**25. Будущие контуры (зафиксированы как точки расширения, не реализуются в 9D):**
`ResponsibilityAssessment` (ограниченная сущность; не назначает виновного
автоматически, не считает сварщика ответственным автоматически, не часть исходного
finding, не видна всем ролям); `FindingPattern` (система предлагает, ОГС подтверждает
вручную); `CorrectivePreventiveAction` (`CORRECTIVE_ACTION`, `PREVENTIVE_ACTION`,
`PROCESS_IMPROVEMENT`, `TRAINING_ACTION`, `DOCUMENT_UPDATE`, `SYSTEM_CHANGE`,
`SUPPLIER_ACTION`; локальный ремонт Joint — не CAPA); `ComplianceRule` /
`ComplianceRuleTestCase` / `AutomatedRuleTest` / `ComplianceRuleExecution` /
`ComplianceOverride` / `ComplianceExecutionCorrection`. Архитектурная граница:
**applicable document ≠ active machine rule**.

### Жизненные циклы

```text
QualityFinding:
DRAFT → REGISTERED → UNDER_EVALUATION → DISPOSITION_PENDING → ACTION_REQUIRED
      → ACTION_IN_PROGRESS → REINSPECTION_PENDING → READY_FOR_CLOSURE → CLOSED
Ветка внешнего решения:
DISPOSITION_PENDING → CUSTOMER_DECISION_PENDING → READY_FOR_CLOSURE
Исключения:  DRAFT → deleted ;  REGISTERED → CANCELLED
(CLOSED и CANCELLED конечны; закрытый finding не открывается повторно —
новое обстоятельство создаёт новый finding или новую версию оценки)

EngineeringEvaluation:
DRAFT → PENDING_APPROVAL → APPROVED → SUPERSEDED
Возврат:  PENDING_APPROVAL → RETURNED → DRAFT
Отмена:   DRAFT → CANCELLED ;  RETURNED → CANCELLED
(APPROVED неизменяема и не отменяется через CANCELLED; исправление — новой версией;
утверждает только CHIEF_WELDER)

FindingDisposition:
DRAFT → PENDING_APPROVAL → APPROVED → IN_EXECUTION → COMPLETED
Возврат:  PENDING_APPROVAL → RETURNED → DRAFT
Отмена:   DRAFT → CANCELLED ;  RETURNED → CANCELLED
Пересмотр: APPROVED / IN_EXECUTION → SUPERSEDED
(создаётся только по APPROVED EngineeringEvaluation; IN_EXECUTION вычисляется по
фактическим действиям; COMPLETED не закрывает finding автоматически; REJECT_JOINT
может быть COMPLETED, даже если Joint остаётся отклонённым)

ProductionHold:
ACTIVE → CONFIRMED → PARTIALLY_RELEASED → RELEASED
Замена:  ACTIVE / CONFIRMED / PARTIALLY_RELEASED → SUPERSEDED
Ошибка:  ACTIVE → INVALIDATED
(подтверждает ОГС; partial/full release — через ProductionHoldRelease; прямое
изменение статуса запрещено; RELEASED не закрывает finding; просроченный hold не
снимается автоматически)
```

### Closure

Готовность к закрытию **вычисляется системой**. Finding становится `READY_FOR_CLOSURE`
только если: существует актуальная `APPROVED` `EngineeringEvaluation`; существует
допустимый актуальный disposition; обязательные действия завершены;
`ReinspectionRequirement` выполнены; обязательное внешнее решение зарегистрировано;
активные `ProductionHold` сняты либо допустимо заменены; нет незавершённых блокирующих
зависимостей; обязательные доказательства присутствуют. Формальное закрытие выполняет
авторизованная роль ОГС; уровень полномочий зависит от серьёзности и disposition.

### Роли (действующий канон; новых role_code не вводится)

- **`CHIEF_WELDER`** — неограниченная административная роль системы: утверждение
  `EngineeringEvaluation`, критические disposition, подтверждение технических hold,
  формальное закрытие критических finding, техническое утверждение применимости
  документов; утверждение `ComplianceOverride` — в будущем контуре. **Обязательный
  fallback** критических полномочий ОТК при отсутствии внутреннего ОТК (решение
  008-07-BP).
- **`WELDING_ENGINEER`** — контур ОГС: подготовка evaluation, регистрация и
  рассмотрение finding, подготовка disposition, действия в пределах утверждённой
  матрицы полномочий; общесистемные административные права **не** получает; **не
  заменяет** `CHIEF_WELDER` в критических решениях.
- **`OTK_INSPECTOR`** — **опциональная проектная роль** (решение 008-07-BP): наличие
  внутреннего ОТК определяется конфигурацией проекта; отдельное внутреннее
  подразделение ОТК **не обязательно**. Workflow **не требует** фиктивного пользователя
  ОТК: при отсутствии внутреннего ОТК его критические полномочия в сварочном контуре
  выполняет `CHIEF_WELDER`. При наличии реального внутреннего ОТК роль активируется
  **без изменения доменной модели**.
- **Заказчик** — **не** является внутренним ОТК и **не** заменяет внутреннее инженерное
  решение ОГС; фиксируется через `CustomerQualityDecision`. Результат контроля
  «годен / не годен», установленный представителем заказчика, сохраняется как **внешний**
  результат/решение (или внешний inspection result), а не как внутреннее решение ОГС.

Итоговая схема (решение 008-07-BP): `internal OTK workflow = configurable` ·
`CHIEF_WELDER fallback = mandatory` · `customer inspector = external quality contour`.

### Границы первой реализации Task 9D

**Входят:** `QualityFinding`, `FindingLocation`, `FindingEvidence`, `FindingCorrection`,
`FindingAssignment`, `EngineeringEvaluation`, `RequirementReference`, `Defect`,
`DefectType`, `DefectMeasurement`, `DefectAcceptanceAssessment`, `FindingDisposition`,
`ProductionHold`, `ProductionHoldRelease`, `CustomerQualityDecision`,
`CorrectiveActionLink`, `ReinspectionRequirement`.

**Не входят:** `ResponsibilityAssessment`, `FindingPattern`,
`CorrectivePreventiveAction`, `ComplianceRule`, `ComplianceRuleTestCase`,
`AutomatedRuleTest`, `ComplianceRuleExecution`, `ComplianceOverride`,
`ComplianceExecutionCorrection`. Для исключённых контуров фиксируются архитектурные
точки расширения; пустые таблицы не создаются, API не обещается, реализация в Task 9D
не выполняется.

### Разбиение Task 9D (утверждено Session 008-07)

| Подзадача | Содержание |
|-----------|-----------|
| **9D-1 — QualityFinding Core** | `QualityFinding`, `FindingLocation`, `FindingEvidence`, `FindingCorrection`, `FindingAssignment`, нумерация, register, acknowledge, базовый lifecycle |
| **9D-2 — Engineering Evaluation** | `EngineeringEvaluation`, версии, approval, классификация, `confirmed_severity`, `impact_scope`, `RequirementReference` |
| **9D-3 — Defect Technical Model** | `Defect`, `DefectType`, `DefectMeasurement`, `DefectAcceptanceAssessment`, вычисляемый lifecycle |
| **9D-4 — Finding Disposition and Holds** | `FindingDisposition`, `ProductionHold`, `ProductionHoldRelease`, вычисление quality state |
| **9D-5 — Customer Quality Decision** | `CustomerQualityDecision`, внешний участник, внутренний регистратор, evidence, version history |
| **9D-6 — Corrective Action and Reinspection Links** | `CorrectiveActionLink`, `ReinspectionRequirement`, интеграция с repair/reweld/inspection, closure readiness |
| **9D-7 — API, permissions and integration** | API, RBAC, cross-module validation, integration tests |
| **9D-8 — Architecture consolidation** | финальная консолидация документации **после** реализации блоков |

> Текущее задание — **архитектурная консолидация Session 008-07 до начала реализации**
> и **не заменяет** будущий блок **9D-8** (консолидация по итогам реализации).

### Архитектурные границы

- ADR-019 определяет **внутреннюю механику участка `Quality Finding → Closure`** и не
  переопределяет технический слой контроля (ADR-015/016) и контур официальных
  документов (ADR-018).
- Инженерная оценка обязана ссылаться на конкретные **редакции** официального документа
  (`Document Revision`, ADR-018) и на структурированную `RequirementReference`.
- В канон **не входят**: реализация (модели, миграции, API, тесты, frontend, backend),
  импорт результатов НК, будущие контуры Responsibility/Pattern/CAPA/Compliance,
  генераторы документов и ЭП.

### Решения 008-07-BO / BP / BQ

Три ранее открытых вопроса консолидации закрыты утверждёнными решениями Session 008-07.

**008-07-BO — реализационная структура Task 9D.** Официальной реализационной структурой
становится **9D-1 … 9D-8** (см. «Разбиение Task 9D»). Старые Tasks **9E — 9K** (Session
008 / ADR-017) **не** остаются параллельной конкурирующей структурой: полностью или
частично поглощённые части помечаются `SUPERSEDED_BY_TASK_9D`; непоглощённый
функциональный объём (полный ремонтный lifecycle, реестр документов качества)
сохраняется как будущие задачи и подлежит отдельной перенумерации после утверждения
mapping table. Полная таблица соответствия (старый Task → поглощённый блок 9D →
остаточный объём → будущая задача) — в
[[docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP#Соответствие старых Tasks 9E — 9K блокам 9D-1 … 9D-8 (решение 008-07-BO)|Implementation Plan]].
Кратко: `9E → 9D-2 + 9D-4 + 9D-5`; `9F → 9D-3`; `9G → частично 9D-2 + 9D-3` (root cause
и `ResponsibilityAssessment` — будущий контур); `9J → частично 9D-6` (фактическое
выполнение контроля — в Inspection Core).

**008-07-BP — роль ОТК.** Канон: `OTK_INSPECTOR = optional project role` (см. «Роли»).
Отдельное внутреннее подразделение ОТК не обязательно; его наличие определяется
конфигурацией проекта; workflow не требует фиктивного пользователя ОТК; при отсутствии
внутреннего ОТК критические полномочия выполняет `CHIEF_WELDER` (`CHIEF_WELDER fallback =
mandatory`); `WELDING_ENGINEER` не заменяет `CHIEF_WELDER`; представитель заказчика —
внешний контур качества (`CustomerQualityDecision`), а не внутренний ОТК. Согласовано с
ADR-017 (частичное замещение безусловной обязательности ОТК) и разделами архитектуры
(§5.8/§5.9, матрица владельцев доменов).

> **Локальное ограничение ADR-024.** Fallback `CHIEF_WELDER` не применяется к
> `DefectDisposition`: в MVP ordinary `APPROVE` выполняет только effective
> `OTK_INSPECTOR` в GLOBAL/соответствующем PROJECT scope, а override отсутствует. Это
> ограничение не отменяет BP для других исторических/будущих контуров качества.

**008-07-BQ — статус ADR-017.** `ADR-017 = PARTIALLY_SUPERSEDED_BY_ADR-019` (см. статусный
блок ADR-017). Каноническая декомпозиция: `Quality Decision → EngineeringEvaluation →
DefectAcceptanceAssessment → FindingDisposition`; **`Quality Decision` более не является
самостоятельной доменной сущностью** — допустимо только как обобщённое бизнес-понятие.
Термин `Defect Severity` в текущем каноне заменён на `Confirmed Severity`. ADR-017
сохраняется в истории и действует в непересекающейся части (общий принцип внутреннего и
внешнего решения; связь с печатными формами; роли и внешние решения, не противоречащие
ADR-019; положения вне модели finding/evaluation/defect/disposition).

### Где зафиксировано

```text
docs/ARCHITECTURE.md (§5.9)
docs/project/ARCHITECTURE_SESSIONS.md (Session 008-07)
docs/project/DECISIONS.md (ADR-019)
docs/project/PROJECT_SUMMARY.md (Quality Finding / Task 9D canon)
docs/project/UBIQUITOUS_LANGUAGE.md (Раздел 11)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Task 9D — 9D-1 … 9D-8)
```

---

## ADR-003. Исключение модуля нормирования из активного MVP

Дата: 2026-07-03

Статус: принято

Сводный узел: [[10_Проектирование_WeldPassport/10_Нормы_времени/00_Нормы_времени|Нормы времени (backlog)]] ·
канон: [[docs/ARCHITECTURE#Активное ядро MVP и отложенные модули|Архитектура — активное ядро MVP]].

### Контекст

Ранее в проекте рассматривался модуль расчёта норм времени для операций `MARK_PIPE`,
`CUT_PIPE`, `EDGE_PREP`, `JOINT_ASSEMBLY`, `WELD_PIPE`, `QC_WELD`. Были предусмотрены
таблицы `norms.material_groups`, `norms.base_norms`, `norms.org_norms`. Однако расчёт
времени сварки для каждого сварщика требует отдельной методики, нормативной базы,
коэффициентов и согласования — на текущем этапе это слишком сложно, спорно и может
затормозить основной проект.

### Решение

Модуль нормирования времени сварочных работ **исключён из активного MVP WeldPassport**.

- Не использовать `norms.*` как часть основной бизнес-логики MVP.
- Перенести модуль в backlog / future modules.
- Материалы проектирования (`10_Проектирование_WeldPassport/10_Нормы_времени/`) и
  обсуждения в чатах сохраняются как задел на будущее — **не удалять**.

Формулировка для документации:

> Модуль нормирования времени сварочных работ исключён из активного MVP WeldPassport.
> Причина — высокая сложность и спорность расчёта времени сварки по каждому сварщику
> на текущем этапе. Модуль сохраняется как возможное будущее расширение, но активная
> архитектура MVP фокусируется на учёте работников, ролей, допусков сварщика, факта
> сварки, контроля, исполнительной документации и закрытия стыков.

### Последствия

MVP фокусируется на жизненном цикле сварочного производства:

- `hr.workers`, `hr.worker_roles`;
- `ogs` / `welding.welder_admissions`;
- `production.joints`, `production.weld_operations`;
- `periodic_kss.*`;
- результаты контроля QC / NDT;
- исполнительная документация ПТО;
- закрытие и история стыка.

Нормирование может быть возвращено позже отдельным этапом.

### Ограничение

**Не добавлять** новые зависимости активных модулей от `norms.*`.

Поле `material_groups` в `welding.welder_admissions` относится к допуску сварщика
(группы материалов по аттестации) и **не является** частью схемы `norms.*`.

### Где зафиксировано

```text
docs/project/DECISIONS.md
docs/ARCHITECTURE.md
docs/project/PROJECT_SUMMARY.md
10_Проектирование_WeldPassport/10_Нормы_времени/00_Нормы_времени.md
```

---

## ADR-020. Отдельный Project Control Center для контроля реализации

Дата: 2026-07-19

Статус: принято; первый рабочий срез реализован

### Контекст

Статусные документы, Git и результаты проверок дают разрозненное представление о
ходе реализации. Владельцу и разработчикам нужна единая интерактивная карта,
показывающая готовность по жизненному циклу WeldPassport, зависимости, риски и
следующий практический шаг.

### Решение

Создать отдельный **Project Control Center**, не являющийся производственным модулем
WeldPassport. Первая версия работает как самостоятельная web-панель, читает
локальный Git, GitHub, CI, тесты, миграции и проектные документы, а собственные
данные хранит в PostgreSQL в отдельной схеме `project_control`.

Техническая готовность рассчитывается автоматически. Владелец не изменяет этот
процент, а отдельно подтверждает управленческий статус. Все оценки и снимки
версионируются. Интеграции с Git и GitHub работают только на чтение; производственные
таблицы WeldPassport не используются.

### Последствия

- служебный контроль разработки не смешивается с MES/ERP-контуром;
- появляется единое объяснимое представление прогресса;
- дальнейшее развитие ведётся по утверждённой спецификации; по решению владельца
  отдельный файл плана перед первым рабочим срезом не создавался;
- встраивание интерфейса в WeldPassport допускается позднее без объединения данных;
- автоматическое редактирование ADR, roadmap и исходного кода в MVP запрещено.

### Где зафиксировано

```text
docs/superpowers/specs/2026-07-19-project-control-center-design.md
docs/project/DECISIONS.md (ADR-020)
docs/project/PROJECT_SUMMARY.md
docs/project/ROADMAP.md
```

---

## ADR-021. EngineeringEvaluation Core Canon (Task 9D-2)

Дата: 2026-07-19

Статус: **ACCEPTED — принято.** Углублённый канон ядра сущности `EngineeringEvaluation`
под **Task 9D-2** (ревизионность, источники, критерии, `EngineeringException`, уровень
уверенности, остаточный риск, срок пересмотра, lifecycle ревизии, события/аудит).
Канон утверждён без создания кода/моделей/миграций/API/тестов.

> **Историческая ссылка на disposition.** Упоминания `FindingDisposition` в ADR-021
> сохраняют контекст решения 9D-2. ADR-024 заменяет эту модель на
> `DefectDisposition` только для подтверждённых Defect; `recommended_disposition` остаётся
> необязывающей рекомендацией EngineeringEvaluation и не мигрирует автоматически.

Статус реализации: **NOT IMPLEMENTED — не реализовано.** Реализован только предыдущий
блок Task 9D-1 (`QualityFinding` Core). Task 9D-2 не начата.

Контур: Quality / Engineering Evaluation.

Продолжает и детализирует: [[docs/project/DECISIONS#ADR-019. Quality Finding and Engineering Evaluation Canon (Session 008-07)|ADR-019]]
(канон участка `Quality Finding → Closure`; ADR-019 ввёл сущность `EngineeringEvaluation`,
ADR-021 задаёт её внутреннее ядро под Task 9D-2)

Опирается на: [[docs/project/DECISIONS#ADR-016. Quality Execution Model (Task 9C)|ADR-016]]
(`Inspection Result` как источник) ·
[[docs/project/DECISIONS#ADR-017. Quality Decision, Defect, Repair and Quality Documents Canon (Session 008)|ADR-017]] ·
[[docs/project/DECISIONS#ADR-018. Electronic Documents and Printed Forms Canon (Session 008-06)|ADR-018]]
(документы и редакции как источники/критерии)

> **Номер.** Заголовок исходного материала содержал «ADR-019», но номера ADR-019
> (Quality Finding and Engineering Evaluation Canon) и ADR-020 (Project Control Center)
> уже заняты. Присланный документ — отдельный, более детальный канон ядра
> `EngineeringEvaluation`, поэтому зафиксирован как **ADR-021** (следующий свободный
> номер). Ссылка исходной шапки `Связанные решения: ADR-016, ADR-020` скорректирована:
> по смыслу канон продолжает **ADR-019**, а не ADR-020.

> **Отношение к ADR-019 — решение 9D-2-C01 (принято 2026-07-19).** ADR-019 намеренно
> разделил единое `Quality Decision` на сущности `EngineeringEvaluation` — «что установлено»,
> `DefectAcceptanceAssessment` — «приемлем ли дефект технически» и `FindingDisposition` —
> «что необходимо сделать». Ранняя редакция ADR-021 давала `EngineeringEvaluation` единый
> `decision_type`, значения которого (`REPAIR_REQUIRED` и т.п.) вторгались в зону
> `FindingDisposition`. Конфликт закрыт решением **9D-2-C01**:
>
> 1. `EngineeringEvaluation.decision_type` заменён на **два поля**: `evaluation_outcome`
>    (что установлено) и `recommended_disposition` (рекомендация ОГС по дальнейшему маршруту).
> 2. `FindingDisposition` остаётся **отдельной сущностью** и **единственным источником
>    официального исполняемого решения** (контур Task 9D-4).
> 3. `recommended_disposition` **не** меняет статус `QualityFinding`, **не** создаёт
>    `Repair` / `Rework` / `Inspection`, **не** является действующим распоряжением и служит
>    лишь входом для подготовки `FindingDisposition`.
>
> Каноническая формулировка — в §2.5 и §2.6.

> **Решения 9D-2-C02 … C10 (2026-07-19).**
>
> **9D-2-C02 — классификация в ревизии.** `confirmed_severity`
> (`NOT_APPLICABLE`/`MINOR`/`MAJOR`/`CRITICAL`) и `impact_scope`
> (`NO_OPERATIONAL_IMPACT`/`DOCUMENT_HANDOVER_BLOCK`/`INSPECTION_ACCEPTANCE_BLOCK`/
> `FURTHER_PROCESSING_BLOCK`/`TECHNICAL_ACCEPTANCE_BLOCK`/`FULL_JOINT_BLOCK`) хранятся в
> `EngineeringEvaluationRevision` и относятся к конкретной ревизии; редактируются только в
> `DRAFT`, после `PREPARED` неизменяемы. Новая ревизия может изменить классификацию. Поля
> `QualityFinding` ими не переписываются; актуальные значения читаются только из
> `EFFECTIVE`-ревизии. Перечни `confirmed_severity`/`impact_scope` — канон ADR-019
> (решения 3 и 10); новые enum не вводятся.
>
> **9D-2-C03 — терминология действующей оценки.** Термин «`APPROVED` EngineeringEvaluation»
> признан **устаревшим forward reference**. Каноническая замена — **`EFFECTIVE`
> EngineeringEvaluationRevision**. Новый статус `APPROVED` не вводится: `FIXED` = «зафиксировано»,
> `EFFECTIVE` = «действует». Устаревший комментарий/forward reference в коде 9D-1 исправляется
> без изменения поведения.
>
> **9D-2-C04 — lifecycle finding не трогается.** Task 9D-2 **не меняет** `QualityFinding.status`.
> После `EFFECTIVE`-оценки finding остаётся `UNDER_EVALUATION`. Переход
> `UNDER_EVALUATION → DISPOSITION_PENDING` принадлежит `FindingDisposition` (Task 9D-4).
> `recommended_disposition` не создаёт `FindingDisposition` автоматически.
>
> **9D-2-C05 — терминологическая консолидация.** Термин «`APPROVED` EngineeringEvaluation»
> (ранний forward reference ADR-019) заменяется на **`EFFECTIVE` `EngineeringEvaluationRevision`**
> во всех действующих нормативных формулировках (`ARCHITECTURE.md`, `ARCHITECTURE_SESSIONS.md`,
> `UBIQUITOUS_LANGUAGE.md`). В ADR-019 ранний термин **сохраняется как архитектурный след** с
> явной отсылкой к 9D-2-C03/C05 (текст ADR-019 не переписывается). Статус `APPROVED` у
> `FindingDisposition` (её собственный lifecycle `… → APPROVED → IN_EXECUTION`) — **иное
> понятие** и не затрагивается.
>
> **9D-2-C06 — консолидация lifecycle EngineeringEvaluation.** Живые lifecycle-диаграммы
> `EngineeringEvaluationRevision` в `ARCHITECTURE.md` и `ARCHITECTURE_SESSIONS.md` приводятся к
> модели ADR-021:
>
> ```text
> DRAFT → PREPARED → FIXED → EFFECTIVE → SUPERSEDED
>       (при обязательном согласовании: FIXED → PENDING_APPROVAL → EFFECTIVE)
>       (+ PREPARED → DRAFT возврат; PREPARED/FIXED/PENDING_APPROVAL → WITHDRAWN)
> ```
>
> Статус `APPROVED` для `EngineeringEvaluation` **не используется**. Статус `APPROVED` у
> `FindingDisposition` **не меняется**. ADR-019 остаётся историческим следом с редиректом на
> ADR-021 / 9D-2-C03·C05·C06 (текст ADR-019 не переписывается).
>
> **9D-2-C07 — classification в ревизии.** Инженерная классификация `classification` хранится
> в `EngineeringEvaluationRevision`, обязательна перед `PREPARED`, изменяема только в `DRAFT`,
> актуальна только из `EFFECTIVE`-ревизии; поля `QualityFinding` не изменяются. Точный enum —
> канон ADR-019 («Evaluation Classification»):
>
> ```text
> CONFIRMED_DEFECT · NOT_CONFIRMED · TECHNOLOGICAL_DEVIATION · DOCUMENTATION_NONCONFORMITY ·
> INSPECTION_PROCESS_NONCONFORMITY · MATERIAL_TRACEABILITY_NONCONFORMITY ·
> PERSONNEL_QUALIFICATION_NONCONFORMITY · REQUIRES_ADDITIONAL_EVIDENCE · OUT_OF_SCOPE
> ```
>
> Обязательная согласованность `classification → evaluation_outcome` (иные комбинации →
> `EVAL_CLASSIFICATION_OUTCOME_MISMATCH`):
>
> ```text
> CONFIRMED_DEFECT                       → NONCONFORMING | CONDITIONALLY_ACCEPTABLE
> NOT_CONFIRMED                          → ACCEPTABLE
> TECHNOLOGICAL_DEVIATION                → NONCONFORMING | CONDITIONALLY_ACCEPTABLE
> DOCUMENTATION_NONCONFORMITY            → NONCONFORMING | CONDITIONALLY_ACCEPTABLE
> INSPECTION_PROCESS_NONCONFORMITY       → NONCONFORMING | CONDITIONALLY_ACCEPTABLE
> MATERIAL_TRACEABILITY_NONCONFORMITY    → NONCONFORMING | CONDITIONALLY_ACCEPTABLE
> PERSONNEL_QUALIFICATION_NONCONFORMITY  → NONCONFORMING | CONDITIONALLY_ACCEPTABLE
> REQUIRES_ADDITIONAL_EVIDENCE           → INSUFFICIENT_DATA
> OUT_OF_SCOPE                           → NOT_APPLICABLE
> ```
>
> `CONFIRMED_DEFECT` не порождает `Defect` в Task 9D-2 (автосоздание `Defect` — вне scope).
>
> **9D-2-C08 — единая структурная миграция.** В блоке **9D-2A** одна миграция создаёт **7**
> канонических таблиц: `engineering_evaluations`, `engineering_evaluation_revisions`,
> `engineering_evaluation_sources`, `engineering_evaluation_criteria`, `engineering_exceptions`,
> `engineering_evaluation_events`, `engineering_evaluation_sequences`. Таблицы `sources`,
> `criteria`, `exceptions` создаются **структурно**; их бизнес-поведение (сверка источников,
> критерии, исключения, матрица) остаётся в **9D-2B/2C**. Плановых `ALTER TABLE` для уже
> известных полей не используется.
>
> **9D-2-C09 — возврат.** `RETURNED_FOR_REVISION` **не является статусом**. Канонический набор
> статусов ревизии — **семь**: `DRAFT`, `PREPARED`, `FIXED`, `PENDING_APPROVAL`, `EFFECTIVE`,
> `SUPERSEDED`, `WITHDRAWN`. Возврат моделируется переходом `PREPARED → DRAFT`, оформляется
> событием `EVALUATION_RETURNED`; **причина возврата обязательна**. Новая ревизия при возврате
> не создаётся.
>
> **9D-2-C10 — полная структура Revision сразу.** `EngineeringEvaluationRevision` создаётся
> **полной** уже в 9D-2A: `evaluation_outcome`, `classification`, `recommended_disposition`,
> `confirmed_severity`, `impact_scope`, `rationale`, `confidence_level`, `confidence_note`,
> `residual_risk`, `application_conditions`, `review_due_at`, `revision_reason`,
> `supersedes_impact`, `required_approval_route`. Поля **nullable в `DRAFT`**; enum-CHECK
> действует при значении `NOT NULL`; обязательность и матрица комплектности применяются на
> `prepare` (позже). `rationale` **не** является безусловным DB `NOT NULL`.

> **Решения блоков 9D-2B/2C (валидация, источники, исключения; зафиксировано 2026-07-19).**
> Эти решения приняты при реализации 9D-2C и фиксируются ретроспективно, чтобы канон был
> непрерывным (C10 → C11 … C17 → C18).
>
> **9D-2-C11 — агрегированный `ValidationResult`.** Проверка комплектности `prepare`
> возвращает **полный список нарушений** (`ValidationResult.violations`), а **не** first-fail:
> инженер видит все проблемы ревизии сразу. HTTP-обёртка — `EVAL_PREPARE_INCOMPLETE` (422) с
> массивом кодов в `detail.violations`.
>
> **9D-2-C12 — validation checker строго read-only.** Функции `check_prepare_completeness`
> работают над уже загруженными объектами и **не** обращаются к БД, **не** пишут `verified_at`,
> **не** меняют хэши/статусы, **не** коммитят. Свежесть источников (нужна БД) вычисляется в
> repository и передаётся в checker как `source_issues`. Переход статуса выполняет сервис (9D-2D).
>
> **9D-2-C13 — `recommended_disposition` обязателен всегда.** Поле обязательно на `prepare`
> при любом исходе; **`NONE` — валидное значение** (не «пусто»). Отсутствие → `EVAL_DISPOSITION_REQUIRED`;
> недопустимое по исходу значение → `EVAL_DISPOSITION_NOT_ALLOWED` (матрица §8).
>
> **9D-2-C14 — `INSUFFICIENT_DATA` определяется критерием.** Исход `INSUFFICIENT_DATA`
> обоснован **наличием хотя бы одного критерия с `comparison_result = INSUFFICIENT_DATA`**, а не
> отдельным флагом ревизии. Несоответствие исхода составу критериев → `EVAL_OUTCOME_CRITERIA_MISMATCH`.
>
> **9D-2-C15 — отклонённый (deviated) критерий.** Отклонённым считается критерий с
> `comparison_result ∈ {DOES_NOT_COMPLY, CONDITIONALLY_COMPLIES}`. Только такой критерий может
> нести `EngineeringException`; исключение на иной результат → `EVAL_EXCEPTION_CRITERION_INVALID`.
>
> **9D-2-C16 — `confidence_level = LOW` требует `confidence_note`.** При низкой уверенности
> обязательно текстовое обоснование (причины + меры); отсутствие → `EVAL_CONFIDENCE_NOTE_REQUIRED`.
> Дублируется CHECK-инвариантом строки ревизии.
>
> **9D-2-C17 — дубль `EngineeringException`.** Одно исключение на критерий: **repository
> pre-check** (явная проверка существующего перед вставкой → `EVAL_EXCEPTION_DUPLICATE`) **плюс**
> DB `UNIQUE(revision_id, criterion_id)` как страховка от гонок.

> **Решения блока 9D-2E (хардненинг; зафиксировано 2026-07-19).** Реализация 9D-2A–2D
> закрыла ядро, lifecycle, валидацию и API; следующий блок **9D-2E** дорабатывает три
> контура. Только эти три решения — предмет 9D-2E; внешний approval workflow остаётся 9D-3.
>
> **9D-2-C18 — копирование содержимого при `create_revision`.** Новая ревизия-пересмотр
> **копирует** источники, критерии и исключения предыдущей (`previous_revision_id =`
> текущая `current_revision_id`) в одной транзакции. Правила копии: каждой копии — **новый
> `id`** и **новый `revision_id`**; исключения копируются с **`is_draft_copy = true`**
> (действительность/согласования/подтверждения не наследуются); `exception.criterion_id`
> **переназначается на копию критерия** (по карте старый→новый `criterion_id`); у источников
> **`verified_at` сбрасывается в `NULL`** (перед `prepare` требуется повторная сверка),
> `source_hash`/`hash_schema_version` переносятся как есть. Действующая (`EFFECTIVE`)
> ревизия **не** переводится в `SUPERSEDED` при создании новой — замещение выполняется
> **только** на `set_effective` новой ревизии. Пишется **одно** агрегированное событие
> `EVALUATION_CREATED` с метаданными количества копий (`sources_copied`, `criteria_copied`,
> `exceptions_copied`, `source_revision_id`, `revision_no`).
>
> **9D-2-C19 — `REVIEW_OVERDUE` идемпотентно и по команде.** Событие **не** эмитится на
> `GET`. Вводится **идемпотентная команда** `check_review_overdue` (эндпойнт
> `POST …/engineering-evaluation-revisions/{id}/check-review-overdue`). Событие
> `REVIEW_OVERDUE` создаётся **один раз на просроченный review-цикл**: цикл идентифицируется
> текущим значением `review_due_at` действующей ревизии; повторный вызов при уже
> зафиксированном событии этого цикла — **no-op**. После `confirm-review`, обновляющего
> `review_due_at`, начинается новый цикл (новое событие возможно). Команда **не меняет**
> `status` ревизии и **не меняет** `QualityFinding`. Сервис-метод спроектирован пригодным
> для будущего вызова планировщиком (идемпотентность + системный актор).
>
> **9D-2-C20 — fingerprint подтверждения пересмотра.** `request-review-confirmation`
> генерирует **`correlation_id`** и сохраняет в метаданных события `REVIEW_CONFIRMATION_REQUESTED`:
> **`content_fingerprint`** (хэш содержимого действующей ревизии), **`proposed_review_due_at`**
> (обязателен) и **`revision_version`**. `confirm-review` **пересчитывает** fingerprint по
> текущему содержимому; **несовпадение → `EVAL_REVIEW_CONTENT_CHANGED`**. Подтверждение
> применяет **`proposed_review_due_at` из запроса** (подтверждающий не задаёт срок отдельно);
> `REVIEW_CONFIRMED` связывается с запросом тем же `correlation_id`. **Повторное подтверждение
> уже закрытого запроса запрещено** (нет открытого запроса → `EVAL_REVIEW_NOT_REQUESTED`).
> Инвариант «разные акторы request/confirm» (`EVAL_SAME_ACTOR_REVIEW`) сохраняется.
>
> Состав fingerprint (детерминированный `sha256`, алгоритм — Spec §16): **decision — 13 полей**
> (`evaluation_outcome`, `classification`, `recommended_disposition`, `confirmed_severity`,
> `impact_scope`, `rationale`, `confidence_level`, `confidence_note`, `residual_risk`,
> `application_conditions`, `required_approval_route`, `revision_reason`, `supersedes_impact`);
> **sources — 8 полей** (`id`, `source_role`, `source_entity_type`, `source_entity_id`,
> `source_revision_id`, `source_hash`, `hash_schema_version`, `applicability_note`); плюс
> `criteria` и `exceptions` (состав — Spec §16). **Исключены:** `status`/`version`; audit
> actor/time; `verified_at`; `review_due_at`; `is_draft_copy`; события и review-metadata.

### 1. Контекст

`QualityFinding` фиксирует инженерно значимое выявленное несоответствие, отклонение,
наблюдение или иной результат качества, требующий решения ОГС. Сам `QualityFinding` не
должен содержать окончательное инженерное решение. Для этого вводится отдельный объект
`EngineeringEvaluation`, обеспечивающий: структурированное инженерное решение;
обязательное инженерное обоснование; связь с доказательной базой; проверку применимых
критериев; сохранение истории пересмотров; разделение подготовки и окончательной
фиксации; неизменяемый инженерный след. Task 9D-2 не должна превращаться в универсальный
механизм согласований, исполнения ремонтов, управления политиками или массовой
переоценки.

### 2. Решение

**2.1. Одна логическая оценка.** Один `QualityFinding` имеет не более одной логической
цепочки `EngineeringEvaluation`, представленной последовательными ревизиями. Параллельные
независимые оценки ОГС/ОТК/технадзора в рамках `EngineeringEvaluation` не допускаются.
Согласования сторон — отдельный процесс, не создающий параллельные оценки.

**2.2. Ревизионность.** Каждая значимая редакция создаётся отдельной ревизией. Предыдущая
ревизия не редактируется, не удаляется, остаётся в истории и сохраняет связь со следующей.
Черновые технические сохранения новых ревизий не создают. Новая ревизия создаётся только
после фиксации предыдущей либо при отзыве подготовленной ревизии.

**2.3. Граница изменяемости.** Ревизия изменяема только в статусе `DRAFT`. После передачи
на фиксацию неизменяемыми становятся: инженерный исход (`evaluation_outcome`) и
рекомендованный маршрут (`recommended_disposition`); инженерная классификация
(`classification`, `confirmed_severity`, `impact_scope`); инженерное обоснование; источники;
критерии; инженерные исключения; уровень уверенности; остаточный риск; условия применения;
срок пересмотра; структурированные директивы (если входят в реализацию). Возврат на
доработку до окончательной фиксации не создаёт новую ревизию: текущая рабочая ревизия
возвращается в `DRAFT`, факт возврата сохраняется событием.

**2.4. Разделение ролей.** `WELDING_ENGINEER` создаёт ревизию, редактирует `DRAFT`,
формирует доказательную базу, определяет критерии, готовит решение, передаёт на фиксацию.
`CHIEF_WELDER` проверяет подготовленную ревизию, возвращает на доработку, фиксирует
решение, подтверждает пересмотр действующей оценки без изменения содержания. Подготовка и
окончательная фиксация не выполняются одной ролью в стандартном workflow.

**2.5. Структурированное решение (решение 9D-2-C01).** Каждая ревизия содержит **два**
обязательных структурированных поля и обязательное текстовое обоснование:

- `evaluation_outcome` — **что установлено** инженерно (исход оценки). Согласованный перечень:

  ```text
  ACCEPTABLE                — соответствует применимым критериям
  NONCONFORMING             — установлено несоответствие
  CONDITIONALLY_ACCEPTABLE  — приемлемо при выполнении условий/ограничений
  INSUFFICIENT_DATA         — данных недостаточно для окончательного вывода
  NOT_APPLICABLE            — применимые критерии отсутствуют / оценка неприменима
  ```

- `recommended_disposition` — **рекомендация ОГС** по дальнейшему маршруту (необязывающая).
  Согласованный перечень:

  ```text
  NONE                    — дополнительных действий не требуется
  ACCEPT_AS_IS            — рекомендовано принять как есть
  REPAIR                  — рекомендован ремонт
  REWORK                  — рекомендована переделка
  ADDITIONAL_INSPECTION   — рекомендован дополнительный контроль
  REJECT                  — рекомендовано отклонение/выбраковка
  ```

`recommended_disposition` — **рекомендация, а не распоряжение**: она не меняет статус
`QualityFinding`, не создаёт `Repair` / `Rework` / `Inspection`, не является действующим
распоряжением. Единственный источник официального исполняемого решения — отдельная сущность
`FindingDisposition` (Task 9D-4); `recommended_disposition` служит для неё входом при
подготовке. Свободный текст не заменяет ни `evaluation_outcome`, ни `recommended_disposition`.

**2.6. Влияние на QualityFinding (решение 9D-2-C01).** Зафиксированная оценка **не** меняет
статус `QualityFinding` сама по себе и **не** маршрутизирует finding напрямую. Оценка
фиксирует инженерный исход (`evaluation_outcome`) и рекомендацию (`recommended_disposition`).
Официальный исполняемый маршрут finding задаётся **только** отдельной сущностью
`FindingDisposition` (Task 9D-4), которая может опираться на рекомендацию оценки как на вход.

`evaluation_outcome` при этом **ограничивает** допустимый набор последующих `FindingDisposition`
(например, при `NONCONFORMING` рекомендация/disposition `ACCEPT_AS_IS` требует оформленного
`EngineeringException`), но само изменение статуса, создание `Repair` / `Rework` / `Inspection`
и закрытие finding остаются за контуром `FindingDisposition`. Ориентировочная связь исхода и
типового дальнейшего состояния finding:

```text
ACCEPTABLE                → finding может быть завершён (через FindingDisposition, после обязательных согласований)
CONDITIONALLY_ACCEPTABLE  → finding остаётся активным до подтверждения условий (через FindingDisposition)
NONCONFORMING             → finding остаётся активным до исполнения назначенной FindingDisposition
INSUFFICIENT_DATA         → до получения и оценки дополнительных данных окончательная FindingDisposition не назначается
NOT_APPLICABLE            → оценка неприменима; маршрут определяется контекстом finding через FindingDisposition
```

`EngineeringEvaluation` формирует инженерный вывод и рекомендацию, но не владеет workflow
ремонта, переделки, контроля и не выполняет переходы статуса finding.

**2.7. Пересмотр решения.** Новая ревизия может изменить тип ранее зафиксированного
решения при новых данных, изменении критериев или ошибке. Обязательно сохраняются: ссылка
на предыдущую ревизию; причина пересмотра; новое обоснование; влияние на ранее
созданные/выполненные действия. Производственный след предыдущего решения не удаляется и не
переписывается; автоматическая отмена связанных объектов запрещена.

**2.8. Действующая ревизия.** Зафиксированная ревизия не всегда становится действующей
немедленно — момент вступления зависит от типа решения, критичности finding, наличия
исключений, требований проекта и обязательных внешних согласований. Без согласования ревизия
может стать `EFFECTIVE` после фиксации; с согласованием — ожидает завершения отдельного
процесса. Одновременно `EFFECTIVE` может быть только одна ревизия; предыдущая сохраняет
действие до вступления новой, после чего получает `SUPERSEDED`. Ретроактивное вступление в
силу запрещено.

**2.9. Отзыв подготовленной ревизии.** Ревизия на согласовании или в ожидании вступления
может быть отозвана: получает конечный статус `WITHDRAWN`, остаётся неизменяемой, не
возвращается в `DRAFT`, не может стать `EFFECTIVE`, сохраняет причину/автора/дату отзыва.
Следующая версия — новая ревизия.

**2.10. Источники оценки.** Ревизия хранит структурированные ссылки на источники
(`QualityFinding`, результаты и ревизии методов контроля, лабораторные заключения,
инженерные документы, данные о сварном соединении, производственные записи, предыдущие
оценки, иные проверяемые сущности). Для ревизионной сущности обязательна ссылка на
конкретную ревизию. Для неревизионной сохраняются `source_entity_type`, `source_entity_id`,
`source_role`, `source_hash`, `hash_schema_version`, `verified_at`. Полный снимок исходной
сущности внутри оценки не создаётся.

**2.11. Актуальность неревизионных источников.** Для неревизионных источников хранится
контрольный хэш инженерно значимых полей; состав полей задаётся системным версионируемым
профилем по каждому `source_entity_type` (пользователь поля не выбирает произвольно). Перед
передачей на фиксацию и перед окончательной фиксацией система повторно проверяет актуальность.
Если источник изменился, недоступен или не соответствует сохранённой ревизии/хэшу — фиксация
блокируется. Автоматическая подмена источника или обновление хэша запрещены.

**2.12. Роли источников.** Одна ревизия может содержать несколько источников одного типа.
Роли: `PRIMARY_EVIDENCE`, `SUPPORTING_EVIDENCE`, `ACCEPTANCE_CRITERIA`, `CONTEXT`,
`REJECTED_EVIDENCE`. Для каждого смыслового блока — один основной источник. Комментарий о
применимости обязателен для основного, спорного, отклонённого источника и для исключения из
доказательной базы.

**2.13. Критерии оценки.** Применимые нормы и критерии приемки оформляются отдельными
записями `EngineeringEvaluationCriterion`: ссылка на нормативный/проектный/технический
источник; раздел/пункт/таблица/требование; проверяемый параметр; фактическое значение;
допустимое значение/условие; единица измерения; результат сравнения; инженерный комментарий.
Базовые результаты: `COMPLIES`, `DOES_NOT_COMPLY`, `CONDITIONALLY_COMPLIES`, `NOT_APPLICABLE`,
`INSUFFICIENT_DATA`. Критерии — часть конкретной ревизии и проходят ту же границу
неизменяемости.

**2.14. Приёмка (ACCEPTABLE / рекомендация ACCEPT_AS_IS).** Исход
`evaluation_outcome = ACCEPTABLE` допускается, только если все применимые критерии имеют
результат `COMPLIES` или `NOT_APPLICABLE`. При наличии `DOES_NOT_COMPLY` или
`CONDITIONALLY_COMPLIES` исход `ACCEPTABLE` неприменим — используется
`CONDITIONALLY_ACCEPTABLE`; при `INSUFFICIENT_DATA` по критериям — исход `INSUFFICIENT_DATA`.
Рекомендация `recommended_disposition = ACCEPT_AS_IS` при отклонении от критерия (исход
`CONDITIONALLY_ACCEPTABLE`) требует явного `EngineeringException` либо иного канонического
основания. Свободный текст критерии не обходит.

**2.15. EngineeringException.** Отступление от конкретного критерия оформляется отдельной
сущностью; одно исключение — один критерий. Содержит: ссылку на ревизию; ссылку на критерий;
основание; обоснование; остаточный риск; условия/ограничения применения; требуемый маршрут
согласования; автора и дату. Одна ревизия может содержать несколько исключений. При новой
ревизии исключения копируются только как черновые заготовки — действительность, согласования
и подтверждения не наследуются.

**2.16. Уровень уверенности.** Обязателен при повышенной неопределённости
(`INSUFFICIENT_DATA`, `CONDITIONALLY_COMPLIES`, использование исключения, неполные/косвенные
доказательства, повышенный остаточный риск). Шкала `HIGH` / `MEDIUM` / `LOW`; при `LOW`
обязательны пояснение причин и меры снижения риска. Не заменяет критерии, источники и
обоснование.

**2.17. Остаточный риск.** Обязателен, если после решения сохраняются значимые риски или
ограничения (исключения; условное соответствие; уверенность `MEDIUM`/`LOW`; условия
эксплуатации; временные компенсирующие меры; рекомендация `ACCEPT_AS_IS` при отклонении).
Описывает:
сохраняющуюся опасность/неопределённость; последствия; условия допустимости; компенсирующие
меры; ограничения применения; ответственного за контроль условий (если применимо).

**2.18. Срок пересмотра.** `review_due_at` обязателен для условных и риск-ориентированных
решений. Наступление срока не отменяет оценку автоматически, создаёт событие `REVIEW_OVERDUE`,
требует повторной проверки и может ограничивать новые зависимые действия. Без изменения
содержания пересмотр оформляется событием `REVIEW_CONFIRMED` (готовит `WELDING_ENGINEER`,
подтверждает `CHIEF_WELDER`). При изменении источников/критериев/решения/исключений/риска/
условий — требуется новая ревизия.

**2.19. Минимальная комплектность.** Перед передачей на фиксацию система выполняет проверку.
Минимально обязательны: структурированный `evaluation_outcome`; `recommended_disposition`
(когда исход предполагает дальнейший маршрут); обоснование; хотя бы один источник;
хотя бы один критерий; доступность источников; актуальность ссылок и хэшей; обязательные
комментарии по основным/спорным/отклонённым источникам; необходимые исключения; уровень
уверенности (когда обязателен); остаточный риск (когда обязателен); срок пересмотра (когда
обязателен). Результат критерия `INSUFFICIENT_DATA` не блокирует фиксацию автоматически —
допустимость зависит от исхода (при недостаточности данных допустимы
`evaluation_outcome = INSUFFICIENT_DATA` с `recommended_disposition = ADDITIONAL_INSPECTION`,
а также обоснованный `NONCONFORMING` с рекомендацией `REPAIR` / `REWORK` / `REJECT`). Исход
`ACCEPTABLE` при отклонении от критерия запрещён; при обоснованном отступлении используется
`CONDITIONALLY_ACCEPTABLE` с оформленным `EngineeringException`.

### 3. Предварительный lifecycle

```text
DRAFT → PREPARED → FIXED → EFFECTIVE → SUPERSEDED
```

Дополнительные состояния: `WITHDRAWN`, `PENDING_APPROVAL` (возврат `PREPARED → DRAFT` —
событие `EVALUATION_RETURNED`, причина обязательна; `RETURNED_FOR_REVISION` не статус, 9D-2-C09). Точный
набор статусов уточняется в Implementation Spec при сохранении инвариантов: редактируется
только `DRAFT`; после `PREPARED` содержание неизменяемо; `WITHDRAWN` — конечный статус;
`SUPERSEDED` — только после вступления новой ревизии в силу; одновременно `EFFECTIVE` — одна
ревизия.

### 4. События и аудит

```text
EVALUATION_CREATED   EVALUATION_UPDATED   EVALUATION_PREPARED   EVALUATION_RETURNED
EVALUATION_FIXED     EVALUATION_WITHDRAWN EVALUATION_BECAME_EFFECTIVE   EVALUATION_SUPERSEDED
SOURCE_ADDED         SOURCE_REMOVED       SOURCE_REVERIFIED
REVIEW_OVERDUE       REVIEW_CONFIRMED
```

Событие хранит: тип; ревизию оценки; actor; actor_role; дату/время; комментарий; технический
контекст; `correlation_id` (если применимо). События не заменяют хранение ревизий и
содержимого решения.

### 5. Инварианты

1. Один `QualityFinding` — не более одной логической цепочки `EngineeringEvaluation`.
2. Одновременно действующей может быть только одна ревизия.
3. После передачи на фиксацию содержание ревизии неизменяемо.
4. Новая версия решения создаётся только новой ревизией.
5. Зафиксированная ревизия не удаляется физически.
6. Ревизионный источник всегда связывается с конкретной ревизией.
7. Неревизионный источник проверяется по версионируемому хэшу значимых полей.
8. Недоступный или изменившийся источник блокирует фиксацию.
9. При отклонении от критерия исход `ACCEPTABLE` неприменим; обоснованное отступление
   оформляется исходом `CONDITIONALLY_ACCEPTABLE` с `EngineeringException`.
10. Одно `EngineeringException` относится только к одному критерию.
11. Предыдущая действующая ревизия сохраняет действие до вступления новой.
12. Новая ревизия не вступает в силу задним числом.
13. Производственные действия и их история не удаляются при пересмотре решения.
14. Подготовка — `WELDING_ENGINEER`, фиксация — `CHIEF_WELDER`.
15. `EngineeringEvaluation` фиксирует `evaluation_outcome` и необязывающий
    `recommended_disposition`; официальное исполняемое решение по finding — только
    отдельная `FindingDisposition` (9D-2-C01). `recommended_disposition` не меняет статус
    finding и не создаёт производственных действий.
16. `classification`, `confirmed_severity` и `impact_scope` — часть ревизии (9D-2-C02/C07);
    актуальные значения читаются только из `EFFECTIVE`-ревизии; поля `QualityFinding` ими не
    переписываются. `classification` согласована с `evaluation_outcome` (9D-2-C07), иначе
    `EVAL_CLASSIFICATION_OUTCOME_MISMATCH`.
17. Действующая оценка — `EFFECTIVE` `EngineeringEvaluationRevision`; статус `APPROVED` не
    вводится (9D-2-C03).
18. Task 9D-2 не меняет `QualityFinding.status` (9D-2-C04): после `EFFECTIVE`-оценки finding
    остаётся `UNDER_EVALUATION`; переход в `DISPOSITION_PENDING` — контур `FindingDisposition`
    (Task 9D-4).

### 6. Граница Task 9D-2

**Входит:** модели логической оценки и ревизий; lifecycle ревизии; поля исхода и рекомендации
(`evaluation_outcome`, `recommended_disposition`); инженерная классификация ревизии
(`classification`, `confirmed_severity`, `impact_scope` — 9D-2-C02/C07); источники оценки; проверка ревизионных и неревизионных источников; критерии; `EngineeringException`;
уровень уверенности; остаточный риск; срок пересмотра; базовые события и аудит; RBAC; API;
миграция; тесты; контрактные поля для будущего согласования и исполнения.

**Не входит:** универсальный `ApprovalRoute`; версии маршрутов согласования; решения внешних
согласующих; `ApprovalCondition`; исполнение условий согласования; полный workflow
`EvaluationDirective`; создание ремонта/переделки/доп. контроля; перенос исполнения между
директивами; управление версиями политик; `PolicyImpactAssessment`; кампании массовой
переоценки; автоматическое закрытие производственных workflow; изменение
`QualityFinding.status` (в т.ч. переход `UNDER_EVALUATION → DISPOSITION_PENDING` — Task 9D-4);
вычисление состояния `Joint`/`ProductionHold` по `impact_scope` (Task 9D-4); файлы, печатные
формы и импорт.

### 7. Последующие контуры

Расширенные контуры оформляются отдельно (ненумерованные последующие отдельные контуры):

- Engineering Approval Workflow
- Evaluation Directives and Execution Linkage
- Policy Governance and Reassessment

Нумерация и место этих контуров в IMPLEMENTATION_PLAN определяются отдельно. ADR-021 не
резервирует за ними номера задач.

Каждый контур требует отдельной архитектурной сессии и отдельного ADR либо дополнения к канону.

### 8. Последствия

**Положительные:** сохраняется полный инженерный след; решение отделено от finding и
производственного исполнения; исключается редактирование выпущенного заключения; критерии и
доказательства структурированы; поддерживается контролируемый пересмотр; ОГС/согласование/
исполнение не смешиваются; Task 9D-2 остаётся реализуемой по объёму.

**Отрицательные:** модель сложнее CRUD; нужна отдельная таблица ревизий; нужны проверки
актуальности источников; нужно версионирование профилей хэширования; часть workflow отложена.

**Компромиссы:** полный снимок источников внутри оценки не хранится; согласование не
реализуется в 9D-2; производственные директивы представлены только контрактной границей;
массовая переоценка политик вынесена из scope.

### 9. Итоговое архитектурное правило

`EngineeringEvaluation` — отдельное ревизионное инженерное решение ОГС по одному
`QualityFinding`, фиксирующее **что установлено** (`evaluation_outcome`) и **необязывающую
рекомендацию** по маршруту (`recommended_disposition`). Черновик готовит `WELDING_ENGINEER`,
фиксирует `CHIEF_WELDER`; после фиксации решение не изменяется и сохраняет структурированную
доказательную базу, критерии, исключения и обоснование. Официальное исполняемое решение по
finding принадлежит **только** отдельной сущности `FindingDisposition` (Task 9D-4).
Согласование, производственное исполнение и управление политиками остаются отдельными
доменными контурами.

### Где зафиксировано

```text
docs/project/DECISIONS.md (ADR-021)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Task 9D-2)
```

## ADR-022. Defect Technical Model (Task 9D-3)

Дата: 2026-07-20

Статус: **ACCEPTED — принято.** Технический канон самостоятельной сущности `Defect`
под **Task 9D-3** (назначение, основание создания из `CONFIRMED_DEFECT`, кардинальность,
обязательные связи, структурированная техническая классификация, локализация, измеримые
характеристики, нормативная ссылка, lifecycle, версионность через supersede, отмена,
аудит, роли, границы). Канон утверждён без создания кода/моделей/миграций/API/тестов.

> **Историческая ссылка на disposition.** Граница `FindingDisposition` в ADR-022
> сохраняется как след первоначального проектирования. ADR-024 заменяет её целевым
> `DefectDisposition`, принадлежащим `DefectRoot`; это не меняет техническую модель самого
> `Defect`.

Статус реализации: **NOT IMPLEMENTED — не реализовано.** Реализованы предыдущие блоки
Task 9D-1 (`QualityFinding` Core) и Task 9D-2 (`EngineeringEvaluation`, ADR-021). Task 9D-3
**не начата**: следующий этап — Implementation Spec Task 9D-3. Ни модель `Defect`, ни
миграции, ни API, ни тесты этого контура ещё не создавались.

Контур: Quality / Defect.

Продолжает и детализирует: [[docs/project/DECISIONS#ADR-019. Quality Finding and Engineering Evaluation Canon (Session 008-07)|ADR-019]]
(канон участка `Quality Finding → Closure`; ADR-019 ввёл сущность `Defect` и классификацию
`CONFIRMED_DEFECT`, ADR-022 задаёт её техническую модель под Task 9D-3) ·
[[docs/project/DECISIONS#ADR-021. EngineeringEvaluation Core Canon (Task 9D-2)|ADR-021]]
(`EFFECTIVE` `EngineeringEvaluationRevision` с классификацией `CONFIRMED_DEFECT` — единственное
основание регистрации `Defect`)

Опирается на: [[docs/project/DECISIONS#ADR-017. Quality Decision, Defect, Repair and Quality Documents Canon (Session 008)|ADR-017]]
(исходный канон Defect/Repair — уточняется технической моделью, см. примечание ниже) ·
[[docs/project/DECISIONS#ADR-016. Quality Execution Model (Task 9C)|ADR-016]]
(контур Method Execution Result как первичный источник обнаружения) ·
[[docs/project/ADR-007-joint-lifecycle-and-engineering-model|ADR-007]] (Joint как контекст) ·
[[docs/project/DECISIONS#ADR-010. Joint MVP — расширенная модель, двойное согласование, история ревизий и bulk-импорт|ADR-010]] ·
[[docs/project/DECISIONS#ADR-011. Жизненный цикл Joint, согласования, блокировки и контроль областей|ADR-011]] ·
[[docs/project/DECISIONS#ADR-012. WeldOperation как неизменяемый производственный факт сварки|ADR-012]]
(неизменяемый производственный факт; ответственность сварщика — вне 9D-3) ·
[[docs/project/DECISIONS#ADR-014. Heat Treatment Integration (Task 8F)|ADR-014]] ·
[[docs/project/DECISIONS#ADR-020. Отдельный Project Control Center для контроля реализации|ADR-020]]

> **Номер.** Исходный материал задачи был озаглавлен «ADR-021 — Defect Technical Model»,
> но номер **ADR-021 уже занят** каноном `EngineeringEvaluation Core Canon (Task 9D-2)`
> (принят 2026-07-19). Технический канон `Defect` зафиксирован под следующим свободным
> номером **ADR-022** (тот же приём ранее применён при перенумерации ADR-019 → ADR-021).
> Каноническое положение Task 9D-2 (ADR-021) при этом **не изменяется**. Список
> «связанные решения» приведён в соответствие: помимо перечисленных в задаче (ADR-007,
> ADR-010, ADR-011, ADR-012, ADR-014, ADR-016, ADR-020) добавлены **ADR-017** и **ADR-019**
> как действующий канон `Defect`/`EngineeringEvaluation`/`FindingDisposition`.

> **Отношение к ADR-017 и ADR-019 (техническая модель Defect).** ADR-017 (Session 008)
> дал ранний, ремонт-ориентированный жизненный цикл `Defect`
> (`DRAFT → CONFIRMED → REPAIR_REQUIRED → REINSPECTION_REQUIRED → CLOSURE_PENDING → …`),
> смешивавший техническую запись дефекта с ремонтом, повторным контролем и закрытием.
> ADR-019 обозначил `Defect` как отдельную подтверждённую запись после `CONFIRMED_DEFECT`
> с «вычисляемым lifecycle». **ADR-022 уточняет и сужает** техническую модель `Defect` для
> реализации Task 9D-3: `Defect` — **чистый технический факт наличия дефекта** с явным
> lifecycle `DRAFT → ACTIVE → SUPERSEDED / CANCELLED` **без** статусов ремонта, повторного
> контроля и закрытия. Ремонт, переварка, повторный контроль, приемлемость, disposition и
> окончательное закрытие несоответствия относятся к **отдельным** контурам (`FindingDisposition`
> — Task 9D-4; Repair/Reweld/Reinspection — будущие контуры) и в модель `Defect` не входят.
> Ранние статусы ADR-017 (`REPAIR_REQUIRED`/`CLOSED_AFTER_REPAIR` и т.п.) в технической модели
> `Defect` **не используются**; текст ADR-017/ADR-019 не переписывается и сохраняется как
> архитектурный след с редиректом на ADR-022. Расхождение с ранним эскизом объёма 9D-3
> (`DefectAcceptanceAssessment`, `RepairExcavationZone`/`RepairWeldZone`, «вычисляемый
> lifecycle») отражено в §13 (границы Task 9D-3).

### 1. Назначение Defect

`Defect` — самостоятельная **техническая** сущность, представляющая подтверждённый
технический дефект конкретного `Joint`. `Defect` фиксирует: технический факт наличия
дефекта; происхождение; классификацию; локализацию; измеримые характеристики; нормативную
классификацию; историю версий.

`Defect` **не является**: первичным результатом НК; `QualityFinding`; `EngineeringEvaluation`;
`FindingDisposition`; заданием на ремонт; фактом Repair; фактом Reweld; повторным контролем;
решением о пригодности; окончательным закрытием несоответствия.

### 2. Основание создания

`Defect` может быть зарегистрирован **только** на основании: существующего `QualityFinding`;
действующей утверждённой `EngineeringEvaluation` (`EFFECTIVE` `EngineeringEvaluationRevision`,
ADR-021); результата оценки `CONFIRMED_DEFECT`. Прямое создание `Defect` из результата НК
**запрещено**.

Лаборатория и исполнитель НК фиксируют наблюдение и результат; **не создают, не активируют,
не подтверждают** `Defect`.

Для MVP принимается **явная отдельная доменная команда регистрации** `Defect` после утверждения
`EngineeringEvaluation`. `Defect` **не создаётся автоматически** скрытым побочным эффектом
утверждения оценки.

### 3. Кардинальность

Один `Joint` может иметь несколько `Defect`. Один `Defect` представляет один самостоятельный
технический дефект. Один `QualityFinding` по умолчанию порождает **не более одного** корневого
`Defect`. Одна `EngineeringEvaluation` с результатом `CONFIRMED_DEFECT` по умолчанию подтверждает
**не более одного** корневого `Defect`. Если одно наблюдение фактически содержит несколько
самостоятельных дефектов, оно должно быть разделено на отдельные `QualityFinding` **до**
регистрации `Defect`.

### 4. Обязательные связи

Каждый `Defect` должен быть связан с: `Joint`; исходным `QualityFinding`; утверждённой
`EngineeringEvaluation`; первичным источником обнаружения из контура Method Execution Result.

Связи с `Inspection`, `InspectionMethodAssignment`, `MethodExecution`, `LaboratoryConclusion`
по умолчанию **выводятся через цепочку происхождения** и не дублируются без доказанной
необходимости.

### 5. Техническая классификация

Модель предусматривает структурированные характеристики: тип дефекта; группу дефекта;
локализацию; ориентацию; поверхность; сторону соединения; положение по длине или окружности
шва; длину; ширину; высоту; глубину; площадь; количество индикаций; нормативное обозначение;
нормативный документ; редакцию документа; пункт или таблицу; техническое описание.

Свободный текст **не заменяет** структурированную классификацию. Коды классификаторов
**стабильны и не зависят от языка** интерфейса. Архитектура **не привязывается только к одному
стандарту**. Методоспецифические данные НК остаются в исходном результате контроля.

### 6. Lifecycle и запрещённые статусы

```text
DRAFT → ACTIVE → SUPERSEDED
          ↓
       CANCELLED
```

Смысл статусов: `DRAFT` — черновая техническая карточка; `ACTIVE` — действующая подтверждённая
техническая запись; `SUPERSEDED` — запись заменена новой технической версией; `CANCELLED` —
запись аннулирована как созданная ошибочно. Переход в `CANCELLED` может быть оформлен отдельными
правилами, если фактическая диаграмма переходов потребует более точного представления (уточняется
в Implementation Spec). `SUPERSEDED` и `CANCELLED` **не означают устранение дефекта**.

В lifecycle `Defect` **не включаются** статусы: `REPAIRED`, `REMOVED`, `REWELDED`, `ACCEPTED`,
`CLOSED`, `PASSED_REINSPECTION`. Физическое устранение дефекта, ремонт, переварка, повторный
контроль и окончательное закрытие относятся к отдельным будущим контурам. Исходный `Defect`
сохраняется как **исторический технический факт** даже после ремонта и успешного повторного
контроля.

### 7. Версионность

После активации существенные технические поля `Defect` напрямую **не изменяются**. Исправление
выполняется через **supersede**: новая версия получает новый UUID; сохраняется общий корневой
идентификатор (`defect_root`); новая версия ссылается на предыдущую; фиксируется причина изменения;
после активации новой версии предыдущая становится `SUPERSEDED`; в одной цепочке версий может быть
только **одна** действующая версия. Номер версии **не** используется как замена UUID.

> **Момент исполнения supersede (timing).** Supersede timing execution model is defined by
> **ADR-022 Addendum D-3B-S01** (см. [[ADR-022-ADDENDUM-DEFECT-SUPERSEDE-TIMING|ADR-022 Addendum
> D-3B-S01]]). Принята **supersede-time** модель: команда `supersede` атомарно переводит предыдущую
> `ACTIVE` → `SUPERSEDED` **и** создаёт новую ревизию как `DRAFT`; приёмка новой действующей версии —
> отдельной командой `activate`. Формулировка «после активации новой версии предыдущая становится
> `SUPERSEDED`» выше сохранена как исходный текст ADR-022 и уточняется аддендумом D-3B-S01 (авторитет
> по моменту исполнения). Инвариант «одна действующая версия» не меняется.

### 8. Граница с FindingDisposition

`Defect` отвечает на вопрос: **какой технический дефект подтверждён?** `FindingDisposition`
отвечает на вопрос: **что необходимо сделать с подтверждённым наблюдением или дефектом?**

В `Defect` **запрещено** хранить решения: принять без ремонта; ремонтировать; выполнить Reweld;
вырезать участок; назначить дополнительный контроль; признать соединение пригодным или непригодным.

### 9. Граница с Repair, Reweld и Reinspection

Repair, Reweld и Reinspection **не входят** в Task 9D-3. Ремонт **не изменяет** исходную запись
`Defect`. Повторный контроль **не заменяет** исходный `Defect`. Новый дефект, выявленный повторным
контролем, проходит **новый цикл**: `QualityFinding → EngineeringEvaluation → Defect`.

### 10. Роли (минимальная матрица)

- **WELDING_ENGINEER** — может: создавать `DRAFT`; заполнять технические характеристики; готовить
  новую версию; активировать в пределах предоставленного scope, если это соответствует действующей
  RBAC-политике.
- **CHIEF_WELDER** — может: выполнять действия `WELDING_ENGINEER`; активировать; подтверждать
  supersede; отменять ошибочно созданные активные записи; выполнять предусмотренный политикой override.
- **OTK** (`OTK_INSPECTOR`) — может просматривать и использовать `Defect` в процессах качества, но
  **не изменяет** техническую классификацию без отдельного полномочия.
- **NDT / лаборатория** — может просматривать связанные `Defect`, но **не может** создавать,
  активировать, отменять, supersede или квалифицировать наблюдение как `Defect`.

Окончательная RBAC-матрица определяется в Implementation Spec.

### 11. Аудит

Обязательные события: `DEFECT_DRAFT_CREATED`; `DEFECT_UPDATED`; `DEFECT_ACTIVATED`;
`DEFECT_CANCELLED`; `DEFECT_REVISION_CREATED`; `DEFECT_SUPERSEDED`.

Фиксируются: actor; время; причина отмены; причина supersede; старое и новое состояние; связь с
доменным объектом; correlation/request identifier, если он используется в проекте.

### 12. Удаление

Физическое удаление `Defect` через бизнес-API **запрещено**. Ошибочные записи отменяются через
`CANCELLED`.

### 13. Граница Task 9D-3

**Входит:** модель `Defect`; lifecycle; регистрация на основании `CONFIRMED_DEFECT`; техническая
классификация; локализация; измеримые характеристики; нормативная ссылка; версионность; отмена;
аудит; repository; services; API; RBAC; тесты; документация.

**Не входит:** `FindingDisposition`; Repair; Reweld; Reinspection; подтверждение устранения;
закрытие дефекта; acceptance decision (в т.ч. `DefectAcceptanceAssessment`); печатные формы; файлы;
вложения; импорт; массовые операции; аналитические витрины; автоматическое определение
ответственности сварщика.

> **Соотношение с ранним эскизом объёма 9D-3.** В `IMPLEMENTATION_PLAN` строка 9D-3 исторически
> перечисляла `DefectAcceptanceAssessment`, «вычисляемый lifecycle» и
> `RepairExcavationZone`/`RepairWeldZone`. ADR-022 сужает объём Task 9D-3 до **чистой технической
> модели** `Defect`: `DefectAcceptanceAssessment` относится к контуру приемлемости/`FindingDisposition`;
> зоны ремонта — к контуру Repair; lifecycle задан явно, а не как вычисляемый. Точное распределение
> перечисленных сущностей по блокам уточняется в Implementation Spec, но техническая модель `Defect`
> следует настоящему ADR.

### 14. Инварианты

1. `Defect` не существует без `Joint`.
2. `Defect` не существует без `QualityFinding`.
3. `Defect` не существует без утверждённой `EngineeringEvaluation`.
4. `EngineeringEvaluation` должна иметь результат `CONFIRMED_DEFECT`.
5. `Joint` должен совпадать во всей цепочке происхождения.
6. Один `QualityFinding` не может породить несколько действующих корневых `Defect`.
7. В одной цепочке версий может быть только одна действующая версия.
8. `ACTIVE` `Defect` не меняет происхождение напрямую.
9. `SUPERSEDED` неизменяем.
10. `CANCELLED` неизменяем, кроме разрешённых служебных полей.
11. Отмена не означает устранение дефекта.
12. Supersede не означает устранение дефекта.
13. Repair не изменяет исходный `Defect`.
14. Повторный контроль не заменяет исходный `Defect`.
15. `FindingDisposition` не хранится в `Defect`.
16. Решение о допустимости не хранится в `Defect`.
17. Лаборатория не создаёт и не активирует `Defect`.
18. Физическое удаление через бизнес-API запрещено.

### 15. Последствия

**Положительные:** `Defect` — устойчивый исторический технический факт, отделённый от disposition,
ремонта, повторного контроля и закрытия; техническая классификация структурирована и не зависит от
языка/стандарта; версионность через supersede сохраняет полный след; границы контуров не смешиваются;
Task 9D-3 остаётся реализуемой по объёму.

**Отрицательные / компромиссы:** модель сложнее CRUD (цепочка версий + happens-only-once активная
версия); часть привычного «жизненного пути дефекта» (ремонт → повторный контроль → закрытие) вынесена
в отдельные контуры и не отражается в статусе `Defect`; ранний ремонт-ориентированный lifecycle
ADR-017 в технической модели не применяется.

### Где зафиксировано

```text
docs/project/DECISIONS.md (ADR-022)
docs/project/ADR-022-ADDENDUM-DEFECT-SUPERSEDE-TIMING.md (аддендум D-3B-S01 — supersede timing)
docs/ARCHITECTURE.md (§5.10)
docs/project/ARCHITECTURE_SESSIONS.md (Архитектурное согласование Task 9D-3 / ADR-022)
docs/project/IMPLEMENTATION_PLAN_ENGINEERING_JOINTS_MVP.md (Task 9D-3)
docs/project/PROJECT_SUMMARY.md (Текущий статус архитектуры)
docs/project/UBIQUITOUS_LANGUAGE.md (Defect; Defect Root; Defect Revision; Confirmed Defect; Defect Supersede; Defect Cancellation)
```

---

## ADR-023. DefectDisposition — модель хранения уровня данных (Task 9D-4A-2)

Дата: 2026-07-21

Статус: **ACCEPTED — принято.** Три частных решения уровня данных для официального
исполняемого решения по дефекту в рамках **Task 9D-4A-2 (Database Migration)**:
представление контролируемых значений, объект-владелец ссылки (`DefectRoot`) и
модель audit-ссылок на исполнителей.

Статус реализации: **IMPLEMENTED — реализовано (уровень данных).** Созданы
ORM-модель `DefectDisposition`
(`09_Разработка/backend/app/quality/defect_disposition_models.py`), Alembic-миграция
`20260721_22_defect_dispositions` и тесты структуры БД
(`09_Разработка/backend/tests/test_defect_disposition_migration.py`). Переходы
статусов, правила утверждения, RBAC, сервисы, workflow и API — **не входят** в
Task 9D-4A-2 и остаются предметом последующих блоков Task 9D-4A.

Контур: Quality / Defect Disposition.

Продолжает и детализирует: [[docs/project/DECISIONS#ADR-022. Defect Technical Model (Task 9D-3)|ADR-022]]
(§8 «Граница с `FindingDisposition`» зарезервировал контур Task 9D-4 под официальное
исполняемое решение по дефекту; ADR-023 фиксирует его модель хранения под именем
`DefectDisposition`)

Опирается на: [[docs/project/DECISIONS#ADR-022. Defect Technical Model (Task 9D-3)|ADR-022]]
(`DefectRoot` как владелец цепочки версий `Defect`; приём частичного `UNIQUE`
«одна действующая запись на корень») ·
[[docs/project/DECISIONS#ADR-021. EngineeringEvaluation Core Canon (Task 9D-2)|ADR-021]] ·
[[docs/project/DECISIONS#ADR-019. Quality Finding and Engineering Evaluation Canon (Session 008-07)|ADR-019]]
(исходно ввёл рабочее понятие `FindingDisposition` для контура Task 9D-4) ·
[[docs/project/DECISIONS#ADR-017. Quality Decision, Defect, Repair and Quality Documents Canon (Session 008)|ADR-017]]

> **Уточнение после независимого review ADR-024.** ADR-023 первоначально трактовал переход
> от `FindingDisposition` к `DefectDisposition` как терминологическое уточнение. Проверка
> показала, что это не чистое переименование: владелец меняется с QualityFinding/evaluation
> на `DefectRoot`, а типы, lifecycle, authority и supersede timing различаются.
> ADR-023 сохраняет принятые решения только уровня хранения `DefectDisposition`; принятый
> [[docs/project/ADR-024-defect-disposition-lifecycle-authority-model|ADR-024]] отдельно
> оформляет архитектурную замену исторической модели, mapping и границы. Это действующий
> целевой канон архитектуры, но текущая реализация ещё не приведена к нему.

### Решения

**9D-4A-C01 — хранение контролируемых значений через `CHECK`, а не native PostgreSQL `ENUM`.**

- **Контекст.** Task 9D-4A вводит контролируемые значения `decision_type` и `status`.
  Существующая схема `quality` использует `VARCHAR`-поля с `CHECK`-ограничениями.
- **Решение.** Используются `VARCHAR` + `CHECK`-ограничения вместо native PostgreSQL `ENUM`.
- **Причина.** Согласованность с существующими модулями `quality`; более простая эволюция
  схемы (изменение допустимых значений не требует `ALTER TYPE`); согласование с ADR-022.
- **Влияние.** БД хранит контролируемые значения через `CHECK`-ограничения
  (`ck_defect_dispositions_decision_type`, `ck_defect_dispositions_status`). Доменные
  значения (`REPAIR_REQUIRED`, `REINSPECTION_REQUIRED`, `ACCEPT_AS_IS`, `REJECT_JOINT`;
  `DRAFT`, `PREPARED`, `APPROVED`, `ACTIVE`, `SUPERSEDED`, `CANCELLED`) не меняются.

**9D-4A-C02 — `DefectDisposition` ссылается на `DefectRoot`, а не на ревизию `Defect`.**

- **Контекст.** У дефекта может быть несколько технических ревизий (ADR-022 §7,
  supersede-цепочка). Официальное инженерное решение должно переживать смену ревизии.
- **Решение.** `DefectDisposition` ссылается на `quality.defect_roots.id`, а **не** на
  `quality.defects.id`.
- **Причина.** `DefectRoot` представляет владельца жизненного цикла дефекта. Disposition —
  решение о жизненном цикле дефекта в целом, а не об одной технической ревизии.
- **Влияние.** На один `defect_root` допускается не более одного действующего решения:
  частичный `UNIQUE`-индекс `uq_defect_dispositions_one_active_per_root` по
  `defect_root_id WHERE status = 'ACTIVE'` — тот же приём, что
  `uq_defects_one_active_per_root` (ADR-022 / 9D-3A).

**9D-4A-C03 — worker-ссылки аудита вместо универсального UUID-пользователя.**

- **Контекст.** Существующие модули `quality` используют worker-based поля аудита
  (`*_by_worker_id`), а не универсальные ссылки на учётную запись.
- **Решение.** Используются `created_by_worker_id` и `approved_by_worker_id`
  (`hr.workers.id`, тип `Integer`, без FK — переходный период) вместо универсальных
  UUID-ссылок на пользователя.
- **Причина.** Согласованность с существующей моделью авторизации проекта: допуск и
  контроль фиксируются как факт работника (`worker`), а не учётной записи (`user_account`)
  — см. AGENTS.md §5 (разделение `worker`/`welder_profile`/`user_account`).
- **Влияние.** Аудит-информация `DefectDisposition` остаётся согласованной с аудитом
  остальных сущностей домена `quality`. Пара `approved_at`/`approved_by_worker_id`
  дополнительно защищена `CHECK`-инвариантом «обе заполнены либо обе `NULL`»
  (`ck_defect_dispositions_approved_pair`).

### Примечание о терминологии Task 9D-4A

Task 9D-4A реализовала модель хранения под именем **`DefectDisposition`**. Исторический
`FindingDisposition` ADR-019 и `DefectDisposition` не являются синонимами. ADR-024
оформляет их отношение как архитектурную замену для подтверждённых Defect и
запрещает автоматический mapping типов/статусов. ADR-023 не принимает эту lifecycle-модель
самостоятельно и сохраняет силу только в своей границе уровня данных.

### Граница Task 9D-4A-2

**Входит:** ORM-модель `DefectDisposition`; Alembic-миграция таблицы
`quality.defect_dispositions`; `CHECK`-ограничения (`decision_type`, `status`,
непустое `justification`, запрет self-supersede, парность `approved_at`/
`approved_by_worker_id`); FK на `defect_roots` и self-FK supersede-цепочки
(`supersedes_disposition_id`); индексы, включая частичный `UNIQUE`; тесты структуры БД.

**Не входит:** переходы статусов (`DRAFT → PREPARED → APPROVED → ACTIVE → SUPERSEDED /
CANCELLED`); правила утверждения; RBAC; repository/services; API; события/аудит-лог
(в отличие от таблицы аудит-полей); workflow согласования — предмет последующих блоков
Task 9D-4A.

### Где зафиксировано

```text
docs/project/DECISIONS.md (ADR-023)
09_Разработка/backend/app/quality/defect_disposition_models.py
09_Разработка/backend/migrations/versions/20260721_22_defect_dispositions.py
09_Разработка/backend/tests/test_defect_disposition_migration.py
```

---

## ADR-024. DefectDisposition Lifecycle and Authority Model

Дата: 2026-07-21

Статус: **ACCEPTED**

Дата принятия: 2026-07-21.

Полный текст решения:
[[docs/project/ADR-024-defect-disposition-lifecycle-authority-model|ADR-024 — DefectDisposition Lifecycle and Authority Model]].

Architecture Session:
[[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 009 — DefectDisposition lifecycle, approval, activation and supersede governance|Session 009]].

Финальный независимый архитектурный review:
[[docs/project/ARCHITECTURE_SESSIONS#K. Итог финального независимого review|этап 2.2 — APPROVED]].

ADR-024 дополняет модель хранения ADR-023 и частично заменяет ADR-019 только в
части исторической модели `FindingDisposition`. Это архитектурная замена, а не чистое
переименование: `DefectDisposition` принадлежит `DefectRoot`, создаётся только после
подтверждённого Defect и имеет новые типы/lifecycle/authority.

В MVP `APPROVE` выполняет только effective `OTK_INSPECTOR` в GLOBAL или том же
PROJECT scope, `ACTIVATE` — только `CHIEF_WELDER`; `APPROVE_OVERRIDE` отсутствует. Без
действующего OTK-route решение нельзя утвердить и активировать. Также определены
UPDATE_DRAFT, creator-only OGS cancel для собственного DRAFT, activate-time replacement,
two-phase visibility/locking, обязательный `Idempotency-Key` для каждой mutating-команды и
immutable authorization snapshot роли/scope. Исторический OTK approval оценивается по
snapshot момента APPROVE; ACTIVATE отдельно требует текущий OTK-route, который не обязан
принадлежать прежнему approver.

Фактическая реализация оценена, но не объявлена каноном. Расхождения перечислены в
ADR-024 §K и требуют отдельной будущей Task Implementation Specification и реализации.
Настоящий документационный этап код, модели, миграции, API и тесты не меняет.

---

## Task 9D-4A-3 — DefectDisposition Approve/Activate Implementation Decision — перенесено

**Это не ADR.** Начиная с 2026-07-21 технические решения уровня Task фиксируются как
**Implementation Decision** в отдельном каталоге, а не как раздел журнала ADR — см.
[[docs/project/ARCHITECTURE_GOVERNANCE#3. Различие ADR и Implementation Decision|AGF §3]].

Принятые архитектурные полномочия и lifecycle описывает
[[docs/project/ADR-024-defect-disposition-lifecycle-authority-model|ADR-024 (ACCEPTED)]].
Implementation Decision не разрешает изменять реализацию без Task Implementation Specification.

Первоначально полный текст был зафиксирован в этом журнале в commit `dfaa87b`
(2026-07-21); отдельного файла Implementation Decision в том commit не существовало.
Единственный полный канонический текст после переноса:
[[docs/project/implementation-decisions/9D-4A-3-disposition-approve-activate-roles|docs/project/implementation-decisions/9D-4A-3-disposition-approve-activate-roles.md]].

---

## Task 9D-4A-4 — DefectDisposition Supersede Implementation Decision — перенесено

**Это не ADR.** См. примечание выше — [[docs/project/ARCHITECTURE_GOVERNANCE#3. Различие ADR и Implementation Decision|AGF §3]].

Принятые архитектурные инварианты и activate-time replacement описывает
[[docs/project/ADR-024-defect-disposition-lifecycle-authority-model|ADR-024 (ACCEPTED)]].
Implementation Decision не разрешает изменять реализацию без Task Implementation Specification.

Первоначально полный текст был зафиксирован в этом журнале в commit `d3a6d87`
(2026-07-21); отдельного файла Implementation Decision в том commit не существовало.
Единственный полный канонический текст после переноса:
[[docs/project/implementation-decisions/9D-4A-4-disposition-supersede-workflow|docs/project/implementation-decisions/9D-4A-4-disposition-supersede-workflow.md]].

---

## ADR-025. Migration Governance and Legacy Schema Boundary

Дата: 2026-07-22

Статус: **ACCEPTED**

Полный текст решения:
[[docs/project/ADR-025-migration-governance-and-legacy-schema-boundary|ADR-025 — Migration Governance and Legacy Schema Boundary]].

Architecture Session:
[[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 010 — Migration Governance and Legacy Boundary|Session 010]].

Принят вариант **Canonical / Legacy Separation**. Canonical Alembic управляет целиком
схемами `hr`, `welding`, `project`, `engineering`, `quality`; workforce, схема `test` и
кириллические legacy-таблицы исключаются из canonical metadata и Alembic. Вводятся
schema-level governance, self-contained migrations, `canonical_baseline_v1`, default
canonical runtime profile и отдельный legacy compatibility profile.

ADR-025 не отменяет ADR-005: deprecated-статус workforce, запрет новых функций и стратегия
вывода сохраняются. Заменяются только технические последствия об участии workforce в
canonical metadata/Alembic и безусловной runtime composition.

Проект реализации декомпозирован в B-03 Migration Foundation, B-04 Canonical Baseline
Adoption, TEST-DB Foundation и отдельную Task runtime compatibility profile. B-04 является
единственным владельцем переноса `alembic_version` и adoption существующей БД. Первый
независимый review завершён с `CHANGES REQUIRED`; после закрытия R-025-01…R-025-07
[[docs/project/ARCHITECTURE_SESSIONS#I. Финальный независимый review|финальный review]]
от 2026-07-22 завершён с `APPROVED`. B-03, B-04, TEST-DB Foundation и runtime profile
ещё не реализованы. Настоящий архитектурный этап не изменяет backend, models, migrations,
API или tests.

Отдельная актуализация реализации 2026-07-28, не изменяющая исторический снимок ADR
выше: B-03 завершён ранее; B-04A Canonical Baseline
Build & Verification завершён и зафиксирован implementation commit
`4487127a3042cf6a8ba003b85cffd143dc920f0e` (`done`). Source commit
`6c56f99edbd4e7346264ee14658d2076b5fd0775`; PostgreSQL 16.14; historical,
baseline и re-upgrade fingerprints равны
`ce2cd0613eab20da8d0a93d8caf675aa32fce932d909dfa219533b0c12dfc9f6`;
верифицированы 73 canonical tables и 15 governed seeds; disposable equivalence
завершена кодом `B04-EQUIVALENCE-EVIDENCE-OK`, pure suite —
`246 passed, 1 skipped`.

Это не является B-04B/adoption: active migration graph и действующий marker не
изменены, рабочая БД не затрагивалась, migration freeze остаётся активным.
B-04B, TEST-DB Foundation и runtime profile не реализованы; старт B-04B требует
отдельной maintenance readiness и отдельного решения.

---

## ADR-026. AI Data Access & Analytics Layer

Дата подготовки: 2026-07-22

Статус: **PROPOSED / FUTURE**

Решение **не принято**. Запись фиксирует будущее архитектурное направление и **не является
основанием для реализации**.

Полный текст направления:
[[docs/project/ADR-026-ai-data-access-analytics-layer|ADR-026 — AI Data Access & Analytics Layer]].

Architecture Session:
[[docs/project/ARCHITECTURE_SESSIONS#Architecture Session 011 — AI Data Access & Analytics Layer|Session 011]]
(`IN PROGRESS`).

Предмет: управляемая граница доступа к данным WeldPassport для аналитических и
AI-потребителей. Рассмотрены варианты прямого доступа к рабочей PostgreSQL (A),
расширения `reporting` (B), отдельного будущего read-only слоя с явными аналитическими
контрактами (C) и выгрузки во внешнее аналитическое хранилище (D). Предлагаемое
направление — вариант C; выбор подлежит подтверждению на Session 011.

Предлагаемые инварианты: read-only; источник истины остаётся в доменных модулях;
модульные границы ADR-006 сохраняются; RBAC и scope применяются в backend к каждому
запросу; произвольный SQL от модели запрещён; показатели вычисляет система
детерминированно; AI-вывод не является доменным решением (допуском, приёмкой,
`EngineeringEvaluation`, `Defect`, `DefectDisposition`, `ProductionHold`) и не заменяет
официальный документ; обязателен полный audit обращений; границы canonical/legacy по
ADR-025 соблюдаются.

Направление относится к этапу после стабилизации HR, Admissions, Joint lifecycle,
WeldOperation, Heat Treatment, Inspection, Defect/Disposition/Repair и RBAC. Оно не входит
в MVP, не заменяет `reporting` и не является частью Project Control Center (ADR-020).
Implementation Task не создаётся. Настоящий документационный этап не изменяет backend,
models, migrations, API, схему БД, зависимости или tests; переход к реализации требует
отдельного принятого ADR и Task Implementation Specification.

---

## ADR-027. QualityDecision Core Canon (Task 10A)

Дата подготовки: 2026-07-23 (v1) / 2026-07-23 (v2) / 2026-07-23 (ACCEPTED)

Статус: **ACCEPTED**

Принято владельцем 2026-07-23 непосредственно перед стартом Task 10A Implementation
Block 1 (Models + Migration), вне формального цикла «архитектурная сессия → ADR»
(принцип №0, [[docs/project/CONSTITUTION#2. Архитектурный принцип №0. Сначала архитектурная сессия — потом код|Конституция §2]]).
Открытые точки §K, необходимые для схемы БД Block 1, разрешены явно: Q-D1 (да,
EFFECTIVE-ревизия обязательна), Q-D2 (ограничение количества на `Joint` — только для
`DECIDED`; `DRAFT`/`UNDER_REVIEW` не ограничены — уточнено Block 1 Correction 2026-07-23
после архитектурного review; исходная редакция ACCEPTED ошибочно вводила ограничение и
для `DRAFT`/`UNDER_REVIEW`), Q-D8 (атомарный `RETURN`, 4 персистентных статуса). Q-D5
(Idempotency-Key) и Q-D9 (правка `DRAFT`) не относятся к моделям/миграции и остаются
открытыми до Block 2 (Services/API). Recovery Addendum от 2026-07-24 закрыл обе
точки: Q-D9 — scoped-правка `summary`/оснований в `DRAFT` с `expected_version` и
SUBMIT snapshot; Q-D5 — обязательный `Idempotency-Key` для пяти мутаций с
PostgreSQL response snapshot и конфликтом повторного ключа при другом payload.

Полный текст решения:
[[docs/project/ADR-027-quality-decision-core-canon|ADR-027 — QualityDecision Core Canon]].

Уточняет границу [[docs/project/DECISIONS#ADR-019. Quality Finding and Engineering Evaluation Canon (Session 008-07)|ADR-019]]
(решение 008-07-BQ), **не отменяя** декомпозицию исторического термина «Quality Decision»
ADR-017/019. `QualityDecision` — новая, отдельно поименованная сущность: официальное решение
по результатам одной или нескольких `EngineeringEvaluationRevision` конкретного `Joint`,
отсутствовавшее в каноне ADR-019/021/022/024 как самостоятельный шаг. Не является
переименованием `DefectDisposition`, не заменяет `EngineeringEvaluation`, не возвращает модель
ADR-017.

Ключевые положения v2: связь с основанием — через `DecisionBasis` → конкретная
`EngineeringEvaluationRevision` (не M:N и не header `EngineeringEvaluation`); одна ревизия не
может быть основанием более чем одного `DECIDED` `QualityDecision`; lifecycle `DRAFT →
UNDER_REVIEW → DECIDED → SUPERSEDED` (+ `RETURN`: `UNDER_REVIEW → DRAFT`, без персистентного
статуса `RETURNED`); `APPROVED` не используется как статус, approval — отдельными полями;
роли `WELDING_ENGINEER`/`OTK_INSPECTOR`, `CHIEF_WELDER` не участвует (осознанное локальное
исключение из fallback-правила 008-07-BP, по прецеденту `ADR-024`); supersede без статуса
`ACTIVE` — при новом `DECIDED` для того же `Joint` старый `DECIDED` атомарно → `SUPERSEDED` в
одной транзакции; аудит — существующая полиморфная `quality_audit_events`, без отдельной
таблицы событий, закрытый список из пяти `event_type`.

Полный список открытых точек и их разрешение при `ACCEPTED` — ADR-027, раздел «Решение по
открытым точкам ACCEPTED» и обновлённая таблица §K. Task 10A реализуется по блокам;
Implementation Block 1 (Models + Migration) не разрешает переходить к Services/API без
отдельного подтверждения. Такое подтверждение получено; recovery/remediation/API прошли
приёмку и зафиксированы commit `c1b551e` от 2026-07-24 (Task 10A: `done`).

---

## ADR-028. QualityDecision RBAC Consolidation (AS-02)

Дата: 2026-07-24

Статус: **ACCEPTED; implementation done / verified 2026-07-24**

Полный текст:
[[docs/project/ADR-028-quality-decision-rbac-consolidation|ADR-028 — QualityDecision RBAC Consolidation]].

После завершения Task 10A принят отдельный AS-02 hardening-контур без изменения
authority-модели ADR-027. Отправитель последнего `SUBMIT` текущего review-cycle не может
сам выполнить ни `RETURN`, ни `DECIDE`; текущий отправитель хранится в
`review_submitted_by_worker_id`, а нарушение возвращает `409 QD_SAME_ACTOR_REVIEW`.

Совмещение `OGS_ENGINEER` и `OTK_INSPECTOR` не блокируется, но фиксируется предупреждением
`QD_DUAL_ROLE_ASSIGNMENT`. Общий permission resolver возвращает конкретный
`AuthorizationGrant`; каждое новое событие `QualityDecision` получает immutable
`authorization_context` с ролью, assignment, scope, validity и временем проверки.
Исторические события маркируются `LEGACY_AUTHORIZATION_SNAPSHOT` без выдуманного scope.

Реализация разделена на отдельные gate Domain Model → Migration → Shared Authorization
Resolver → Workflow → Service → API → Tests. План:
[[docs/project/TASK_AS_02_QUALITY_DECISION_RBAC_IMPLEMENTATION_PLAN|AS-02 Implementation Plan]].
Все gate завершены 2026-07-24. Revision `20260724_27_qd_rbac_sod` принята PostgreSQL;
репетиция `27 → 26 → 27`, migration governance (`35 passed`), focused AS-02 regression
(`100 passed`) и полный backend regression (`1590 passed`) успешны.

---

## ADR-029. Test DB Safety Interlock

Дата: 2026-07-24

Статус: **ACCEPTED; implementation done / verified 2026-07-24**

Полный текст:
[[docs/project/ADR-029-test-db-safety-interlock|ADR-029 — Test DB Safety Interlock]].

После независимого аудита принят предварительный fail-closed interlock перед полным
TEST-DB Foundation ADR-025. Он не создаёт отдельную БД и не меняет B-04 dependency:
обычный integration pytest блокируется до первого соединения и Alembic, пока оператор
явно не подтвердит разрушительный запуск и точное имя БД с test-маркером.

Pure contract suite подтверждён без PostgreSQL: focused `10 passed`, полный
`migration_contract_tests` — `45 passed`. Application regression и Alembic в этом gate
не запускались. Реализация зафиксирована commit `2046384`.

---

## ADR-030. PostgreSQL 18 Target and B-04 Evidence Versioning

Дата: 2026-07-28

Статус: **ACCEPTED**

Полный текст:
[[docs/project/ADR-030-postgresql-18-b04-evidence-versioning|ADR-030 — PostgreSQL 18 Target and B-04 Evidence Versioning]].

PostgreSQL 18.x подтверждён как единственная целевая major-версия WeldPassport.
Точная minor-версия фиксируется в evidence, но не является постоянным архитектурным
pin. Принятое B-04A evidence PostgreSQL 16.14 сохраняется неизменяемым как
`historical_non_authorizing`: оно подтверждает историческую проверку, но не разрешает
adoption рабочей PostgreSQL 18.

Вводится отдельный блок `B-04A-R18`: fingerprint format v2, отдельный PG18 evidence
set и повторная equivalence verification на двух owned disposable PostgreSQL 18.x.
Source cut, 31 frozen revision, `canonical_baseline_v1`, active graph и marker state
не меняются. Рабочая БД в R18 не подключается.

B-04B остаётся `BLOCKED`, migration freeze остаётся active. Переход к B-04B возможен
только после отдельной приёмки B-04A-R18 и нового READ-ONLY Maintenance Readiness
Review с verdict `READY`.

---

## ADR-031. B-04 Dual-State Live and Restore-Roundtrip Evidence

Дата: 2026-07-29

Статус: **ACCEPTED**

Полный текст:
[[docs/project/ADR-031-b04-dual-state-live-restore-evidence|ADR-031 — B-04 Dual-State Live and Restore-Roundtrip Evidence]].

Maintenance Readiness Review подтвердил, что migration-built working PostgreSQL 18.3
имеет принятый live fingerprint `e9e5affd...`, а custom-format restore стабильно
переходит в fingerprint `3a9e682c...`. Повторный restore сохраняет `3a9e682c...`.
Все 135 leaf differences ограничены текстовой формой PostgreSQL deparser:
133 `CHECK` definitions и 2 partial-index predicates; прочего structural/data drift нет.

Принято разделение на два exact authorizing contract:

- существующий `active_authorizing` live fingerprint v2 — только для working DB;
- новый `active_restore_authorizing` restore-roundtrip evidence — только для restored
  rehearsal/recovery DB.

Existing PG16/PG18 evidence остаётся byte-identical. B-04R добавляет только новые
versioned artifacts и exact typed equivalence map без wildcard, regex-normalization
или игнорирования CHECK/predicate. B-04B остаётся `BLOCKED` до отдельной приёмки B-04R
и нового Maintenance Readiness verdict `READY`.

### B-04R candidate-state test harness decision

Дата: 2026-07-29

Статус: **EVIDENCE PROMOTED / PROMOTION COMMIT AUTHORIZED 2026-07-29**

После operator publication candidate-state regression выявил 15 test-harness
failures: repository assertion ожидал пустой restore index, а fixtures копировали
`postgresql-18/` целиком вместе с новым `restore-roundtrip-v1/`.

Принят вариант 1: test sandboxes строятся по explicit allowlist только из четырёх
live artifacts и exact empty restore index; repository-state test строго проверяет
текущий lifecycle stage. До promotion он требует
`candidate_pending_acceptance` и non-authorizing resolver failure; при отдельно
разрешённом promotion assertion отдельно переключается на exact
`active_restore_authorizing`. Общий допуск «pending или active» запрещён.

Specification:
[[docs/project/TASK_B-04R_CANDIDATE_STATE_TEST_REMEDIATION_SPEC|B-04R Candidate-State Test Remediation]].

Implementation Plan и Cursor/Claude Code Prompt подготовлены отдельно и не
разрешают менять runtime/evidence/index или подключаться к PostgreSQL:

- [[docs/project/TASK_B-04R_CANDIDATE_STATE_TEST_REMEDIATION_IMPLEMENTATION_PLAN|Implementation Plan]];
- [[docs/project/TASK_B-04R_CANDIDATE_STATE_TEST_REMEDIATION_CLAUDE_CODE_PROMPT|Cursor / Claude Code Prompt]].

Implementation checkpoint: два разрешённых test-файла дали focused
`59 passed`, но полный suite дал `378 passed, 1 skipped, 1 failed`.
Оставшийся test `test_b04_evidence_versioning.py` сохраняет pre-B-04R
предположение, что `postgresql-18/` не содержит versioned child directory.
Scope amendment принят владельцем 2026-07-29: третий test-файл точечно добавлен
в scope и обязан требовать exact четыре live files плюс exact directory
`restore-roundtrip-v1`. Continuation pure implementation отдельно разрешён и
выполнен: exact `1 passed`, three-file `98 passed`, full
`379 passed, 1 skipped`; 11 governed hashes неизменны. Stage/commit/promotion
не выполнялись на verification checkpoint. Code acceptance, implementation
commit и SHA `1f4d5f7a265dc7bd86b999adb95ef7070bc2ae7b` отдельно приняты
владельцем 2026-07-29. Evidence promotion отдельно разрешён и выполнен:
`accepted_at_utc=2026-07-29T08:55:32Z`,
`accepted_by=repository_owner`,
`verification_report_sha256=5480a2e4e02a085f8378ee9617da0b9ad4c04aca4a42fd0a52b933b4b75554be`.
Это не разрешает B-04B: требуется новый отдельный READ-ONLY Maintenance
Readiness Review.
