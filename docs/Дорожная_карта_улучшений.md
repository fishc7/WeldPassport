# Дорожная карта улучшений репозитория WeldPassport

Дата: 30.06.2026. Итог анализа после чистки и наведения порядка в структуре.
Документ живой — отмечать выполненное и дополнять.

## Что сделано (сессия 30.06.2026)

- Очистка: удалён `projects_my-app` (~100 ГБ, данные спасены в `D:\Archive_данные_projects`),
  база знаний вынесена в `D:\Knowledge_Base`, убран мусор и 6 пустых заготовок паспорта.
- Структура и git: обновлён `.gitignore` (`.obsidian`, `desktop.ini`, `_Входящее`),
  включено удаление файлов из среды.
- Навигация: создан корневой хаб `00_НАВИГАЦИЯ.md`; два файла навигации разведены по
  именам; привязаны ранее «висевшие» документы; заведены индексы паспорта и ролей.
- Документация процесса: `docs/КАК_РАБОТАТЬ.md` (структура, термины, приём наработок),
  папка `_Входящее`.
- Починка: убраны битые ссылки `docs/01..04`, очищен манифест, `docs/_conv.html` удалён.
- Аудит: пустых и осиротевших файлов нет.

## Что улучшить

### Приоритет 1 — организационные хвосты

- [x] Закоммитить накопленные изменения — коммит `7dba9a4` (30.06.2026), рабочее дерево чистое.
- [x] Удалить `docs/_conv.html` (артефакт конвертации).

### Приоритет 2 — контентные пробелы (суть проекта)

- [ ] Заполнить разделы проектирования: написан только «Работники и сварщики» (1 из 8).
      Ключевые пустые: Архитектура, База данных, Фактическая сварка, Исполнительная
      документация, Интерфейс, Импорт Excel.
- [ ] ТЗ по ролям: есть только ОК (1 из 7). Нужны ПТО, Мастер/Прораб, Сварщик,
      Главный сварщик, ОТК, МТО.

### Приоритет 3 — согласованность кода и документов

- [ ] Сверить `03_База_данных/Реестр сущностей` и `docs/db-schema.svg` с реальными
      моделями в `09_Разработка/src/models/` (модели правились — документация могла отстать).
- [ ] Зафиксировать стабильные решения как принятые (версии перестают быть `v0.1`).

### Приоритет 4 — удобство и автоматизация

- [ ] Сделать `00_НАВИГАЦИЯ.md` стартовой страницей Obsidian.
- [ ] Завести шаблоны: ТЗ роли, раздел проектирования — для единообразного заполнения.

## Рекомендуемый порядок

1. Закрыть хвосты приоритета 1 (коммит).
2. Взять один контентный блок приоритета 2 — рекомендуется «Фактическая сварка» (стык)
   как ядро системы, либо следующее ТЗ роли.
3. Параллельно — сверка БД ↔ ORM (приоритет 3), чтобы документация и код не расходились.

## История изменений

- v1 (30.06.2026) — создана дорожная карта по итогам анализа структуры.
